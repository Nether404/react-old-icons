import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AIM3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AIM3" {...props} />;
};

export default AIM3;
