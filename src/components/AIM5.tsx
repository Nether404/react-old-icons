import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AIM5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AIM5" {...props} />;
};

export default AIM5;
