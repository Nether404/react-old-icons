import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AIM6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AIM6" {...props} />;
};

export default AIM6;
