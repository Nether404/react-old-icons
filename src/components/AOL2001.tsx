import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AOL2001: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AOL2001" {...props} />;
};

export default AOL2001;
