import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AOL7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AOL7" {...props} />;
};

export default AOL7;
