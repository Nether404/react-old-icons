import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AOLCompanion: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AOLCompanion" {...props} />;
};

export default AOLCompanion;
