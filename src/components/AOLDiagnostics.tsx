import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AOLDiagnostics: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AOLDiagnostics" {...props} />;
};

export default AOLDiagnostics;
