import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AOLpcgame: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AOLpcgame" {...props} />;
};

export default AOLpcgame;
