import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AVGSG: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AVGSG" {...props} />;
};

export default AVGSG;
