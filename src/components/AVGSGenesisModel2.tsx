import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AVGSGenesisModel2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AVGSGenesisModel2" {...props} />;
};

export default AVGSGenesisModel2;
