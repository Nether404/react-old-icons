import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AVGSGnesh: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AVGSGnesh" {...props} />;
};

export default AVGSGnesh;
