import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AVGSGnintendoDS: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AVGSGnintendoDS" {...props} />;
};

export default AVGSGnintendoDS;
