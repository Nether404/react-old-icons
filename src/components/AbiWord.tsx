import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AbiWord: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AbiWord" {...props} />;
};

export default AbiWord;
