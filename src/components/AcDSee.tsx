import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AcDSee: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AcDSee" {...props} />;
};

export default AcDSee;
