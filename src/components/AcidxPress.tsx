import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AcidxPress: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AcidxPress" {...props} />;
};

export default AcidxPress;
