import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AcrobatReader: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AcrobatReader" {...props} />;
};

export default AcrobatReader;
