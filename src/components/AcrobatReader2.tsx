import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AcrobatReader2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AcrobatReader2" {...props} />;
};

export default AcrobatReader2;
