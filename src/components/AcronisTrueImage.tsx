import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AcronisTrueImage: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AcronisTrueImage" {...props} />;
};

export default AcronisTrueImage;
