import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AdAware: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AdAware" {...props} />;
};

export default AdAware;
