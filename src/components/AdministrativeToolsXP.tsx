import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AdministrativeToolsXP: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AdministrativeToolsXP" {...props} />;
};

export default AdministrativeToolsXP;
