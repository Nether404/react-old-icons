import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AdobeGoLiveCS: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AdobeGoLiveCS" {...props} />;
};

export default AdobeGoLiveCS;
