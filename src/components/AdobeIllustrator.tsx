import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AdobeIllustrator: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AdobeIllustrator" {...props} />;
};

export default AdobeIllustrator;
