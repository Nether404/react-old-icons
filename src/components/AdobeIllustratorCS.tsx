import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AdobeIllustratorCS: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AdobeIllustratorCS" {...props} />;
};

export default AdobeIllustratorCS;
