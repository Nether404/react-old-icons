import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AdobeImageReady: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AdobeImageReady" {...props} />;
};

export default AdobeImageReady;
