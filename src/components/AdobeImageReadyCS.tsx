import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AdobeImageReadyCS: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AdobeImageReadyCS" {...props} />;
};

export default AdobeImageReadyCS;
