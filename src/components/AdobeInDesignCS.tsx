import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AdobeInDesignCS: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AdobeInDesignCS" {...props} />;
};

export default AdobeInDesignCS;
