import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AdobeLiveMotion: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AdobeLiveMotion" {...props} />;
};

export default AdobeLiveMotion;
