import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AdobePageMill: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AdobePageMill" {...props} />;
};

export default AdobePageMill;
