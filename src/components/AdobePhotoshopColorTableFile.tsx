import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AdobePhotoshopColorTableFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AdobePhotoshopColorTableFile" {...props} />;
};

export default AdobePhotoshopColorTableFile;
