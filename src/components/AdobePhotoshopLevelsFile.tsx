import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AdobePhotoshopLevelsFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AdobePhotoshopLevelsFile" {...props} />;
};

export default AdobePhotoshopLevelsFile;
