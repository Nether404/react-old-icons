import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AdobePhotoshopTFF: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AdobePhotoshopTFF" {...props} />;
};

export default AdobePhotoshopTFF;
