import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AdobePremiere: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AdobePremiere" {...props} />;
};

export default AdobePremiere;
