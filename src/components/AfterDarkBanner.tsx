import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AfterDarkBanner: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AfterDarkBanner" {...props} />;
};

export default AfterDarkBanner;
