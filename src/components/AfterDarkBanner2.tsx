import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AfterDarkBanner2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AfterDarkBanner2" {...props} />;
};

export default AfterDarkBanner2;
