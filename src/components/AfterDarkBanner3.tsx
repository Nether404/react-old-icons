import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AfterDarkBanner3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AfterDarkBanner3" {...props} />;
};

export default AfterDarkBanner3;
