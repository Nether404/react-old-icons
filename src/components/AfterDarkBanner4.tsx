import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AfterDarkBanner4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AfterDarkBanner4" {...props} />;
};

export default AfterDarkBanner4;
