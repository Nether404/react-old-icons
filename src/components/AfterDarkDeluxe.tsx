import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AfterDarkDeluxe: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AfterDarkDeluxe" {...props} />;
};

export default AfterDarkDeluxe;
