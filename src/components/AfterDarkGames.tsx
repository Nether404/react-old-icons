import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AfterDarkGames: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AfterDarkGames" {...props} />;
};

export default AfterDarkGames;
