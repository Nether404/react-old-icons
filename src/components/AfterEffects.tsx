import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AfterEffects: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AfterEffects" {...props} />;
};

export default AfterEffects;
