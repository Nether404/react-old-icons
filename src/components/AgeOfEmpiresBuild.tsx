import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AgeOfEmpiresBuild: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AgeOfEmpiresBuild" {...props} />;
};

export default AgeOfEmpiresBuild;
