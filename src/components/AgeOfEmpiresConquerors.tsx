import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AgeOfEmpiresConquerors: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AgeOfEmpiresConquerors" {...props} />;
};

export default AgeOfEmpiresConquerors;
