import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AgeOfEmpiresII: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AgeOfEmpiresII" {...props} />;
};

export default AgeOfEmpiresII;
