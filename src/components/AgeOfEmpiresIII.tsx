import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AgeOfEmpiresIII: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AgeOfEmpiresIII" {...props} />;
};

export default AgeOfEmpiresIII;
