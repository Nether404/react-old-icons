import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AgeOfEmpiresRome: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AgeOfEmpiresRome" {...props} />;
};

export default AgeOfEmpiresRome;
