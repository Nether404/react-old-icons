import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AgeOfEmpiresVillager: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AgeOfEmpiresVillager" {...props} />;
};

export default AgeOfEmpiresVillager;
