import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AgeOfMythology: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AgeOfMythology" {...props} />;
};

export default AgeOfMythology;
