import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AgentComponent: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AgentComponent" {...props} />;
};

export default AgentComponent;
