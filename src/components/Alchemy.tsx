import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Alchemy: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Alchemy" {...props} />;
};

export default Alchemy;
