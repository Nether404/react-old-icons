import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Alcohol120: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Alcohol120" {...props} />;
};

export default Alcohol120;
