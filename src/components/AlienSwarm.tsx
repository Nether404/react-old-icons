import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AlienSwarm: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AlienSwarm" {...props} />;
};

export default AlienSwarm;
