import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AmericaOnline: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AmericaOnline" {...props} />;
};

export default AmericaOnline;
