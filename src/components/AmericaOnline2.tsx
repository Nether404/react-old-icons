import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AmericaOnline2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AmericaOnline2" {...props} />;
};

export default AmericaOnline2;
