import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Anachronox: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Anachronox" {...props} />;
};

export default Anachronox;
