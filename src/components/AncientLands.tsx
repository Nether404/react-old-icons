import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AncientLands: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AncientLands" {...props} />;
};

export default AncientLands;
