import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Anno1602: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Anno1602" {...props} />;
};

export default Anno1602;
