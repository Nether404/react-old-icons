import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AntiVir: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AntiVir" {...props} />;
};

export default AntiVir;
