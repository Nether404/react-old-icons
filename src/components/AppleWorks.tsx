import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AppleWorks: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AppleWorks" {...props} />;
};

export default AppleWorks;
