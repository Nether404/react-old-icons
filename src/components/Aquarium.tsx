import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Aquarium: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Aquarium" {...props} />;
};

export default Aquarium;
