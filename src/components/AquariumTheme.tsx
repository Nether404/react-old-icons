import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AquariumTheme: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AquariumTheme" {...props} />;
};

export default AquariumTheme;
