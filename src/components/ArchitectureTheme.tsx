import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ArchitectureTheme: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ArchitectureTheme" {...props} />;
};

export default ArchitectureTheme;
