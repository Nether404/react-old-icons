import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ArchitectureTheme1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ArchitectureTheme1" {...props} />;
};

export default ArchitectureTheme1;
