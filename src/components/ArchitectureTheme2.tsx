import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ArchitectureTheme2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ArchitectureTheme2" {...props} />;
};

export default ArchitectureTheme2;
