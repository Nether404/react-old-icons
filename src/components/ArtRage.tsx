import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ArtRage: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ArtRage" {...props} />;
};

export default ArtRage;
