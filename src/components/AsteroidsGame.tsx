import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AsteroidsGame: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AsteroidsGame" {...props} />;
};

export default AsteroidsGame;
