import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AtomicaDeluxe: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AtomicaDeluxe" {...props} />;
};

export default AtomicaDeluxe;
