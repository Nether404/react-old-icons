import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AttWorldNet: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AttWorldNet" {...props} />;
};

export default AttWorldNet;
