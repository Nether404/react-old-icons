import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const AutoCAD: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="AutoCAD" {...props} />;
};

export default AutoCAD;
