import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const BadDog: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="BadDog" {...props} />;
};

export default BadDog;
