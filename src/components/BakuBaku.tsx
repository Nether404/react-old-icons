import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const BakuBaku: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="BakuBaku" {...props} />;
};

export default BakuBaku;
