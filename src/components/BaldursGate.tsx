import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const BaldursGate: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="BaldursGate" {...props} />;
};

export default BaldursGate;
