import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const BaldursGate2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="BaldursGate2" {...props} />;
};

export default BaldursGate2;
