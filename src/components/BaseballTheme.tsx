import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const BaseballTheme: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="BaseballTheme" {...props} />;
};

export default BaseballTheme;
