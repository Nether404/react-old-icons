import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const BasiliskIIGUI: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="BasiliskIIGUI" {...props} />;
};

export default BasiliskIIGUI;
