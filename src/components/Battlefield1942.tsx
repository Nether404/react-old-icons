import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Battlefield1942: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Battlefield1942" {...props} />;
};

export default Battlefield1942;
