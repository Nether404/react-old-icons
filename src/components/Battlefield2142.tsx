import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Battlefield2142: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Battlefield2142" {...props} />;
};

export default Battlefield2142;
