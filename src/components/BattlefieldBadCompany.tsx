import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const BattlefieldBadCompany: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="BattlefieldBadCompany" {...props} />;
};

export default BattlefieldBadCompany;
