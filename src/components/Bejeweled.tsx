import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Bejeweled: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Bejeweled" {...props} />;
};

export default Bejeweled;
