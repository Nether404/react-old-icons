import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Bejeweled2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Bejeweled2" {...props} />;
};

export default Bejeweled2;
