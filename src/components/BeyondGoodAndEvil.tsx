import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const BeyondGoodAndEvil: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="BeyondGoodAndEvil" {...props} />;
};

export default BeyondGoodAndEvil;
