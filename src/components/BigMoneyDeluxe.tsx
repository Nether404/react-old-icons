import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const BigMoneyDeluxe: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="BigMoneyDeluxe" {...props} />;
};

export default BigMoneyDeluxe;
