import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const BinkVideoPlayer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="BinkVideoPlayer" {...props} />;
};

export default BinkVideoPlayer;
