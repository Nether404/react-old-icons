import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const BitLockerDriveEncryption: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="BitLockerDriveEncryption" {...props} />;
};

export default BitLockerDriveEncryption;
