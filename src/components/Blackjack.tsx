import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Blackjack: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Blackjack" {...props} />;
};

export default Blackjack;
