import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const BlobFactory: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="BlobFactory" {...props} />;
};

export default BlobFactory;
