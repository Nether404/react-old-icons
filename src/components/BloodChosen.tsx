import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const BloodChosen: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="BloodChosen" {...props} />;
};

export default BloodChosen;
