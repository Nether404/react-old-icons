import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const BookwormDeluxe: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="BookwormDeluxe" {...props} />;
};

export default BookwormDeluxe;
