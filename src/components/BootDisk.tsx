import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const BootDisk: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="BootDisk" {...props} />;
};

export default BootDisk;
