import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Boulderland: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Boulderland" {...props} />;
};

export default Boulderland;
