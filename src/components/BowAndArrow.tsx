import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const BowAndArrow: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="BowAndArrow" {...props} />;
};

export default BowAndArrow;
