import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const BoxWorld: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="BoxWorld" {...props} />;
};

export default BoxWorld;
