import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const BoxesComponent: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="BoxesComponent" {...props} />;
};

export default BoxesComponent;
