import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const BrickBreaker2Turbo: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="BrickBreaker2Turbo" {...props} />;
};

export default BrickBreaker2Turbo;
