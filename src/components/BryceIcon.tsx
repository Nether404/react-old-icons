import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const BryceIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="BryceIcon" {...props} />;
};

export default BryceIcon;
