import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const BugsIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="BugsIcon" {...props} />;
};

export default BugsIcon;
