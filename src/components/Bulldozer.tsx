import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Bulldozer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Bulldozer" {...props} />;
};

export default Bulldozer;
