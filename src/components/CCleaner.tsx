import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CCleaner: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CCleaner" {...props} />;
};

export default CCleaner;
