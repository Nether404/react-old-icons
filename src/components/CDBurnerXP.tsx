import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CDBurnerXP: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CDBurnerXP" {...props} />;
};

export default CDBurnerXP;
