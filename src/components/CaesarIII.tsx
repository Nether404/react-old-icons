import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CaesarIII: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CaesarIII" {...props} />;
};

export default CaesarIII;
