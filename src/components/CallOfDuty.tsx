import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CallOfDuty: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CallOfDuty" {...props} />;
};

export default CallOfDuty;
