import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CallOfDuty4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CallOfDuty4" {...props} />;
};

export default CallOfDuty4;
