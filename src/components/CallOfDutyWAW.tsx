import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CallOfDutyWAW: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CallOfDutyWAW" {...props} />;
};

export default CallOfDutyWAW;
