import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CamcorderXP: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CamcorderXP" {...props} />;
};

export default CamcorderXP;
