import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Carmageddon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Carmageddon" {...props} />;
};

export default Carmageddon;
