import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Catz: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Catz" {...props} />;
};

export default Catz;
