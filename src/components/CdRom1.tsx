import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CdRom1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CdRom1" {...props} />;
};

export default CdRom1;
