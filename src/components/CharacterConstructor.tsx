import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CharacterConstructor: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CharacterConstructor" {...props} />;
};

export default CharacterConstructor;
