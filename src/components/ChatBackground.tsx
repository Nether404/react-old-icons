import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ChatBackground: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ChatBackground" {...props} />;
};

export default ChatBackground;
