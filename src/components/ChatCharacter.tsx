import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ChatCharacter: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ChatCharacter" {...props} />;
};

export default ChatCharacter;
