import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ChemLab: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ChemLab" {...props} />;
};

export default ChemLab;
