import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Chess2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Chess2" {...props} />;
};

export default Chess2;
