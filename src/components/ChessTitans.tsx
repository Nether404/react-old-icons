import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ChessTitans: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ChessTitans" {...props} />;
};

export default ChessTitans;
