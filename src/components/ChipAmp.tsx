import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ChipAmp: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ChipAmp" {...props} />;
};

export default ChipAmp;
