import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ChipsChallenge: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ChipsChallenge" {...props} />;
};

export default ChipsChallenge;
