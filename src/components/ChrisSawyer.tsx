import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ChrisSawyer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ChrisSawyer" {...props} />;
};

export default ChrisSawyer;
