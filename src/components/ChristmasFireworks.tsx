import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ChristmasFireworks: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ChristmasFireworks" {...props} />;
};

export default ChristmasFireworks;
