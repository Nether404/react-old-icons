import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ChristmasForMIDI: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ChristmasForMIDI" {...props} />;
};

export default ChristmasForMIDI;
