import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ChristmasForSanta: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ChristmasForSanta" {...props} />;
};

export default ChristmasForSanta;
