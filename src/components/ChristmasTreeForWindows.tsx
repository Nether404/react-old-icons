import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ChristmasTreeForWindows: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ChristmasTreeForWindows" {...props} />;
};

export default ChristmasTreeForWindows;
