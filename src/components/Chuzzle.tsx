import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Chuzzle: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Chuzzle" {...props} />;
};

export default Chuzzle;
