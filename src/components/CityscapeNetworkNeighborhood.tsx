import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CityscapeNetworkNeighborhood: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CityscapeNetworkNeighborhood" {...props} />;
};

export default CityscapeNetworkNeighborhood;
