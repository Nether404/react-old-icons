import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CityscapeTheme: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CityscapeTheme" {...props} />;
};

export default CityscapeTheme;
