import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CivilizationIV: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CivilizationIV" {...props} />;
};

export default CivilizationIV;
