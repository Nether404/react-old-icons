import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CivilizationMapEditor: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CivilizationMapEditor" {...props} />;
};

export default CivilizationMapEditor;
