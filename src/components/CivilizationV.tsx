import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CivilizationV: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CivilizationV" {...props} />;
};

export default CivilizationV;
