import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ClarisWorks: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ClarisWorks" {...props} />;
};

export default ClarisWorks;
