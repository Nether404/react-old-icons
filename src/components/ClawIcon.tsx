import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ClawIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ClawIcon" {...props} />;
};

export default ClawIcon;
