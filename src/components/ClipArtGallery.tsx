import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ClipArtGallery: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ClipArtGallery" {...props} />;
};

export default ClipArtGallery;
