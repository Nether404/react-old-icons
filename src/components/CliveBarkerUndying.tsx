import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CliveBarkerUndying: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CliveBarkerUndying" {...props} />;
};

export default CliveBarkerUndying;
