import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CloneCD: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CloneCD" {...props} />;
};

export default CloneCD;
