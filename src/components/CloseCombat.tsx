import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CloseCombat: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CloseCombat" {...props} />;
};

export default CloseCombat;
