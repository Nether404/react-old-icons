import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CommandAndConquer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CommandAndConquer" {...props} />;
};

export default CommandAndConquer;
