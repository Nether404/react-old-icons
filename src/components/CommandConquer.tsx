import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CommandConquer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CommandConquer" {...props} />;
};

export default CommandConquer;
