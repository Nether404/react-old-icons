import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ComponentServices: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ComponentServices" {...props} />;
};

export default ComponentServices;
