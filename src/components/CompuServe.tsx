import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CompuServe: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CompuServe" {...props} />;
};

export default CompuServe;
