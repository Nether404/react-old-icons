import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CompuServeComunicate: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CompuServeComunicate" {...props} />;
};

export default CompuServeComunicate;
