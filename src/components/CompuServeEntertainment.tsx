import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CompuServeEntertainment: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CompuServeEntertainment" {...props} />;
};

export default CompuServeEntertainment;
