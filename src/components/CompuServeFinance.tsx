import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CompuServeFinance: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CompuServeFinance" {...props} />;
};

export default CompuServeFinance;
