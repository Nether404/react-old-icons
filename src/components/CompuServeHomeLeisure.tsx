import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CompuServeHomeLeisure: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CompuServeHomeLeisure" {...props} />;
};

export default CompuServeHomeLeisure;
