import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CompuServeManagerShopping: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CompuServeManagerShopping" {...props} />;
};

export default CompuServeManagerShopping;
