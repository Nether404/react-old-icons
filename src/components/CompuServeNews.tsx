import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CompuServeNews: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CompuServeNews" {...props} />;
};

export default CompuServeNews;
