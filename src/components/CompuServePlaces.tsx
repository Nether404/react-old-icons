import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CompuServePlaces: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CompuServePlaces" {...props} />;
};

export default CompuServePlaces;
