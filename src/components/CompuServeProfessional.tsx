import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CompuServeProfessional: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CompuServeProfessional" {...props} />;
};

export default CompuServeProfessional;
