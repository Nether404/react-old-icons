import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CompuServeSports: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CompuServeSports" {...props} />;
};

export default CompuServeSports;
