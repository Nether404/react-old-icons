import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CompuServeTravel: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CompuServeTravel" {...props} />;
};

export default CompuServeTravel;
