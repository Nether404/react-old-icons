import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CompuServeWeather: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CompuServeWeather" {...props} />;
};

export default CompuServeWeather;
