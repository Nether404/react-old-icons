import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ComputersNearMe: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ComputersNearMe" {...props} />;
};

export default ComputersNearMe;
