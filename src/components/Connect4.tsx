import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Connect4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Connect4" {...props} />;
};

export default Connect4;
