import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ConnectToInternet: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ConnectToInternet" {...props} />;
};

export default ConnectToInternet;
