import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CoolEdit: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CoolEdit" {...props} />;
};

export default CoolEdit;
