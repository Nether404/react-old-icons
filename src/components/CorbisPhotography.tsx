import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CorbisPhotography: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CorbisPhotography" {...props} />;
};

export default CorbisPhotography;
