import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CorbisPhotography1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CorbisPhotography1" {...props} />;
};

export default CorbisPhotography1;
