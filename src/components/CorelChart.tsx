import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CorelChart: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CorelChart" {...props} />;
};

export default CorelChart;
