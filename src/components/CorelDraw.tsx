import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CorelDraw: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CorelDraw" {...props} />;
};

export default CorelDraw;
