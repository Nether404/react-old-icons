import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CorelDraw2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CorelDraw2" {...props} />;
};

export default CorelDraw2;
