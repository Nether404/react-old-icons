import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CorelDraw3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CorelDraw3" {...props} />;
};

export default CorelDraw3;
