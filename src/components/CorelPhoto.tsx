import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CorelPhoto: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CorelPhoto" {...props} />;
};

export default CorelPhoto;
