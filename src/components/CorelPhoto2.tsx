import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CorelPhoto2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CorelPhoto2" {...props} />;
};

export default CorelPhoto2;
