import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CorelPhotoHouse: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CorelPhotoHouse" {...props} />;
};

export default CorelPhotoHouse;
