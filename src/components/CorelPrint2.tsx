import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CorelPrint2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CorelPrint2" {...props} />;
};

export default CorelPrint2;
