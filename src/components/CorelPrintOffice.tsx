import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CorelPrintOffice: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CorelPrintOffice" {...props} />;
};

export default CorelPrintOffice;
