import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CorelPrintWiz: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CorelPrintWiz" {...props} />;
};

export default CorelPrintWiz;
