import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CounterStrike: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CounterStrike" {...props} />;
};

export default CounterStrike;
