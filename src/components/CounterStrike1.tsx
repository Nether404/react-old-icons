import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CounterStrike1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CounterStrike1" {...props} />;
};

export default CounterStrike1;
