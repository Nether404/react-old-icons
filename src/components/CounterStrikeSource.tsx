import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CounterStrikeSource: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CounterStrikeSource" {...props} />;
};

export default CounterStrikeSource;
