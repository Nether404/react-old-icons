import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CreativeDisc: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CreativeDisc" {...props} />;
};

export default CreativeDisc;
