import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CreativeMixer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CreativeMixer" {...props} />;
};

export default CreativeMixer;
