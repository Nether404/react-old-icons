import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CreativeMixer6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CreativeMixer6" {...props} />;
};

export default CreativeMixer6;
