import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CreativePlayCenter: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CreativePlayCenter" {...props} />;
};

export default CreativePlayCenter;
