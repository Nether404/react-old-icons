import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CreativeRecorder: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CreativeRecorder" {...props} />;
};

export default CreativeRecorder;
