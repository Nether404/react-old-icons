import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CreativeWaveStudio: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CreativeWaveStudio" {...props} />;
};

export default CreativeWaveStudio;
