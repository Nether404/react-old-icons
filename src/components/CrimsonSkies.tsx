import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CrimsonSkies: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CrimsonSkies" {...props} />;
};

export default CrimsonSkies;
