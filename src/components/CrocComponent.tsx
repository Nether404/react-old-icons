import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CrocComponent: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CrocComponent" {...props} />;
};

export default CrocComponent;
