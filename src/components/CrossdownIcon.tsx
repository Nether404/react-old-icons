import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CrossdownIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CrossdownIcon" {...props} />;
};

export default CrossdownIcon;
