import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CruelIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CruelIcon" {...props} />;
};

export default CruelIcon;
