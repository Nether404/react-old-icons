import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CryptuiDll: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CryptuiDll" {...props} />;
};

export default CryptuiDll;
