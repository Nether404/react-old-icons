import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Crysis: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Crysis" {...props} />;
};

export default Crysis;
