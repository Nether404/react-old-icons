import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CubixWall: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CubixWall" {...props} />;
};

export default CubixWall;
