import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CuteFTP: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CuteFTP" {...props} />;
};

export default CuteFTP;
