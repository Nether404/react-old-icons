import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CuteFTP2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CuteFTP2" {...props} />;
};

export default CuteFTP2;
