import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const CyberPest: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="CyberPest" {...props} />;
};

export default CyberPest;
