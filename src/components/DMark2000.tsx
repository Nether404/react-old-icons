import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DMark2000: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DMark2000" {...props} />;
};

export default DMark2000;
