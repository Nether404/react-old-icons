import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DaemonTools: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DaemonTools" {...props} />;
};

export default DaemonTools;
