import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DangerousCreatures: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DangerousCreatures" {...props} />;
};

export default DangerousCreatures;
