import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DangerousCreatures2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DangerousCreatures2" {...props} />;
};

export default DangerousCreatures2;
