import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DangerousCreatures3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DangerousCreatures3" {...props} />;
};

export default DangerousCreatures3;
