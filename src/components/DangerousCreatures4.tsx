import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DangerousCreatures4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DangerousCreatures4" {...props} />;
};

export default DangerousCreatures4;
