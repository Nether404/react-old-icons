import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Darwinia: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Darwinia" {...props} />;
};

export default Darwinia;
