import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DataExecutionPrevention: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DataExecutionPrevention" {...props} />;
};

export default DataExecutionPrevention;
