import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DayOfTheTentacle: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DayOfTheTentacle" {...props} />;
};

export default DayOfTheTentacle;
