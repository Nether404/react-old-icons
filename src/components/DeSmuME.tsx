import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DeSmuME: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DeSmuME" {...props} />;
};

export default DeSmuME;
