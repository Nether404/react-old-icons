import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DeadSpace: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DeadSpace" {...props} />;
};

export default DeadSpace;
