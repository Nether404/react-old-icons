import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DeathmatchClassic: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DeathmatchClassic" {...props} />;
};

export default DeathmatchClassic;
