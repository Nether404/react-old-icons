import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DeluxeCd: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DeluxeCd" {...props} />;
};

export default DeluxeCd;
