import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DeusEx: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DeusEx" {...props} />;
};

export default DeusEx;
