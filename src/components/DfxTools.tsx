import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DfxTools: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DfxTools" {...props} />;
};

export default DfxTools;
