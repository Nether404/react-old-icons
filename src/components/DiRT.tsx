import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DiRT: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DiRT" {...props} />;
};

export default DiRT;
