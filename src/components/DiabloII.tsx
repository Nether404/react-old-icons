import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DiabloII: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DiabloII" {...props} />;
};

export default DiabloII;
