import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DialUpNetworking: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DialUpNetworking" {...props} />;
};

export default DialUpNetworking;
