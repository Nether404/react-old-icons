import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DiamondInControl: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DiamondInControl" {...props} />;
};

export default DiamondInControl;
