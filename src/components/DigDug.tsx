import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DigDug: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DigDug" {...props} />;
};

export default DigDug;
