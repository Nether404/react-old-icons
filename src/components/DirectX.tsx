import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DirectX: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DirectX" {...props} />;
};

export default DirectX;
