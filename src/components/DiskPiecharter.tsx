import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DiskPiecharter: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DiskPiecharter" {...props} />;
};

export default DiskPiecharter;
