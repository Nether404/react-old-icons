import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DisplayMate: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DisplayMate" {...props} />;
};

export default DisplayMate;
