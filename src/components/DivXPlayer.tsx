import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DivXPlayer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DivXPlayer" {...props} />;
};

export default DivXPlayer;
