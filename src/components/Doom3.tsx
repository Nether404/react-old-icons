import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Doom3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Doom3" {...props} />;
};

export default Doom3;
