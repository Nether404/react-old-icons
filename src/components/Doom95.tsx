import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Doom95: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Doom95" {...props} />;
};

export default Doom95;
