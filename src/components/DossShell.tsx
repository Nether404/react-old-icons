import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DossShell: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DossShell" {...props} />;
};

export default DossShell;
