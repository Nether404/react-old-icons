import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DragFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DragFile" {...props} />;
};

export default DragFile;
