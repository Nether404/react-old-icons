import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DrawMorph: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DrawMorph" {...props} />;
};

export default DrawMorph;
