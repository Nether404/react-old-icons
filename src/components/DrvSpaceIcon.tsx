import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DrvSpaceIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DrvSpaceIcon" {...props} />;
};

export default DrvSpaceIcon;
