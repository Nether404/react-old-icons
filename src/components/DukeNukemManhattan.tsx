import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DukeNukemManhattan: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DukeNukemManhattan" {...props} />;
};

export default DukeNukemManhattan;
