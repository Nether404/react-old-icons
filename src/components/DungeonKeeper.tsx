import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const DungeonKeeper: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="DungeonKeeper" {...props} />;
};

export default DungeonKeeper;
