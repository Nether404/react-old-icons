import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const EPSXe: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ePSXe" {...props} />;
};

export default EPSXe;
