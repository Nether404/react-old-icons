import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const EasyIcons: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="EasyIcons" {...props} />;
};

export default EasyIcons;
