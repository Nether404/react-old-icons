import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const EjectPC: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="EjectPC" {...props} />;
};

export default EjectPC;
