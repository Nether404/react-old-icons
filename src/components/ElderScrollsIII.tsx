import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ElderScrollsIII: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ElderScrollsIII" {...props} />;
};

export default ElderScrollsIII;
