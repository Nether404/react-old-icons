import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ElderScrollsIV: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ElderScrollsIV" {...props} />;
};

export default ElderScrollsIV;
