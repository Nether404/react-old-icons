import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const EmailZIP: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="EmailZIP" {...props} />;
};

export default EmailZIP;
