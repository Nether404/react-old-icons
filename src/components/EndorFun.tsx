import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const EndorFun: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="EndorFun" {...props} />;
};

export default EndorFun;
