import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const EvolveIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="EvolveIcon" {...props} />;
};

export default EvolveIcon;
