import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Excel2010: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Excel2010" {...props} />;
};

export default Excel2010;
