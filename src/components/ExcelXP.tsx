import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ExcelXP: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ExcelXP" {...props} />;
};

export default ExcelXP;
