import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ExcuseMaker: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ExcuseMaker" {...props} />;
};

export default ExcuseMaker;
