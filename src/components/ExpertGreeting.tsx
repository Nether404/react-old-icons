import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ExpertGreeting: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ExpertGreeting" {...props} />;
};

export default ExpertGreeting;
