import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FEARExtraction: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FEARExtraction" {...props} />;
};

export default FEARExtraction;
