import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Falcon4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Falcon4" {...props} />;
};

export default Falcon4;
