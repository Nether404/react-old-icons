import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FallingLeaves: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FallingLeaves" {...props} />;
};

export default FallingLeaves;
