import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Fallout: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Fallout" {...props} />;
};

export default Fallout;
