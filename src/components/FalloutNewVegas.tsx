import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FalloutNewVegas: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FalloutNewVegas" {...props} />;
};

export default FalloutNewVegas;
