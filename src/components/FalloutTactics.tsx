import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FalloutTactics: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FalloutTactics" {...props} />;
};

export default FalloutTactics;
