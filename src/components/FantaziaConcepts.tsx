import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FantaziaConcepts: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FantaziaConcepts" {...props} />;
};

export default FantaziaConcepts;
