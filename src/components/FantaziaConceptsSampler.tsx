import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FantaziaConceptsSampler: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FantaziaConceptsSampler" {...props} />;
};

export default FantaziaConceptsSampler;
