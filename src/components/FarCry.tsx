import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FarCry: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FarCry" {...props} />;
};

export default FarCry;
