import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FashionTheme: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FashionTheme" {...props} />;
};

export default FashionTheme;
