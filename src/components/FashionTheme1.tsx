import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FashionTheme1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FashionTheme1" {...props} />;
};

export default FashionTheme1;
