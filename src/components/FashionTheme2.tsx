import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FashionTheme2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FashionTheme2" {...props} />;
};

export default FashionTheme2;
