import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FastStoneImageViewer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FastStoneImageViewer" {...props} />;
};

export default FastStoneImageViewer;
