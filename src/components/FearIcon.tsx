import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FearIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FearIcon" {...props} />;
};

export default FearIcon;
