import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FearMp: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FearMp" {...props} />;
};

export default FearMp;
