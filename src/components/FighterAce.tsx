import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FighterAce: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FighterAce" {...props} />;
};

export default FighterAce;
