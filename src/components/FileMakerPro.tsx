import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FileMakerPro: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FileMakerPro" {...props} />;
};

export default FileMakerPro;
