import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FileZilla: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FileZilla" {...props} />;
};

export default FileZilla;
