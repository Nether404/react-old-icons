import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FilesTransferWizard: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FilesTransferWizard" {...props} />;
};

export default FilesTransferWizard;
