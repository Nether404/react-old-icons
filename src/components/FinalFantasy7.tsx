import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FinalFantasy7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FinalFantasy7" {...props} />;
};

export default FinalFantasy7;
