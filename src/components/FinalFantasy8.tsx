import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FinalFantasy8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FinalFantasy8" {...props} />;
};

export default FinalFantasy8;
