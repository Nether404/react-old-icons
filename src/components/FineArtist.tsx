import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FineArtist: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FineArtist" {...props} />;
};

export default FineArtist;
