import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FintyFlush: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FintyFlush" {...props} />;
};

export default FintyFlush;
