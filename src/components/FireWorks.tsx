import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FireWorks: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FireWorks" {...props} />;
};

export default FireWorks;
