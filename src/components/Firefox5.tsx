import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Firefox5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Firefox5" {...props} />;
};

export default Firefox5;
