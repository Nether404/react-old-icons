import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FirefoxAurora: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FirefoxAurora" {...props} />;
};

export default FirefoxAurora;
