import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FishIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FishIcon" {...props} />;
};

export default FishIcon;
