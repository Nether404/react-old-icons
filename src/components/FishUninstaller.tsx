import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FishUninstaller: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FishUninstaller" {...props} />;
};

export default FishUninstaller;
