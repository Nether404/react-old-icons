import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FlightUnlimited: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FlightUnlimited" {...props} />;
};

export default FlightUnlimited;
