import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FloppyDisk: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FloppyDisk" {...props} />;
};

export default FloppyDisk;
