import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FloppyDrive: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FloppyDrive" {...props} />;
};

export default FloppyDrive;
