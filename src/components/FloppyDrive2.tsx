import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FloppyDrive2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FloppyDrive2" {...props} />;
};

export default FloppyDrive2;
