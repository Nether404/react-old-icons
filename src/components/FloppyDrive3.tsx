import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FloppyDrive3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FloppyDrive3" {...props} />;
};

export default FloppyDrive3;
