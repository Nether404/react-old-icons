import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FloppyDriveXP: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FloppyDriveXP" {...props} />;
};

export default FloppyDriveXP;
