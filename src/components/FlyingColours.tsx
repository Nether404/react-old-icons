import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FlyingColours: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FlyingColours" {...props} />;
};

export default FlyingColours;
