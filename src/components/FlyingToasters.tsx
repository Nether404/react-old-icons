import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FlyingToasters: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FlyingToasters" {...props} />;
};

export default FlyingToasters;
