import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FontAssistant: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FontAssistant" {...props} />;
};

export default FontAssistant;
