import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FractalPainter: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FractalPainter" {...props} />;
};

export default FractalPainter;
