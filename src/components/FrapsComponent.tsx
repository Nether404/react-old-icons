import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FrapsComponent: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FrapsComponent" {...props} />;
};

export default FrapsComponent;
