import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FreeAgent: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FreeAgent" {...props} />;
};

export default FreeAgent;
