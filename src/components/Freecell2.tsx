import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Freecell2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Freecell2" {...props} />;
};

export default Freecell2;
