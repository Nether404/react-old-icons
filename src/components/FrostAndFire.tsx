import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FrostAndFire: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FrostAndFire" {...props} />;
};

export default FrostAndFire;
