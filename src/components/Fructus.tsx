import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Fructus: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Fructus" {...props} />;
};

export default Fructus;
