import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FuelIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FuelIcon" {...props} />;
};

export default FuelIcon;
