import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FujiGolf: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FujiGolf" {...props} />;
};

export default FujiGolf;
