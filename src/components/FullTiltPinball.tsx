import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const FullTiltPinball: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="FullTiltPinball" {...props} />;
};

export default FullTiltPinball;
