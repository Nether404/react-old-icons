import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GIFConstructionSet: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GIFConstructionSet" {...props} />;
};

export default GIFConstructionSet;
