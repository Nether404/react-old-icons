import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GIMP: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GIMP" {...props} />;
};

export default GIMP;
