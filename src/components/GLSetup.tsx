import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GLSetup: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GLSetup" {...props} />;
};

export default GLSetup;
