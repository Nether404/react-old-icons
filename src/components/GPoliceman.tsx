import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GPoliceman: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GPoliceman" {...props} />;
};

export default GPoliceman;
