import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GabrielKnight: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GabrielKnight" {...props} />;
};

export default GabrielKnight;
