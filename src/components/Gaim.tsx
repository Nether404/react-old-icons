import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Gaim: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Gaim" {...props} />;
};

export default Gaim;
