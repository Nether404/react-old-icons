import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Galaxian: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Galaxian" {...props} />;
};

export default Galaxian;
