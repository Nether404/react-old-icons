import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GameCube: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GameCube" {...props} />;
};

export default GameCube;
