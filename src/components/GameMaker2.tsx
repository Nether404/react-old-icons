import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GameMaker2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GameMaker2" {...props} />;
};

export default GameMaker2;
