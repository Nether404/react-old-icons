import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GameMaker8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GameMaker8" {...props} />;
};

export default GameMaker8;
