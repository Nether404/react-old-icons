import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GameSpy: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GameSpy" {...props} />;
};

export default GameSpy;
