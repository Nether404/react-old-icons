import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GamingZone: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GamingZone" {...props} />;
};

export default GamingZone;
