import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GarfieldThemeMyComputer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GarfieldThemeMyComputer" {...props} />;
};

export default GarfieldThemeMyComputer;
