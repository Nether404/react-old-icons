import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GarrysMod: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GarrysMod" {...props} />;
};

export default GarrysMod;
