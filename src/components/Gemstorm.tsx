import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Gemstorm: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Gemstorm" {...props} />;
};

export default Gemstorm;
