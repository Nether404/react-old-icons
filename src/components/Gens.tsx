import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Gens: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Gens" {...props} />;
};

export default Gens;
