import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GeometryTheme: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GeometryTheme" {...props} />;
};

export default GeometryTheme;
