import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GeometryTheme2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GeometryTheme2" {...props} />;
};

export default GeometryTheme2;
