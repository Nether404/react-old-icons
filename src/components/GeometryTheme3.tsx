import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GeometryTheme3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GeometryTheme3" {...props} />;
};

export default GeometryTheme3;
