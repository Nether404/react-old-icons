import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GeometryThemeGumyDocuments: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GeometryThemeGumyDocuments" {...props} />;
};

export default GeometryThemeGumyDocuments;
