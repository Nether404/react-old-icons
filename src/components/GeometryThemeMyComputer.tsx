import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GeometryThemeMyComputer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GeometryThemeMyComputer" {...props} />;
};

export default GeometryThemeMyComputer;
