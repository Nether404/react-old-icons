import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GetrightIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GetrightIcon" {...props} />;
};

export default GetrightIcon;
