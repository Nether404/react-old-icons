import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GizmosLaunchPad: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GizmosLaunchPad" {...props} />;
};

export default GizmosLaunchPad;
