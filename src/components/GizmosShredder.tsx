import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GizmosShredder: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GizmosShredder" {...props} />;
};

export default GizmosShredder;
