import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GizmosVault: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GizmosVault" {...props} />;
};

export default GizmosVault;
