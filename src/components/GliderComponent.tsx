import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GliderComponent: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GliderComponent" {...props} />;
};

export default GliderComponent;
