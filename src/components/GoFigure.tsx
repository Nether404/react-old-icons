import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GoFigure: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GoFigure" {...props} />;
};

export default GoFigure;
