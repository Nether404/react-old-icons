import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GoldWave: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GoldWave" {...props} />;
};

export default GoldWave;
