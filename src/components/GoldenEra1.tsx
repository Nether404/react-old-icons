import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GoldenEra1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GoldenEra1" {...props} />;
};

export default GoldenEra1;
