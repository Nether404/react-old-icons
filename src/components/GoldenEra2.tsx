import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GoldenEra2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GoldenEra2" {...props} />;
};

export default GoldenEra2;
