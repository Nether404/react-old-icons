import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Gomoku: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Gomoku" {...props} />;
};

export default Gomoku;
