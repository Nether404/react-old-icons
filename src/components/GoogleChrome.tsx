import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GoogleChrome: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GoogleChrome" {...props} />;
};

export default GoogleChrome;
