import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GoogleDesktop: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GoogleDesktop" {...props} />;
};

export default GoogleDesktop;
