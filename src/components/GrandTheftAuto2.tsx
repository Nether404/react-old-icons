import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GrandTheftAuto2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GrandTheftAuto2" {...props} />;
};

export default GrandTheftAuto2;
