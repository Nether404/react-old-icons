import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GrandTheftAutoIII: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GrandTheftAutoIII" {...props} />;
};

export default GrandTheftAutoIII;
