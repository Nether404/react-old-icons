import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GrandTheftAutoIV: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GrandTheftAutoIV" {...props} />;
};

export default GrandTheftAutoIV;
