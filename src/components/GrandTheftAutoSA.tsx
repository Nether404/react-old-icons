import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GrandTheftAutoSA: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GrandTheftAutoSA" {...props} />;
};

export default GrandTheftAutoSA;
