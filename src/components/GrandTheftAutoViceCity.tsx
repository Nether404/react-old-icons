import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GrandTheftAutoViceCity: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GrandTheftAutoViceCity" {...props} />;
};

export default GrandTheftAutoViceCity;
