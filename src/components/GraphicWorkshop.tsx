import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GraphicWorkshop: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GraphicWorkshop" {...props} />;
};

export default GraphicWorkshop;
