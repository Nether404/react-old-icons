import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GravityWell: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GravityWell" {...props} />;
};

export default GravityWell;
