import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GridComponent: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GridComponent" {...props} />;
};

export default GridComponent;
