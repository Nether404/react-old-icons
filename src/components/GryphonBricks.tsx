import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GryphonBricks: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GryphonBricks" {...props} />;
};

export default GryphonBricks;
