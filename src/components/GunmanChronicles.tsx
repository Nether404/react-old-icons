import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const GunmanChronicles: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="GunmanChronicles" {...props} />;
};

export default GunmanChronicles;
