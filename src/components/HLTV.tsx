import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HLTV: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HLTV" {...props} />;
};

export default HLTV;
