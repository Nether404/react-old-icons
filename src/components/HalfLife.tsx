import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HalfLife: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HalfLife" {...props} />;
};

export default HalfLife;
