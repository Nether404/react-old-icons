import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HalfLife2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HalfLife2" {...props} />;
};

export default HalfLife2;
