import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HalfLifeOpposingForce: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HalfLifeOpposingForce" {...props} />;
};

export default HalfLifeOpposingForce;
