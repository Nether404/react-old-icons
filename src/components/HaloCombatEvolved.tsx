import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HaloCombatEvolved: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HaloCombatEvolved" {...props} />;
};

export default HaloCombatEvolved;
