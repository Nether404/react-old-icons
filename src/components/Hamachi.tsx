import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Hamachi: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Hamachi" {...props} />;
};

export default Hamachi;
