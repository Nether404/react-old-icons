import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Hammer2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Hammer2" {...props} />;
};

export default Hammer2;
