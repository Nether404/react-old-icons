import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HandyIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HandyIcon" {...props} />;
};

export default HandyIcon;
