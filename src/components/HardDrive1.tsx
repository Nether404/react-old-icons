import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HardDrive1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HardDrive1" {...props} />;
};

export default HardDrive1;
