import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HeartsHunter: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HeartsHunter" {...props} />;
};

export default HeartsHunter;
