import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HeatIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HeatIcon" {...props} />;
};

export default HeatIcon;
