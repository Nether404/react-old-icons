import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Hellbender: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Hellbender" {...props} />;
};

export default Hellbender;
