import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HitmanCodename47: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HitmanCodename47" {...props} />;
};

export default HitmanCodename47;
