import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HitmanContracts: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HitmanContracts" {...props} />;
};

export default HitmanContracts;
