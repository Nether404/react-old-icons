import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HitmanSilent: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HitmanSilent" {...props} />;
};

export default HitmanSilent;
