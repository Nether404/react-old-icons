import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HoTMetaL: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HoTMetaL" {...props} />;
};

export default HoTMetaL;
