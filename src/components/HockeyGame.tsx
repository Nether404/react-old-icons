import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HockeyGame: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HockeyGame" {...props} />;
};

export default HockeyGame;
