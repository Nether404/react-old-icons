import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HorrorChannel: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HorrorChannel" {...props} />;
};

export default HorrorChannel;
