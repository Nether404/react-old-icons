import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HotDogPro: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HotDogPro" {...props} />;
};

export default HotDogPro;
