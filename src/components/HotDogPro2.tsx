import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HotDogPro2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HotDogPro2" {...props} />;
};

export default HotDogPro2;
