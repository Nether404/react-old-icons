import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HotDogPro3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HotDogPro3" {...props} />;
};

export default HotDogPro3;
