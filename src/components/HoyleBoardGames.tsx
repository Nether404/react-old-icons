import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HoyleBoardGames: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HoyleBoardGames" {...props} />;
};

export default HoyleBoardGames;
