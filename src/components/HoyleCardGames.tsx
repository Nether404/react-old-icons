import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HoyleCardGames: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HoyleCardGames" {...props} />;
};

export default HoyleCardGames;
