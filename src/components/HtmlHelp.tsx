import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HtmlHelp: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HtmlHelp" {...props} />;
};

export default HtmlHelp;
