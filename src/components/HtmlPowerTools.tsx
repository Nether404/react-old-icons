import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HtmlPowerTools: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HtmlPowerTools" {...props} />;
};

export default HtmlPowerTools;
