import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HtmlPowerTools2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HtmlPowerTools2" {...props} />;
};

export default HtmlPowerTools2;
