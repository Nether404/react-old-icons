import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HyperPrincessPitch: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HyperPrincessPitch" {...props} />;
};

export default HyperPrincessPitch;
