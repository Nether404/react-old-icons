import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HyperSnapDX: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HyperSnapDX" {...props} />;
};

export default HyperSnapDX;
