import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HyperTerminal: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HyperTerminal" {...props} />;
};

export default HyperTerminal;
