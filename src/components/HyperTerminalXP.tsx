import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const HyperTerminalXP: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="HyperTerminalXP" {...props} />;
};

export default HyperTerminalXP;
