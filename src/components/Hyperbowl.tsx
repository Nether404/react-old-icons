import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Hyperbowl: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Hyperbowl" {...props} />;
};

export default Hyperbowl;
