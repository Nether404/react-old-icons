import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ICQ2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ICQ2" {...props} />;
};

export default ICQ2;
