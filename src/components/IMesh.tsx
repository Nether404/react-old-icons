import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IMesh: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IMesh" {...props} />;
};

export default IMesh;
