import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ISPYMystery: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ISPYMystery" {...props} />;
};

export default ISPYMystery;
