import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IconArt: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IconArt" {...props} />;
};

export default IconArt;
