import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IconArt2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IconArt2" {...props} />;
};

export default IconArt2;
