import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IconArt3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IconArt3" {...props} />;
};

export default IconArt3;
