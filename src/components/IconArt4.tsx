import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IconArt4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IconArt4" {...props} />;
};

export default IconArt4;
