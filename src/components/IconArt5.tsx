import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IconArt5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IconArt5" {...props} />;
};

export default IconArt5;
