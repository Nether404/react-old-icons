import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons101: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons101" {...props} />;
};

export default Icons101;
