import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons1021: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons1021" {...props} />;
};

export default Icons1021;
