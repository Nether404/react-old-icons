import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons1036: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons1036" {...props} />;
};

export default Icons1036;
