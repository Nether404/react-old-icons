import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons1075: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons1075" {...props} />;
};

export default Icons1075;
