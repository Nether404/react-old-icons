import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons118: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons118" {...props} />;
};

export default Icons118;
