import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons1195: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons1195" {...props} />;
};

export default Icons1195;
