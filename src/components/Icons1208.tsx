import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons1208: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons1208" {...props} />;
};

export default Icons1208;
