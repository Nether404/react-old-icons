import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons1209: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons1209" {...props} />;
};

export default Icons1209;
