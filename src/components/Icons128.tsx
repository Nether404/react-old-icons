import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons128: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons128" {...props} />;
};

export default Icons128;
