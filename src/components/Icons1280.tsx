import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons1280: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons1280" {...props} />;
};

export default Icons1280;
