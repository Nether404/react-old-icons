import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons1282: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons1282" {...props} />;
};

export default Icons1282;
