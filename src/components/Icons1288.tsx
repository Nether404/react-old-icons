import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons1288: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons1288" {...props} />;
};

export default Icons1288;
