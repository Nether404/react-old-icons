import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons1291: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons1291" {...props} />;
};

export default Icons1291;
