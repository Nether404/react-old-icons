import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons13: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons13" {...props} />;
};

export default Icons13;
