import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons1304: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons1304" {...props} />;
};

export default Icons1304;
