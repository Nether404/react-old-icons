import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons14: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons14" {...props} />;
};

export default Icons14;
