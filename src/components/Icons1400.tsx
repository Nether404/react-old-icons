import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons1400: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons1400" {...props} />;
};

export default Icons1400;
