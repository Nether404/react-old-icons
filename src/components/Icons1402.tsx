import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons1402: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons1402" {...props} />;
};

export default Icons1402;
