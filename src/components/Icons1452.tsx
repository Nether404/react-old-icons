import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons1452: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons1452" {...props} />;
};

export default Icons1452;
