import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons1556: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons1556" {...props} />;
};

export default Icons1556;
