import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons158: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons158" {...props} />;
};

export default Icons158;
