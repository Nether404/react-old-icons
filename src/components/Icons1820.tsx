import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons1820: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons1820" {...props} />;
};

export default Icons1820;
