import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons184: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons184" {...props} />;
};

export default Icons184;
