import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons1878: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons1878" {...props} />;
};

export default Icons1878;
