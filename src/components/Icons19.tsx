import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons19: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons19" {...props} />;
};

export default Icons19;
