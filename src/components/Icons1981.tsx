import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons1981: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons1981" {...props} />;
};

export default Icons1981;
