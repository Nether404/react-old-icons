import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons1988: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons1988" {...props} />;
};

export default Icons1988;
