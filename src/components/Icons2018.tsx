import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons2018: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons2018" {...props} />;
};

export default Icons2018;
