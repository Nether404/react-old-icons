import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons2026: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons2026" {...props} />;
};

export default Icons2026;
