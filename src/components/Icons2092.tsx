import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons2092: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons2092" {...props} />;
};

export default Icons2092;
