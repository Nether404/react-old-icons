import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons2257: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons2257" {...props} />;
};

export default Icons2257;
