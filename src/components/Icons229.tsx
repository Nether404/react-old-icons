import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons229: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons229" {...props} />;
};

export default Icons229;
