import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons2308: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons2308" {...props} />;
};

export default Icons2308;
