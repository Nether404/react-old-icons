import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons2381: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons2381" {...props} />;
};

export default Icons2381;
