import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons2388: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons2388" {...props} />;
};

export default Icons2388;
