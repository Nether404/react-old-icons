import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons243: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons243" {...props} />;
};

export default Icons243;
