import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons2438: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons2438" {...props} />;
};

export default Icons2438;
