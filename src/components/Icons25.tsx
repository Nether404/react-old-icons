import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons25: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons25" {...props} />;
};

export default Icons25;
