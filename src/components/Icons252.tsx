import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons252: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons252" {...props} />;
};

export default Icons252;
