import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons2591: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons2591" {...props} />;
};

export default Icons2591;
