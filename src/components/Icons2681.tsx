import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons2681: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons2681" {...props} />;
};

export default Icons2681;
