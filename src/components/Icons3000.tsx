import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons3000: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons3000" {...props} />;
};

export default Icons3000;
