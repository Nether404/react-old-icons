import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons30002: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons30002" {...props} />;
};

export default Icons30002;
