import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons30003: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons30003" {...props} />;
};

export default Icons30003;
