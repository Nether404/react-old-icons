import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons30004: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons30004" {...props} />;
};

export default Icons30004;
