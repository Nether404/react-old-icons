import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons30005: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons30005" {...props} />;
};

export default Icons30005;
