import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons30006: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons30006" {...props} />;
};

export default Icons30006;
