import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons3491: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons3491" {...props} />;
};

export default Icons3491;
