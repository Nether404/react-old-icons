import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons3524: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons3524" {...props} />;
};

export default Icons3524;
