import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons3537: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons3537" {...props} />;
};

export default Icons3537;
