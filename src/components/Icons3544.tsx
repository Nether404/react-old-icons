import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons3544: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons3544" {...props} />;
};

export default Icons3544;
