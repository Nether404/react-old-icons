import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons3572: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons3572" {...props} />;
};

export default Icons3572;
