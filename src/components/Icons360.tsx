import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons360: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons360" {...props} />;
};

export default Icons360;
