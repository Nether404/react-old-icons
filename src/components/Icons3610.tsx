import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons3610: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons3610" {...props} />;
};

export default Icons3610;
