import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons3691: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons3691" {...props} />;
};

export default Icons3691;
