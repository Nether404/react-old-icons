import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons3704: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons3704" {...props} />;
};

export default Icons3704;
