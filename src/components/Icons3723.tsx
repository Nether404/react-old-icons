import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons3723: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons3723" {...props} />;
};

export default Icons3723;
