import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons499: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons499" {...props} />;
};

export default Icons499;
