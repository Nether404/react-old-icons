import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons561: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons561" {...props} />;
};

export default Icons561;
