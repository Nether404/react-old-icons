import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons565: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons565" {...props} />;
};

export default Icons565;
