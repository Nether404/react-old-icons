import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons602: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons602" {...props} />;
};

export default Icons602;
