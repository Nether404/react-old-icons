import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons667: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons667" {...props} />;
};

export default Icons667;
