import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons74: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons74" {...props} />;
};

export default Icons74;
