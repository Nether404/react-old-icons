import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons760: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons760" {...props} />;
};

export default Icons760;
