import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons773: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons773" {...props} />;
};

export default Icons773;
