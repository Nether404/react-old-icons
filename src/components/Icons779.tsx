import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons779: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons779" {...props} />;
};

export default Icons779;
