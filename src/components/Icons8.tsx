import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Icons8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Icons8" {...props} />;
};

export default Icons8;
