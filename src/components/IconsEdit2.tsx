import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IconsEdit2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IconsEdit2" {...props} />;
};

export default IconsEdit2;
