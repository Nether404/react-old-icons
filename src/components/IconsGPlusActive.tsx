import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IconsGPlusActive: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IconsGPlusActive" {...props} />;
};

export default IconsGPlusActive;
