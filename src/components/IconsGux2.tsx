import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IconsGux2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IconsGux2" {...props} />;
};

export default IconsGux2;
