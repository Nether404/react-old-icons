import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IconsSeven: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IconsSeven" {...props} />;
};

export default IconsSeven;
