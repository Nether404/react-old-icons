import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IconsZipper: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IconsZipper" {...props} />;
};

export default IconsZipper;
