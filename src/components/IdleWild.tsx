import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IdleWild: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IdleWild" {...props} />;
};

export default IdleWild;
