import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Ignition: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Ignition" {...props} />;
};

export default Ignition;
