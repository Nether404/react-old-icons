import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ImpossibleCreatures: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ImpossibleCreatures" {...props} />;
};

export default ImpossibleCreatures;
