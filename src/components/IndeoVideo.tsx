import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IndeoVideo: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IndeoVideo" {...props} />;
};

export default IndeoVideo;
