import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InfoPath: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InfoPath" {...props} />;
};

export default InfoPath;
