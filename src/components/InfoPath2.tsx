import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InfoPath2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InfoPath2" {...props} />;
};

export default InfoPath2;
