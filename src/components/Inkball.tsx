import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Inkball: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Inkball" {...props} />;
};

export default Inkball;
