import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InsaneIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InsaneIcon" {...props} />;
};

export default InsaneIcon;
