import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InsideYour: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InsideYour" {...props} />;
};

export default InsideYour;
