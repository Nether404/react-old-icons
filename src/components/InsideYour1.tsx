import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InsideYour1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InsideYour1" {...props} />;
};

export default InsideYour1;
