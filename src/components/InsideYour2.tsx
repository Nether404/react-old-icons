import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InsideYour2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InsideYour2" {...props} />;
};

export default InsideYour2;
