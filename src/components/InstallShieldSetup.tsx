import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InstallShieldSetup: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InstallShieldSetup" {...props} />;
};

export default InstallShieldSetup;
