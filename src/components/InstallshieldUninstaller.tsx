import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InstallshieldUninstaller: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InstallshieldUninstaller" {...props} />;
};

export default InstallshieldUninstaller;
