import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelChipsetIdentification: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelChipsetIdentification" {...props} />;
};

export default IntelChipsetIdentification;
