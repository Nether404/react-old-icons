import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPoint: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPoint" {...props} />;
};

export default IntelliPoint;
