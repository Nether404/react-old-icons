import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPoint2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPoint2" {...props} />;
};

export default IntelliPoint2;
