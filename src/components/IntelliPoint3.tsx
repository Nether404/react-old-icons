import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPoint3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPoint3" {...props} />;
};

export default IntelliPoint3;
