import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPoint4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPoint4" {...props} />;
};

export default IntelliPoint4;
