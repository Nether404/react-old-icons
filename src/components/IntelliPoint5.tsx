import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPoint5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPoint5" {...props} />;
};

export default IntelliPoint5;
