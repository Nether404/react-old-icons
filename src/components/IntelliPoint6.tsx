import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPoint6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPoint6" {...props} />;
};

export default IntelliPoint6;
