import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPointCursor: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPointCursor" {...props} />;
};

export default IntelliPointCursor;
