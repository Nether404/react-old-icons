import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPointCursor10: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPointCursor10" {...props} />;
};

export default IntelliPointCursor10;
