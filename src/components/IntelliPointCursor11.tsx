import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPointCursor11: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPointCursor11" {...props} />;
};

export default IntelliPointCursor11;
