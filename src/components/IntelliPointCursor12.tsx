import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPointCursor12: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPointCursor12" {...props} />;
};

export default IntelliPointCursor12;
