import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPointCursor14: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPointCursor14" {...props} />;
};

export default IntelliPointCursor14;
