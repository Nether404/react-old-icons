import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPointCursor15: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPointCursor15" {...props} />;
};

export default IntelliPointCursor15;
