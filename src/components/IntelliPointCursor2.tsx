import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPointCursor2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPointCursor2" {...props} />;
};

export default IntelliPointCursor2;
