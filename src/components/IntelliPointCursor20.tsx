import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPointCursor20: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPointCursor20" {...props} />;
};

export default IntelliPointCursor20;
