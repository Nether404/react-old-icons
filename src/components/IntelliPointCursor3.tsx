import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPointCursor3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPointCursor3" {...props} />;
};

export default IntelliPointCursor3;
