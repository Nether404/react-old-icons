import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPointCursor4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPointCursor4" {...props} />;
};

export default IntelliPointCursor4;
