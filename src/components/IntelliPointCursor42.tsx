import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPointCursor42: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPointCursor42" {...props} />;
};

export default IntelliPointCursor42;
