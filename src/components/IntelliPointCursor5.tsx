import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPointCursor5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPointCursor5" {...props} />;
};

export default IntelliPointCursor5;
