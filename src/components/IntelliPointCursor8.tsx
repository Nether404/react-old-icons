import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPointCursor8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPointCursor8" {...props} />;
};

export default IntelliPointCursor8;
