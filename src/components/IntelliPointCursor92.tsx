import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPointCursor92: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPointCursor92" {...props} />;
};

export default IntelliPointCursor92;
