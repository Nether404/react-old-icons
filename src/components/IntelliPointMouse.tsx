import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPointMouse: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPointMouse" {...props} />;
};

export default IntelliPointMouse;
