import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IntelliPointMouseInfo: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IntelliPointMouseInfo" {...props} />;
};

export default IntelliPointMouseInfo;
