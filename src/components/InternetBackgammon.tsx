import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetBackgammon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetBackgammon" {...props} />;
};

export default InternetBackgammon;
