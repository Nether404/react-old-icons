import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetCheckers: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetCheckers" {...props} />;
};

export default InternetCheckers;
