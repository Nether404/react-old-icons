import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetConnection: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetConnection" {...props} />;
};

export default InternetConnection;
