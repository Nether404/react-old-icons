import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetExplorer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetExplorer" {...props} />;
};

export default InternetExplorer;
