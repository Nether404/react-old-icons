import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetExplorer10: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetExplorer10" {...props} />;
};

export default InternetExplorer10;
