import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetExplorer11: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetExplorer11" {...props} />;
};

export default InternetExplorer11;
