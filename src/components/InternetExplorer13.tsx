import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetExplorer13: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetExplorer13" {...props} />;
};

export default InternetExplorer13;
