import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetExplorer4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetExplorer4" {...props} />;
};

export default InternetExplorer4;
