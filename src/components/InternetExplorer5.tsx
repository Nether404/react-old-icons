import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetExplorer5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetExplorer5" {...props} />;
};

export default InternetExplorer5;
