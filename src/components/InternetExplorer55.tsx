import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetExplorer55: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetExplorer55" {...props} />;
};

export default InternetExplorer55;
