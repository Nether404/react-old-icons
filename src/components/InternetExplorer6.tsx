import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetExplorer6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetExplorer6" {...props} />;
};

export default InternetExplorer6;
