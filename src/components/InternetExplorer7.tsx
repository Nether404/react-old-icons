import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetExplorer7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetExplorer7" {...props} />;
};

export default InternetExplorer7;
