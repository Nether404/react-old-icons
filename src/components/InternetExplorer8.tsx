import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetExplorer8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetExplorer8" {...props} />;
};

export default InternetExplorer8;
