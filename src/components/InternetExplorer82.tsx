import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetExplorer82: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetExplorer82" {...props} />;
};

export default InternetExplorer82;
