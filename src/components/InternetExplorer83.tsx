import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetExplorer83: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetExplorer83" {...props} />;
};

export default InternetExplorer83;
