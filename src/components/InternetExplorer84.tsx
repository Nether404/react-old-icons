import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetExplorer84: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetExplorer84" {...props} />;
};

export default InternetExplorer84;
