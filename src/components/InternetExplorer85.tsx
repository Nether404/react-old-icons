import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetExplorer85: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetExplorer85" {...props} />;
};

export default InternetExplorer85;
