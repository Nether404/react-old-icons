import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetExplorer86: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetExplorer86" {...props} />;
};

export default InternetExplorer86;
