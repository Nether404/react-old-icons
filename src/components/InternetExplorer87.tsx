import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetExplorer87: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetExplorer87" {...props} />;
};

export default InternetExplorer87;
