import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetExplorer88: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetExplorer88" {...props} />;
};

export default InternetExplorer88;
