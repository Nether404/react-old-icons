import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetOptions: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetOptions" {...props} />;
};

export default InternetOptions;
