import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetReversi: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetReversi" {...props} />;
};

export default InternetReversi;
