import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetSpades: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetSpades" {...props} />;
};

export default InternetSpades;
