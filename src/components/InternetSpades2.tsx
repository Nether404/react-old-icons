import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const InternetSpades2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="InternetSpades2" {...props} />;
};

export default InternetSpades2;
