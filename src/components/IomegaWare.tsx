import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IomegaWare: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IomegaWare" {...props} />;
};

export default IomegaWare;
