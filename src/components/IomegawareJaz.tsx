import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IomegawareJaz: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IomegawareJaz" {...props} />;
};

export default IomegawareJaz;
