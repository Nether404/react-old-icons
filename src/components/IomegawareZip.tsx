import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const IomegawareZip: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="IomegawareZip" {...props} />;
};

export default IomegawareZip;
