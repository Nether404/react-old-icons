import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Irfanview: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Irfanview" {...props} />;
};

export default Irfanview;
