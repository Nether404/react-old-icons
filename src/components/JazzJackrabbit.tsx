import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const JazzJackrabbit: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="JazzJackrabbit" {...props} />;
};

export default JazzJackrabbit;
