import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const JazzTheme: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="JazzTheme" {...props} />;
};

export default JazzTheme;
