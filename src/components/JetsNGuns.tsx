import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const JetsNGuns: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="JetsNGuns" {...props} />;
};

export default JetsNGuns;
