import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const JezzBall: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="JezzBall" {...props} />;
};

export default JezzBall;
