import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const JigSawed: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="JigSawed" {...props} />;
};

export default JigSawed;
