import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const JumperRedux: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="JumperRedux" {...props} />;
};

export default JumperRedux;
