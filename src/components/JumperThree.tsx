import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const JumperThree: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="JumperThree" {...props} />;
};

export default JumperThree;
