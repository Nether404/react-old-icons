import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const JungleThemeMyComputer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="JungleThemeMyComputer" {...props} />;
};

export default JungleThemeMyComputer;
