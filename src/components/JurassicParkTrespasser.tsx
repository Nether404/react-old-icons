import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const JurassicParkTrespasser: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="JurassicParkTrespasser" {...props} />;
};

export default JurassicParkTrespasser;
