import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const JustCause2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="JustCause2" {...props} />;
};

export default JustCause2;
