import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const KaZaA: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="KaZaA" {...props} />;
};

export default KaZaA;
