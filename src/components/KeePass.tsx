import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const KeePass: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="KeePass" {...props} />;
};

export default KeePass;
