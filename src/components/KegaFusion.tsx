import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const KegaFusion: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="KegaFusion" {...props} />;
};

export default KegaFusion;
