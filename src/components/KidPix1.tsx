import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const KidPix1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="KidPix1" {...props} />;
};

export default KidPix1;
