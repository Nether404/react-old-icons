import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const KidPix2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="KidPix2" {...props} />;
};

export default KidPix2;
