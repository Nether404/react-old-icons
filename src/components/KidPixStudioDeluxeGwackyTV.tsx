import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const KidPixStudioDeluxeGwackyTV: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="KidPixStudioDeluxeGwackyTV" {...props} />;
};

export default KidPixStudioDeluxeGwackyTV;
