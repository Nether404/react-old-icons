import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const KlikPlay: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="KlikPlay" {...props} />;
};

export default KlikPlay;
