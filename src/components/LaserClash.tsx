import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LaserClash: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LaserClash" {...props} />;
};

export default LaserClash;
