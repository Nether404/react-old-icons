import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LeFranglophile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LeFranglophile" {...props} />;
};

export default LeFranglophile;
