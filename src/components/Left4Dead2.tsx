import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Left4Dead2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Left4Dead2" {...props} />;
};

export default Left4Dead2;
