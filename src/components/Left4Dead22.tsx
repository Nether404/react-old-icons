import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Left4Dead22: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Left4Dead22" {...props} />;
};

export default Left4Dead22;
