import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LegoCreator: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LegoCreator" {...props} />;
};

export default LegoCreator;
