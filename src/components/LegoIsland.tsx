import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LegoIsland: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LegoIsland" {...props} />;
};

export default LegoIsland;
