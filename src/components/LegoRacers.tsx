import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LegoRacers: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LegoRacers" {...props} />;
};

export default LegoRacers;
