import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LegoRockRaiders: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LegoRockRaiders" {...props} />;
};

export default LegoRockRaiders;
