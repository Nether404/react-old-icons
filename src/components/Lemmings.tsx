import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Lemmings: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Lemmings" {...props} />;
};

export default Lemmings;
