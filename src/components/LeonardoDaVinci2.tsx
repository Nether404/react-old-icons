import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LeonardoDaVinci2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LeonardoDaVinci2" {...props} />;
};

export default LeonardoDaVinci2;
