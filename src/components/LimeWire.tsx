import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LimeWire: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LimeWire" {...props} />;
};

export default LimeWire;
