import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LodeRunner: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LodeRunner" {...props} />;
};

export default LodeRunner;
