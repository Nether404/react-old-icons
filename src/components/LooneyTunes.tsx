import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LooneyTunes: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LooneyTunes" {...props} />;
};

export default LooneyTunes;
