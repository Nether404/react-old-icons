import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LooneyTunes10: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LooneyTunes10" {...props} />;
};

export default LooneyTunes10;
