import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LooneyTunes2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LooneyTunes2" {...props} />;
};

export default LooneyTunes2;
