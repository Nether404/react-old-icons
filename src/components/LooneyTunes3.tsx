import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LooneyTunes3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LooneyTunes3" {...props} />;
};

export default LooneyTunes3;
