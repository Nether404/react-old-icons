import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LooneyTunes4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LooneyTunes4" {...props} />;
};

export default LooneyTunes4;
