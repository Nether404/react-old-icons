import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LooneyTunes5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LooneyTunes5" {...props} />;
};

export default LooneyTunes5;
