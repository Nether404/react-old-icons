import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LooneyTunes6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LooneyTunes6" {...props} />;
};

export default LooneyTunes6;
