import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LooneyTunes7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LooneyTunes7" {...props} />;
};

export default LooneyTunes7;
