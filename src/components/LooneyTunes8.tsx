import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LooneyTunes8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LooneyTunes8" {...props} />;
};

export default LooneyTunes8;
