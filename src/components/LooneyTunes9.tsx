import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LooneyTunes9: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LooneyTunes9" {...props} />;
};

export default LooneyTunes9;
