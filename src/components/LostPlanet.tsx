import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LostPlanet: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LostPlanet" {...props} />;
};

export default LostPlanet;
