import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LotusFastsite: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LotusFastsite" {...props} />;
};

export default LotusFastsite;
