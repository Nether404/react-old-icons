import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LotusFreelance: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LotusFreelance" {...props} />;
};

export default LotusFreelance;
