import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LotusIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LotusIcon" {...props} />;
};

export default LotusIcon;
