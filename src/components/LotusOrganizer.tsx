import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LotusOrganizer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LotusOrganizer" {...props} />;
};

export default LotusOrganizer;
