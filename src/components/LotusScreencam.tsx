import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LotusScreencam: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LotusScreencam" {...props} />;
};

export default LotusScreencam;
