import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LotusWordPro: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LotusWordPro" {...props} />;
};

export default LotusWordPro;
