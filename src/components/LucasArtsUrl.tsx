import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LucasArtsUrl: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LucasArtsUrl" {...props} />;
};

export default LucasArtsUrl;
