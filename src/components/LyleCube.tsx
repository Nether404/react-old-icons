import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const LyleCube: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="LyleCube" {...props} />;
};

export default LyleCube;
