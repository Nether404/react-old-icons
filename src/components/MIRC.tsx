import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MIRC: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="mIRC" {...props} />;
};

export default MIRC;
