import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MP3TagClinic: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MP3TagClinic" {...props} />;
};

export default MP3TagClinic;
