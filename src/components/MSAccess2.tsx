import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MSAccess2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MSAccess2" {...props} />;
};

export default MSAccess2;
