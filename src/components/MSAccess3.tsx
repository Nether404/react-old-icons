import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MSAccess3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MSAccess3" {...props} />;
};

export default MSAccess3;
