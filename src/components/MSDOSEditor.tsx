import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MSDOSEditor: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MSDOSEditor" {...props} />;
};

export default MSDOSEditor;
