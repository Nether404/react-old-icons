import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MSFindFast: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MSFindFast" {...props} />;
};

export default MSFindFast;
