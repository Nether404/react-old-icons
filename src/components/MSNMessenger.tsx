import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MSNMessenger: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MSNMessenger" {...props} />;
};

export default MSNMessenger;
