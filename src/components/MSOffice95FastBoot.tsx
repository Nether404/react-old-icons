import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MSOffice95FastBoot: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MSOffice95FastBoot" {...props} />;
};

export default MSOffice95FastBoot;
