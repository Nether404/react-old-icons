import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MSPublisher: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MSPublisher" {...props} />;
};

export default MSPublisher;
