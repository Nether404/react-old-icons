import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MSPublisher2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MSPublisher2" {...props} />;
};

export default MSPublisher2;
