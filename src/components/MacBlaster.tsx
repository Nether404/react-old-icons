import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MacBlaster: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MacBlaster" {...props} />;
};

export default MacBlaster;
