import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MacromediaAftershock: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MacromediaAftershock" {...props} />;
};

export default MacromediaAftershock;
