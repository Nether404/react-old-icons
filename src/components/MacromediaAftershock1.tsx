import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MacromediaAftershock1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MacromediaAftershock1" {...props} />;
};

export default MacromediaAftershock1;
