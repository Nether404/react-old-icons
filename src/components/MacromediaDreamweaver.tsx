import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MacromediaDreamweaver: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MacromediaDreamweaver" {...props} />;
};

export default MacromediaDreamweaver;
