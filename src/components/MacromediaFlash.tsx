import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MacromediaFlash: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MacromediaFlash" {...props} />;
};

export default MacromediaFlash;
