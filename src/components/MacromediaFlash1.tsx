import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MacromediaFlash1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MacromediaFlash1" {...props} />;
};

export default MacromediaFlash1;
