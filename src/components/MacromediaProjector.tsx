import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MacromediaProjector: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MacromediaProjector" {...props} />;
};

export default MacromediaProjector;
