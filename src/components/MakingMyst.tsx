import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MakingMyst: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MakingMyst" {...props} />;
};

export default MakingMyst;
