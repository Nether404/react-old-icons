import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MarbleDrop: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MarbleDrop" {...props} />;
};

export default MarbleDrop;
