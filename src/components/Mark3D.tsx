import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Mark3D: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Mark3D" {...props} />;
};

export default Mark3D;
