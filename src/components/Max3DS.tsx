import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Max3DS: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Max3DS" {...props} />;
};

export default Max3DS;
