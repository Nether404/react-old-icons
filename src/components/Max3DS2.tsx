import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Max3DS2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Max3DS2" {...props} />;
};

export default Max3DS2;
