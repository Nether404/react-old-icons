import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Max3DS3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Max3DS3" {...props} />;
};

export default Max3DS3;
