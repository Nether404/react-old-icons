import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Max3DS4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Max3DS4" {...props} />;
};

export default Max3DS4;
