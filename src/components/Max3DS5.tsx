import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Max3DS5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Max3DS5" {...props} />;
};

export default Max3DS5;
