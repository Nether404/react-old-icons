import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MaxPayne: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MaxPayne" {...props} />;
};

export default MaxPayne;
