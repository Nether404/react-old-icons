import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MaxPayne2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MaxPayne2" {...props} />;
};

export default MaxPayne2;
