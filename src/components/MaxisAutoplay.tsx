import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MaxisAutoplay: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MaxisAutoplay" {...props} />;
};

export default MaxisAutoplay;
