import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MaxisLogo: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MaxisLogo" {...props} />;
};

export default MaxisLogo;
