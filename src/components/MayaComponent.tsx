import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MayaComponent: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MayaComponent" {...props} />;
};

export default MayaComponent;
