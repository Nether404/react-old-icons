import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MazeRace: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MazeRace" {...props} />;
};

export default MazeRace;
