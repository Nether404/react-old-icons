import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const McAfeeNutsBolts2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="McAfeeNutsBolts2" {...props} />;
};

export default McAfeeNutsBolts2;
