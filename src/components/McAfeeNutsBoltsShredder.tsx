import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const McAfeeNutsBoltsShredder: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="McAfeeNutsBoltsShredder" {...props} />;
};

export default McAfeeNutsBoltsShredder;
