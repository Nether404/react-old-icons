import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const McAfeeSecurity: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="McAfeeSecurity" {...props} />;
};

export default McAfeeSecurity;
