import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const McAfeeSpamKiller: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="McAfeeSpamKiller" {...props} />;
};

export default McAfeeSpamKiller;
