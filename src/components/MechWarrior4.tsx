import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MechWarrior4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MechWarrior4" {...props} />;
};

export default MechWarrior4;
