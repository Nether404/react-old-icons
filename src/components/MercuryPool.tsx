import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MercuryPool: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MercuryPool" {...props} />;
};

export default MercuryPool;
