import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MetalGear: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MetalGear" {...props} />;
};

export default MetalGear;
