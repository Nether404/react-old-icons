import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicroMan: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicroMan" {...props} />;
};

export default MicroMan;
