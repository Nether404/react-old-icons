import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrografxDesigner: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrografxDesigner" {...props} />;
};

export default MicrografxDesigner;
