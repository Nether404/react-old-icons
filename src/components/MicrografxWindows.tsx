import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrografxWindows: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrografxWindows" {...props} />;
};

export default MicrografxWindows;
