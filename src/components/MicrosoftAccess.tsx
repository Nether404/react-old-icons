import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftAccess: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftAccess" {...props} />;
};

export default MicrosoftAccess;
