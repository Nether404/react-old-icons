import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftAccess2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftAccess2" {...props} />;
};

export default MicrosoftAccess2;
