import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftAntivirus: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftAntivirus" {...props} />;
};

export default MicrosoftAntivirus;
