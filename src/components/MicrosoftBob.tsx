import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftBob: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftBob" {...props} />;
};

export default MicrosoftBob;
