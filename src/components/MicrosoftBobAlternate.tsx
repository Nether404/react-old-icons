import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftBobAlternate: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftBobAlternate" {...props} />;
};

export default MicrosoftBobAlternate;
