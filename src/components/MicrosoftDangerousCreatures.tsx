import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftDangerousCreatures: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftDangerousCreatures" {...props} />;
};

export default MicrosoftDangerousCreatures;
