import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftDinosaurs: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftDinosaurs" {...props} />;
};

export default MicrosoftDinosaurs;
