import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftDogs: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftDogs" {...props} />;
};

export default MicrosoftDogs;
