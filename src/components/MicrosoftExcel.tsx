import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftExcel: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftExcel" {...props} />;
};

export default MicrosoftExcel;
