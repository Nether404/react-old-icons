import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftExcel2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftExcel2" {...props} />;
};

export default MicrosoftExcel2;
