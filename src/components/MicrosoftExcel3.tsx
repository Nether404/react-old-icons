import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftExcel3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftExcel3" {...props} />;
};

export default MicrosoftExcel3;
