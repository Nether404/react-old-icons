import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftGolf: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftGolf" {...props} />;
};

export default MicrosoftGolf;
