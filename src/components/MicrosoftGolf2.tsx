import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftGolf2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftGolf2" {...props} />;
};

export default MicrosoftGolf2;
