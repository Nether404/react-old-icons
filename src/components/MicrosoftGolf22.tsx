import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftGolf22: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftGolf22" {...props} />;
};

export default MicrosoftGolf22;
