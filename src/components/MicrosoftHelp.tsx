import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftHelp: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftHelp" {...props} />;
};

export default MicrosoftHelp;
