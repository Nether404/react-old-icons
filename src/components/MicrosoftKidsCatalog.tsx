import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftKidsCatalog: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftKidsCatalog" {...props} />;
};

export default MicrosoftKidsCatalog;
