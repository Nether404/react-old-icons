import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftMoney: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftMoney" {...props} />;
};

export default MicrosoftMoney;
