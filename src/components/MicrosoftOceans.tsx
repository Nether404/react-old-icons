import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOceans: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOceans" {...props} />;
};

export default MicrosoftOceans;
