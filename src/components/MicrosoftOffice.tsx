import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOffice: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOffice" {...props} />;
};

export default MicrosoftOffice;
