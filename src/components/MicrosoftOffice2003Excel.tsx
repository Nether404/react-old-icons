import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOffice2003Excel: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOffice2003Excel" {...props} />;
};

export default MicrosoftOffice2003Excel;
