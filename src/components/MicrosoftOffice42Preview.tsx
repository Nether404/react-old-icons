import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOffice42Preview: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOffice42Preview" {...props} />;
};

export default MicrosoftOffice42Preview;
