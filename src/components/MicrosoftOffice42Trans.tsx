import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOffice42Trans: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOffice42Trans" {...props} />;
};

export default MicrosoftOffice42Trans;
