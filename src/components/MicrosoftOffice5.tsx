import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOffice5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOffice5" {...props} />;
};

export default MicrosoftOffice5;
