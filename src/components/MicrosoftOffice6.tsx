import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOffice6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOffice6" {...props} />;
};

export default MicrosoftOffice6;
