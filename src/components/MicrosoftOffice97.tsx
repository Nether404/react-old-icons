import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOffice97: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOffice97" {...props} />;
};

export default MicrosoftOffice97;
