import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOffice972: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOffice972" {...props} />;
};

export default MicrosoftOffice972;
