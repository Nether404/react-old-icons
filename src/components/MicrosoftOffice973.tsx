import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOffice973: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOffice973" {...props} />;
};

export default MicrosoftOffice973;
