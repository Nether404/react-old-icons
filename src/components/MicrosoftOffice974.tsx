import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOffice974: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOffice974" {...props} />;
};

export default MicrosoftOffice974;
