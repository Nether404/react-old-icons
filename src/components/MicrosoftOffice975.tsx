import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOffice975: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOffice975" {...props} />;
};

export default MicrosoftOffice975;
