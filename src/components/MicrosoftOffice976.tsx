import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOffice976: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOffice976" {...props} />;
};

export default MicrosoftOffice976;
