import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOffice97Binder: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOffice97Binder" {...props} />;
};

export default MicrosoftOffice97Binder;
