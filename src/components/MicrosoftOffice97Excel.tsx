import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOffice97Excel: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOffice97Excel" {...props} />;
};

export default MicrosoftOffice97Excel;
