import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOffice97PhotoEditor: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOffice97PhotoEditor" {...props} />;
};

export default MicrosoftOffice97PhotoEditor;
