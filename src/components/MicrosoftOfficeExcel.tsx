import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOfficeExcel: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOfficeExcel" {...props} />;
};

export default MicrosoftOfficeExcel;
