import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOfficeExcel2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOfficeExcel2" {...props} />;
};

export default MicrosoftOfficeExcel2;
