import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOfficeOutlook: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOfficeOutlook" {...props} />;
};

export default MicrosoftOfficeOutlook;
