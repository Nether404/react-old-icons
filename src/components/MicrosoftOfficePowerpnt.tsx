import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOfficePowerpnt: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOfficePowerpnt" {...props} />;
};

export default MicrosoftOfficePowerpnt;
