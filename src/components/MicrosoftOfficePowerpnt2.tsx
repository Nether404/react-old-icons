import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOfficePowerpnt2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOfficePowerpnt2" {...props} />;
};

export default MicrosoftOfficePowerpnt2;
