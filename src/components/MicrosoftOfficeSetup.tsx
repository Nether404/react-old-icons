import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOfficeSetup: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOfficeSetup" {...props} />;
};

export default MicrosoftOfficeSetup;
