import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOfficeWord: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOfficeWord" {...props} />;
};

export default MicrosoftOfficeWord;
