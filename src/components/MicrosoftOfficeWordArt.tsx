import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOfficeWordArt: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOfficeWordArt" {...props} />;
};

export default MicrosoftOfficeWordArt;
