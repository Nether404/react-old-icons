import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOfficeXP: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOfficeXP" {...props} />;
};

export default MicrosoftOfficeXP;
