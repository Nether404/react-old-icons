import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOfficeXP2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOfficeXP2" {...props} />;
};

export default MicrosoftOfficeXP2;
