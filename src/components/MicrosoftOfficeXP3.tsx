import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOfficeXP3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOfficeXP3" {...props} />;
};

export default MicrosoftOfficeXP3;
