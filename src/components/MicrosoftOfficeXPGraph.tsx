import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOfficeXPGraph: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOfficeXPGraph" {...props} />;
};

export default MicrosoftOfficeXPGraph;
