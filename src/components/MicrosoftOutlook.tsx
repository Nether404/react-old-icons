import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftOutlook: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftOutlook" {...props} />;
};

export default MicrosoftOutlook;
