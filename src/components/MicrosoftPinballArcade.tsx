import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftPinballArcade: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftPinballArcade" {...props} />;
};

export default MicrosoftPinballArcade;
