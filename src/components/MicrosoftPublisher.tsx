import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftPublisher: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftPublisher" {...props} />;
};

export default MicrosoftPublisher;
