import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftPublisher2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftPublisher2" {...props} />;
};

export default MicrosoftPublisher2;
