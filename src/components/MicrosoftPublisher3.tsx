import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftPublisher3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftPublisher3" {...props} />;
};

export default MicrosoftPublisher3;
