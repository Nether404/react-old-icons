import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftPublisher4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftPublisher4" {...props} />;
};

export default MicrosoftPublisher4;
