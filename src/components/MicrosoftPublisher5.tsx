import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftPublisher5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftPublisher5" {...props} />;
};

export default MicrosoftPublisher5;
