import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftPublisher6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftPublisher6" {...props} />;
};

export default MicrosoftPublisher6;
