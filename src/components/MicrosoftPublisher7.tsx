import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftPublisher7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftPublisher7" {...props} />;
};

export default MicrosoftPublisher7;
