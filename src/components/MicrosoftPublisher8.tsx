import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftPublisher8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftPublisher8" {...props} />;
};

export default MicrosoftPublisher8;
