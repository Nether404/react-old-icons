import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftScenes: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftScenes" {...props} />;
};

export default MicrosoftScenes;
