import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftUndelete: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftUndelete" {...props} />;
};

export default MicrosoftUndelete;
