import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftVisualCpp: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftVisualCpp" {...props} />;
};

export default MicrosoftVisualCpp;
