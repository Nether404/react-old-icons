import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftVisualCpp10: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftVisualCpp10" {...props} />;
};

export default MicrosoftVisualCpp10;
