import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftVisualCpp12: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftVisualCpp12" {...props} />;
};

export default MicrosoftVisualCpp12;
