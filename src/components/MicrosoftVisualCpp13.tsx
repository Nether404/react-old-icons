import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftVisualCpp13: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftVisualCpp13" {...props} />;
};

export default MicrosoftVisualCpp13;
