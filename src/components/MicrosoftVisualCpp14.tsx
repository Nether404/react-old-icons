import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftVisualCpp14: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftVisualCpp14" {...props} />;
};

export default MicrosoftVisualCpp14;
