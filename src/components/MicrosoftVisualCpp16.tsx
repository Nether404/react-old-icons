import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftVisualCpp16: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftVisualCpp16" {...props} />;
};

export default MicrosoftVisualCpp16;
