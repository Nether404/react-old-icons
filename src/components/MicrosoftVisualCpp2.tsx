import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftVisualCpp2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftVisualCpp2" {...props} />;
};

export default MicrosoftVisualCpp2;
