import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftVisualCpp20: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftVisualCpp20" {...props} />;
};

export default MicrosoftVisualCpp20;
