import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftVisualCpp21: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftVisualCpp21" {...props} />;
};

export default MicrosoftVisualCpp21;
