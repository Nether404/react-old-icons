import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftVisualCpp22: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftVisualCpp22" {...props} />;
};

export default MicrosoftVisualCpp22;
