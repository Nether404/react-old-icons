import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftVisualCpp4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftVisualCpp4" {...props} />;
};

export default MicrosoftVisualCpp4;
