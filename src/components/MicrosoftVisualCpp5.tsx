import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftVisualCpp5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftVisualCpp5" {...props} />;
};

export default MicrosoftVisualCpp5;
