import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftVisualCpp7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftVisualCpp7" {...props} />;
};

export default MicrosoftVisualCpp7;
