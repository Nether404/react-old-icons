import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftVisualCppBooks: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftVisualCppBooks" {...props} />;
};

export default MicrosoftVisualCppBooks;
