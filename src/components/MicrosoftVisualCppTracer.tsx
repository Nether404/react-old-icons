import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftVisualCppTracer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftVisualCppTracer" {...props} />;
};

export default MicrosoftVisualCppTracer;
