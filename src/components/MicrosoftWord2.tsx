import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftWord2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftWord2" {...props} />;
};

export default MicrosoftWord2;
