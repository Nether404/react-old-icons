import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftWord2007: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftWord2007" {...props} />;
};

export default MicrosoftWord2007;
