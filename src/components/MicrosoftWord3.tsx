import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftWord3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftWord3" {...props} />;
};

export default MicrosoftWord3;
