import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MicrosoftWord5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MicrosoftWord5" {...props} />;
};

export default MicrosoftWord5;
