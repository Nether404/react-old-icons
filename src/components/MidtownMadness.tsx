import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MidtownMadness: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MidtownMadness" {...props} />;
};

export default MidtownMadness;
