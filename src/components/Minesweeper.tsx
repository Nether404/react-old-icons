import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Minesweeper: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Minesweeper" {...props} />;
};

export default Minesweeper;
