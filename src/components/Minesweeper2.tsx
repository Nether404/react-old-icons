import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Minesweeper2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Minesweeper2" {...props} />;
};

export default Minesweeper2;
