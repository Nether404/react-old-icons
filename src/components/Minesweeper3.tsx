import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Minesweeper3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Minesweeper3" {...props} />;
};

export default Minesweeper3;
