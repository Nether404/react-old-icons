import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MinesweeperLost: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MinesweeperLost" {...props} />;
};

export default MinesweeperLost;
