import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MinesweeperWon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MinesweeperWon" {...props} />;
};

export default MinesweeperWon;
