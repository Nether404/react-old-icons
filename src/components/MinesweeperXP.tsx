import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MinesweeperXP: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MinesweeperXP" {...props} />;
};

export default MinesweeperXP;
