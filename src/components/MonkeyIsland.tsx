import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MonkeyIsland: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MonkeyIsland" {...props} />;
};

export default MonkeyIsland;
