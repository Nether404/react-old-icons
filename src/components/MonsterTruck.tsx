import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MonsterTruck: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MonsterTruck" {...props} />;
};

export default MonsterTruck;
