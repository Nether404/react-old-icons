import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MonsterTruck1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MonsterTruck1" {...props} />;
};

export default MonsterTruck1;
