import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MoreNetworkNeighborhood: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MoreNetworkNeighborhood" {...props} />;
};

export default MoreNetworkNeighborhood;
