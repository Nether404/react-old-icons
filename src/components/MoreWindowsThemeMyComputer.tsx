import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MoreWindowsThemeMyComputer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MoreWindowsThemeMyComputer" {...props} />;
};

export default MoreWindowsThemeMyComputer;
