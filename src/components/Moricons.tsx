import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Moricons: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Moricons" {...props} />;
};

export default Moricons;
