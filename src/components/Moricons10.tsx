import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Moricons10: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Moricons10" {...props} />;
};

export default Moricons10;
