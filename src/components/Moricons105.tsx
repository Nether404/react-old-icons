import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Moricons105: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Moricons105" {...props} />;
};

export default Moricons105;
