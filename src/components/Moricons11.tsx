import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Moricons11: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Moricons11" {...props} />;
};

export default Moricons11;
