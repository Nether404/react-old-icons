import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Moricons12: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Moricons12" {...props} />;
};

export default Moricons12;
