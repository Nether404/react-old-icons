import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Moricons13: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Moricons13" {...props} />;
};

export default Moricons13;
