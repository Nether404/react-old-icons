import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Moricons132: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Moricons132" {...props} />;
};

export default Moricons132;
