import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Moricons14: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Moricons14" {...props} />;
};

export default Moricons14;
