import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Moricons15: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Moricons15" {...props} />;
};

export default Moricons15;
