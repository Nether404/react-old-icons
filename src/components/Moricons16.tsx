import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Moricons16: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Moricons16" {...props} />;
};

export default Moricons16;
