import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Moricons17: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Moricons17" {...props} />;
};

export default Moricons17;
