import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Moricons2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Moricons2" {...props} />;
};

export default Moricons2;
