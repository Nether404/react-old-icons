import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Moricons3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Moricons3" {...props} />;
};

export default Moricons3;
