import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Moricons32: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Moricons32" {...props} />;
};

export default Moricons32;
