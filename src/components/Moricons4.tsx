import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Moricons4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Moricons4" {...props} />;
};

export default Moricons4;
