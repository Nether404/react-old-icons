import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Moricons5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Moricons5" {...props} />;
};

export default Moricons5;
