import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Moricons6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Moricons6" {...props} />;
};

export default Moricons6;
