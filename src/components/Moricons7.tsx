import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Moricons7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Moricons7" {...props} />;
};

export default Moricons7;
