import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Moricons9: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Moricons9" {...props} />;
};

export default Moricons9;
