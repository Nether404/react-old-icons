import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MoriconsIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MoriconsIcon" {...props} />;
};

export default MoriconsIcon;
