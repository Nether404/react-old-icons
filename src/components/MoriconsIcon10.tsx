import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MoriconsIcon10: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MoriconsIcon10" {...props} />;
};

export default MoriconsIcon10;
