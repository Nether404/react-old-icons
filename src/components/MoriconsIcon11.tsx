import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MoriconsIcon11: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MoriconsIcon11" {...props} />;
};

export default MoriconsIcon11;
