import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MoriconsIcon12: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MoriconsIcon12" {...props} />;
};

export default MoriconsIcon12;
