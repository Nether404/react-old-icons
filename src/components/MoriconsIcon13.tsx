import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MoriconsIcon13: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MoriconsIcon13" {...props} />;
};

export default MoriconsIcon13;
