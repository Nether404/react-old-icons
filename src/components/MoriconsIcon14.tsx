import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MoriconsIcon14: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MoriconsIcon14" {...props} />;
};

export default MoriconsIcon14;
