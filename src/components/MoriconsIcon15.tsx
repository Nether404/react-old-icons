import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MoriconsIcon15: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MoriconsIcon15" {...props} />;
};

export default MoriconsIcon15;
