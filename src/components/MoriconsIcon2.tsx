import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MoriconsIcon2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MoriconsIcon2" {...props} />;
};

export default MoriconsIcon2;
