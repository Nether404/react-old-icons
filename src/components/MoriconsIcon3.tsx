import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MoriconsIcon3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MoriconsIcon3" {...props} />;
};

export default MoriconsIcon3;
