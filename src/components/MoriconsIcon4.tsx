import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MoriconsIcon4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MoriconsIcon4" {...props} />;
};

export default MoriconsIcon4;
