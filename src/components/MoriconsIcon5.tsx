import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MoriconsIcon5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MoriconsIcon5" {...props} />;
};

export default MoriconsIcon5;
