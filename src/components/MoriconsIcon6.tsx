import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MoriconsIcon6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MoriconsIcon6" {...props} />;
};

export default MoriconsIcon6;
