import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MoriconsIcon7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MoriconsIcon7" {...props} />;
};

export default MoriconsIcon7;
