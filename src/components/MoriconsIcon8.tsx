import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MoriconsIcon8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MoriconsIcon8" {...props} />;
};

export default MoriconsIcon8;
