import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MoriconsIcon9: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MoriconsIcon9" {...props} />;
};

export default MoriconsIcon9;
