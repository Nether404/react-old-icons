import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MorpheusIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MorpheusIcon" {...props} />;
};

export default MorpheusIcon;
