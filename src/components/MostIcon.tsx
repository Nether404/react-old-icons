import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MostIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MostIcon" {...props} />;
};

export default MostIcon;
