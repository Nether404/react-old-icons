import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MostIcon2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MostIcon2" {...props} />;
};

export default MostIcon2;
