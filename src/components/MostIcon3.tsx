import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MostIcon3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MostIcon3" {...props} />;
};

export default MostIcon3;
