import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MotocrossMadness: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MotocrossMadness" {...props} />;
};

export default MotocrossMadness;
