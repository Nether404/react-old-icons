import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MovieMaker3D: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MovieMaker3D" {...props} />;
};

export default MovieMaker3D;
