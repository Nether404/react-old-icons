import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Mozilla121: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Mozilla121" {...props} />;
};

export default Mozilla121;
