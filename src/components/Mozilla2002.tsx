import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Mozilla2002: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Mozilla2002" {...props} />;
};

export default Mozilla2002;
