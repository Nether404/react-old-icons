import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MozillaFirebird: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MozillaFirebird" {...props} />;
};

export default MozillaFirebird;
