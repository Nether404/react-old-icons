import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MozillaFirefox2009: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MozillaFirefox2009" {...props} />;
};

export default MozillaFirefox2009;
