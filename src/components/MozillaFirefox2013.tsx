import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MozillaFirefox2013: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MozillaFirefox2013" {...props} />;
};

export default MozillaFirefox2013;
