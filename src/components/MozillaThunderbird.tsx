import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MozillaThunderbird: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MozillaThunderbird" {...props} />;
};

export default MozillaThunderbird;
