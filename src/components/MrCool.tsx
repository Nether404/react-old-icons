import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MrCool: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MrCool" {...props} />;
};

export default MrCool;
