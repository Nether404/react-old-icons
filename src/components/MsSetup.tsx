import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MsSetup: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MsSetup" {...props} />;
};

export default MsSetup;
