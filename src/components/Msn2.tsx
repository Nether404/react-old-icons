import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Msn2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Msn2" {...props} />;
};

export default Msn2;
