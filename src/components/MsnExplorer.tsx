import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MsnExplorer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MsnExplorer" {...props} />;
};

export default MsnExplorer;
