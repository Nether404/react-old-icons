import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MummyMaze: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MummyMaze" {...props} />;
};

export default MummyMaze;
