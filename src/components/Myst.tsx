import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Myst: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Myst" {...props} />;
};

export default Myst;
