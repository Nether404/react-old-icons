import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MystIII: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MystIII" {...props} />;
};

export default MystIII;
