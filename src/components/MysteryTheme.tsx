import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MysteryTheme: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MysteryTheme" {...props} />;
};

export default MysteryTheme;
