import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MysteryTheme1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MysteryTheme1" {...props} />;
};

export default MysteryTheme1;
