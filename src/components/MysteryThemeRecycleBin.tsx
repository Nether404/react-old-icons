import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const MysteryThemeRecycleBin: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="MysteryThemeRecycleBin" {...props} />;
};

export default MysteryThemeRecycleBin;
