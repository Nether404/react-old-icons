import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Name5Component: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Name5Component" {...props} />;
};

export default Name5Component;
