import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Napster: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Napster" {...props} />;
};

export default Napster;
