import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NatureIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NatureIcon" {...props} />;
};

export default NatureIcon;
