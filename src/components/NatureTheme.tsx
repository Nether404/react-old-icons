import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NatureTheme: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NatureTheme" {...props} />;
};

export default NatureTheme;
