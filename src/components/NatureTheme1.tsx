import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NatureTheme1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NatureTheme1" {...props} />;
};

export default NatureTheme1;
