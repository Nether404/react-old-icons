import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NeedForSpeedMostWanted: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NeedForSpeedMostWanted" {...props} />;
};

export default NeedForSpeedMostWanted;
