import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NeedForSpeedMostWanted2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NeedForSpeedMostWanted2" {...props} />;
};

export default NeedForSpeedMostWanted2;
