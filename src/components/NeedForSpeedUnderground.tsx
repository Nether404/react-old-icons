import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NeedForSpeedUnderground: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NeedForSpeedUnderground" {...props} />;
};

export default NeedForSpeedUnderground;
