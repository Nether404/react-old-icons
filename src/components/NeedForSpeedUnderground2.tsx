import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NeedForSpeedUnderground2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NeedForSpeedUnderground2" {...props} />;
};

export default NeedForSpeedUnderground2;
