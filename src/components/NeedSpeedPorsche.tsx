import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NeedSpeedPorsche: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NeedSpeedPorsche" {...props} />;
};

export default NeedSpeedPorsche;
