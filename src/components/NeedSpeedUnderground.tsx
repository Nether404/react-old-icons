import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NeedSpeedUnderground: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NeedSpeedUnderground" {...props} />;
};

export default NeedSpeedUnderground;
