import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NekoComponent: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NekoComponent" {...props} />;
};

export default NekoComponent;
