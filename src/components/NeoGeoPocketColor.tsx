import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NeoGeoPocketColor: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NeoGeoPocketColor" {...props} />;
};

export default NeoGeoPocketColor;
