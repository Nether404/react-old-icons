import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NeroBurningROM: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NeroBurningROM" {...props} />;
};

export default NeroBurningROM;
