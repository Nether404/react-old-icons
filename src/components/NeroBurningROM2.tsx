import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NeroBurningROM2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NeroBurningROM2" {...props} />;
};

export default NeroBurningROM2;
