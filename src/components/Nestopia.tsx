import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Nestopia: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Nestopia" {...props} />;
};

export default Nestopia;
