import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetShellIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetShellIcon" {...props} />;
};

export default NetShellIcon;
