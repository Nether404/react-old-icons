import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetShowPlayer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetShowPlayer" {...props} />;
};

export default NetShowPlayer;
