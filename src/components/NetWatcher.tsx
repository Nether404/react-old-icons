import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetWatcher: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetWatcher" {...props} />;
};

export default NetWatcher;
