import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetmanageChameleon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetmanageChameleon" {...props} />;
};

export default NetmanageChameleon;
