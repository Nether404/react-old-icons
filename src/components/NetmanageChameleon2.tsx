import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetmanageChameleon2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetmanageChameleon2" {...props} />;
};

export default NetmanageChameleon2;
