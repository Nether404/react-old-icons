import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetmanageChameleon22: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetmanageChameleon22" {...props} />;
};

export default NetmanageChameleon22;
