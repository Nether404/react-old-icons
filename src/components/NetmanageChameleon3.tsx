import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetmanageChameleon3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetmanageChameleon3" {...props} />;
};

export default NetmanageChameleon3;
