import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetmanageChameleon4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetmanageChameleon4" {...props} />;
};

export default NetmanageChameleon4;
