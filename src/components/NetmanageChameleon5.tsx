import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetmanageChameleon5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetmanageChameleon5" {...props} />;
};

export default NetmanageChameleon5;
