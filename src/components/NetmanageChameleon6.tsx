import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetmanageChameleon6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetmanageChameleon6" {...props} />;
};

export default NetmanageChameleon6;
