import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetmanageChameleon7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetmanageChameleon7" {...props} />;
};

export default NetmanageChameleon7;
