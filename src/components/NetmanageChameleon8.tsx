import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetmanageChameleon8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetmanageChameleon8" {...props} />;
};

export default NetmanageChameleon8;
