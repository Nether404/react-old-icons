import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetmanageChameleon9: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetmanageChameleon9" {...props} />;
};

export default NetmanageChameleon9;
