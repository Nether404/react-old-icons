import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetmanageSoftware: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetmanageSoftware" {...props} />;
};

export default NetmanageSoftware;
