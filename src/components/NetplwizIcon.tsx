import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetplwizIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetplwizIcon" {...props} />;
};

export default NetplwizIcon;
