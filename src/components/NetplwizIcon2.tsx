import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetplwizIcon2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetplwizIcon2" {...props} />;
};

export default NetplwizIcon2;
