import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Netscape7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Netscape7" {...props} />;
};

export default Netscape7;
