import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetscapeAddress: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetscapeAddress" {...props} />;
};

export default NetscapeAddress;
