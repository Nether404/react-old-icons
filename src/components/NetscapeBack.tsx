import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetscapeBack: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetscapeBack" {...props} />;
};

export default NetscapeBack;
