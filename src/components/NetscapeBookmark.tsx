import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetscapeBookmark: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetscapeBookmark" {...props} />;
};

export default NetscapeBookmark;
