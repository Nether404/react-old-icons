import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetscapeCommunicator: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetscapeCommunicator" {...props} />;
};

export default NetscapeCommunicator;
