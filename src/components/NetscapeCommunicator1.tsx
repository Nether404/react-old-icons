import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetscapeCommunicator1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetscapeCommunicator1" {...props} />;
};

export default NetscapeCommunicator1;
