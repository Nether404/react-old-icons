import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetscapeCommunicator2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetscapeCommunicator2" {...props} />;
};

export default NetscapeCommunicator2;
