import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetscapeCommunicator3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetscapeCommunicator3" {...props} />;
};

export default NetscapeCommunicator3;
