import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetscapeCommunicator4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetscapeCommunicator4" {...props} />;
};

export default NetscapeCommunicator4;
