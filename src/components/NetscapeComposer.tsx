import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetscapeComposer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetscapeComposer" {...props} />;
};

export default NetscapeComposer;
