import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetscapeHome: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetscapeHome" {...props} />;
};

export default NetscapeHome;
