import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetscapeInbox: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetscapeInbox" {...props} />;
};

export default NetscapeInbox;
