import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetscapeNewsgroups: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetscapeNewsgroups" {...props} />;
};

export default NetscapeNewsgroups;
