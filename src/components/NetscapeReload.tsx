import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetscapeReload: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetscapeReload" {...props} />;
};

export default NetscapeReload;
