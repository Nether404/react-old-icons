import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetscapeSearch: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetscapeSearch" {...props} />;
};

export default NetscapeSearch;
