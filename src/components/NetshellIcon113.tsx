import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetshellIcon113: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetshellIcon113" {...props} />;
};

export default NetshellIcon113;
