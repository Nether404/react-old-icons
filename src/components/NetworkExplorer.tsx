import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetworkExplorer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetworkExplorer" {...props} />;
};

export default NetworkExplorer;
