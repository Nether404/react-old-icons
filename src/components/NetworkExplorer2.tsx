import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NetworkExplorer2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NetworkExplorer2" {...props} />;
};

export default NetworkExplorer2;
