import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NewsBinPro: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NewsBinPro" {...props} />;
};

export default NewsBinPro;
