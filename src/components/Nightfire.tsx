import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Nightfire: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Nightfire" {...props} />;
};

export default Nightfire;
