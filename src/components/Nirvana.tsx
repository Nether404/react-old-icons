import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Nirvana: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Nirvana" {...props} />;
};

export default Nirvana;
