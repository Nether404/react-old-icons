import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Nitemare3D: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Nitemare3D" {...props} />;
};

export default Nitemare3D;
