import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NoOneLivesForever: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NoOneLivesForever" {...props} />;
};

export default NoOneLivesForever;
