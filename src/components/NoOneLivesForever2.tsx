import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NoOneLivesForever2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NoOneLivesForever2" {...props} />;
};

export default NoOneLivesForever2;
