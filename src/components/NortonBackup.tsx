import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonBackup: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonBackup" {...props} />;
};

export default NortonBackup;
