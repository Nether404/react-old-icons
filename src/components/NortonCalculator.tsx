import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonCalculator: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonCalculator" {...props} />;
};

export default NortonCalculator;
