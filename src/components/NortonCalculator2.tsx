import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonCalculator2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonCalculator2" {...props} />;
};

export default NortonCalculator2;
