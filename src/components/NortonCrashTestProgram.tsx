import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonCrashTestProgram: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonCrashTestProgram" {...props} />;
};

export default NortonCrashTestProgram;
