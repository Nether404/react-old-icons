import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonDayPlanner: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonDayPlanner" {...props} />;
};

export default NortonDayPlanner;
