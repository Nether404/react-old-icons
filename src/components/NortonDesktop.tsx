import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonDesktop: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonDesktop" {...props} />;
};

export default NortonDesktop;
