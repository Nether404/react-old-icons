import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonDesktop2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonDesktop2" {...props} />;
};

export default NortonDesktop2;
