import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonDesktop22: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonDesktop22" {...props} />;
};

export default NortonDesktop22;
