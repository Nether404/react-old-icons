import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonDesktop3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonDesktop3" {...props} />;
};

export default NortonDesktop3;
