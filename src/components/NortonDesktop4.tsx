import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonDesktop4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonDesktop4" {...props} />;
};

export default NortonDesktop4;
