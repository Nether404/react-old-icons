import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonDesktopBatchrun: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonDesktopBatchrun" {...props} />;
};

export default NortonDesktopBatchrun;
