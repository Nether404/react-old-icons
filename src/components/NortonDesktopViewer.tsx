import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonDesktopViewer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonDesktopViewer" {...props} />;
};

export default NortonDesktopViewer;
