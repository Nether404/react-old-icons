import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonDiskCopy: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonDiskCopy" {...props} />;
};

export default NortonDiskCopy;
