import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonFileCompare: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonFileCompare" {...props} />;
};

export default NortonFileCompare;
