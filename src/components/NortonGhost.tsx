import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonGhost: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonGhost" {...props} />;
};

export default NortonGhost;
