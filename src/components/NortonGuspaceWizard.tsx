import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonGuspaceWizard: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonGuspaceWizard" {...props} />;
};

export default NortonGuspaceWizard;
