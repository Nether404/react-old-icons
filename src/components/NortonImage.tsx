import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonImage: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonImage" {...props} />;
};

export default NortonImage;
