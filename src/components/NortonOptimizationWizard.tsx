import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonOptimizationWizard: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonOptimizationWizard" {...props} />;
};

export default NortonOptimizationWizard;
