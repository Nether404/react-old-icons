import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonScheduler: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonScheduler" {...props} />;
};

export default NortonScheduler;
