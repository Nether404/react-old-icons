import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonScreenSaver: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonScreenSaver" {...props} />;
};

export default NortonScreenSaver;
