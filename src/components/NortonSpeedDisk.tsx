import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonSpeedDisk: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonSpeedDisk" {...props} />;
};

export default NortonSpeedDisk;
