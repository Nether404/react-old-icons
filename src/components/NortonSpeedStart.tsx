import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonSpeedStart: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonSpeedStart" {...props} />;
};

export default NortonSpeedStart;
