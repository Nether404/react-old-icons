import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonSuperfind: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonSuperfind" {...props} />;
};

export default NortonSuperfind;
