import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonViewer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonViewer" {...props} />;
};

export default NortonViewer;
