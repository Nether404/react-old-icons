import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NortonWinDoctor: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NortonWinDoctor" {...props} />;
};

export default NortonWinDoctor;
