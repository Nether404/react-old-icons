import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NullsoftInstall: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NullsoftInstall" {...props} />;
};

export default NullsoftInstall;
