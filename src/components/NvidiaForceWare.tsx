import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NvidiaForceWare: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NvidiaForceWare" {...props} />;
};

export default NvidiaForceWare;
