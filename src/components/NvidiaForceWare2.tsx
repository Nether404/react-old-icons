import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const NvidiaForceWare2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="NvidiaForceWare2" {...props} />;
};

export default NvidiaForceWare2;
