import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Oddballz: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Oddballz" {...props} />;
};

export default Oddballz;
