import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OfficeAccess: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OfficeAccess" {...props} />;
};

export default OfficeAccess;
