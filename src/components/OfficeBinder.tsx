import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OfficeBinder: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OfficeBinder" {...props} />;
};

export default OfficeBinder;
