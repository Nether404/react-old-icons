import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OfficeBinder2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OfficeBinder2" {...props} />;
};

export default OfficeBinder2;
