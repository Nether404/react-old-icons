import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OfficeClip1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OfficeClip1" {...props} />;
};

export default OfficeClip1;
