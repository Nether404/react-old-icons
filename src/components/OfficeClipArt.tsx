import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OfficeClipArt: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OfficeClipArt" {...props} />;
};

export default OfficeClipArt;
