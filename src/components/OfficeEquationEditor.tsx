import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OfficeEquationEditor: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OfficeEquationEditor" {...props} />;
};

export default OfficeEquationEditor;
