import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OfficeGraph: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OfficeGraph" {...props} />;
};

export default OfficeGraph;
