import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OfficeGroove: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OfficeGroove" {...props} />;
};

export default OfficeGroove;
