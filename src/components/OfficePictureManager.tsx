import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OfficePictureManager: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OfficePictureManager" {...props} />;
};

export default OfficePictureManager;
