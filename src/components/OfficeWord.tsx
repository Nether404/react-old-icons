import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OfficeWord: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OfficeWord" {...props} />;
};

export default OfficeWord;
