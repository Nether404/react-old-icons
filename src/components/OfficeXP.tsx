import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OfficeXP: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OfficeXP" {...props} />;
};

export default OfficeXP;
