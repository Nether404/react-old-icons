import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OfficeXP12: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OfficeXP12" {...props} />;
};

export default OfficeXP12;
