import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OneNote: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OneNote" {...props} />;
};

export default OneNote;
