import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OpenOffice: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OpenOffice" {...props} />;
};

export default OpenOffice;
