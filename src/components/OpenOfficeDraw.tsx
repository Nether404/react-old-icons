import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OpenOfficeDraw: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OpenOfficeDraw" {...props} />;
};

export default OpenOfficeDraw;
