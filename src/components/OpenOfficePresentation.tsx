import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OpenOfficePresentation: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OpenOfficePresentation" {...props} />;
};

export default OpenOfficePresentation;
