import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OpenOfficeSpreadsheet: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OpenOfficeSpreadsheet" {...props} />;
};

export default OpenOfficeSpreadsheet;
