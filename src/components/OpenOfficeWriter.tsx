import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OpenOfficeWriter: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OpenOfficeWriter" {...props} />;
};

export default OpenOfficeWriter;
