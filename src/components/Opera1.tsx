import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Opera1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Opera1" {...props} />;
};

export default Opera1;
