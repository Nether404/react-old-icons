import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Opera20092: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Opera20092" {...props} />;
};

export default Opera20092;
