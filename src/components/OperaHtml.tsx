import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OperaHtml: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OperaHtml" {...props} />;
};

export default OperaHtml;
