import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OregonTrail: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OregonTrail" {...props} />;
};

export default OregonTrail;
