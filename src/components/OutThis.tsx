import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OutThis: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OutThis" {...props} />;
};

export default OutThis;
