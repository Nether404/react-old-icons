import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Outlook: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Outlook" {...props} />;
};

export default Outlook;
