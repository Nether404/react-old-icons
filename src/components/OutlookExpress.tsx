import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OutlookExpress: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OutlookExpress" {...props} />;
};

export default OutlookExpress;
