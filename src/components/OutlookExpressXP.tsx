import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const OutlookExpressXP: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="OutlookExpressXP" {...props} />;
};

export default OutlookExpressXP;
