import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PCGamerCD: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PCGamerCD" {...props} />;
};

export default PCGamerCD;
