import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PSone: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PSone" {...props} />;
};

export default PSone;
