import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PaintAirbrush: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PaintAirbrush" {...props} />;
};

export default PaintAirbrush;
