import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PaintBrush: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PaintBrush" {...props} />;
};

export default PaintBrush;
