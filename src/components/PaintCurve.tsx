import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PaintCurve: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PaintCurve" {...props} />;
};

export default PaintCurve;
