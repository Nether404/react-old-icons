import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PaintErasercolor: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PaintErasercolor" {...props} />;
};

export default PaintErasercolor;
