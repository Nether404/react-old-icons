import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PaintFill: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PaintFill" {...props} />;
};

export default PaintFill;
