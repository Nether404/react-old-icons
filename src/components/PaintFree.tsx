import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PaintFree: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PaintFree" {...props} />;
};

export default PaintFree;
