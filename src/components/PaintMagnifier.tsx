import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PaintMagnifier: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PaintMagnifier" {...props} />;
};

export default PaintMagnifier;
