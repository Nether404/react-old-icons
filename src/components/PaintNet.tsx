import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PaintNet: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PaintNet" {...props} />;
};

export default PaintNet;
