import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PaintPencil: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PaintPencil" {...props} />;
};

export default PaintPencil;
