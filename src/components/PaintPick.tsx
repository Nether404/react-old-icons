import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PaintPick: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PaintPick" {...props} />;
};

export default PaintPick;
