import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PaintPolygon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PaintPolygon" {...props} />;
};

export default PaintPolygon;
