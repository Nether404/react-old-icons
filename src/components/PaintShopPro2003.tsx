import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PaintShopPro2003: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PaintShopPro2003" {...props} />;
};

export default PaintShopPro2003;
