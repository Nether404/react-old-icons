import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PaintShopPro20032: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PaintShopPro20032" {...props} />;
};

export default PaintShopPro20032;
