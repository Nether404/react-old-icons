import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PaintText: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PaintText" {...props} />;
};

export default PaintText;
