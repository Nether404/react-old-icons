import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ParadoxNupdate: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ParadoxNupdate" {...props} />;
};

export default ParadoxNupdate;
