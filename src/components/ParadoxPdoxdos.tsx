import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ParadoxPdoxdos: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ParadoxPdoxdos" {...props} />;
};

export default ParadoxPdoxdos;
