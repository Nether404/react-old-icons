import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ParadoxTutility: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ParadoxTutility" {...props} />;
};

export default ParadoxTutility;
