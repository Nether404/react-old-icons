import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Pariah: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Pariah" {...props} />;
};

export default Pariah;
