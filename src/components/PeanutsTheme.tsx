import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PeanutsTheme: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PeanutsTheme" {...props} />;
};

export default PeanutsTheme;
