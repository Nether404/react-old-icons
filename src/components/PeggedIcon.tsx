import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PeggedIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PeggedIcon" {...props} />;
};

export default PeggedIcon;
