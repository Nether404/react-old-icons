import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PeggleDeluxe: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PeggleDeluxe" {...props} />;
};

export default PeggleDeluxe;
