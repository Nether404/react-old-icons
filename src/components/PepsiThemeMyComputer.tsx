import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PepsiThemeMyComputer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PepsiThemeMyComputer" {...props} />;
};

export default PepsiThemeMyComputer;
