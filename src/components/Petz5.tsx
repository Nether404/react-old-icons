import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Petz5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Petz5" {...props} />;
};

export default Petz5;
