import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PetzII: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PetzII" {...props} />;
};

export default PetzII;
