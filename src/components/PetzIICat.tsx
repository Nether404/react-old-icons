import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PetzIICat: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PetzIICat" {...props} />;
};

export default PetzIICat;
