import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PetzIIDog: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PetzIIDog" {...props} />;
};

export default PetzIIDog;
