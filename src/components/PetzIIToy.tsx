import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PetzIIToy: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PetzIIToy" {...props} />;
};

export default PetzIIToy;
