import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PhotoDiscNetworkNeighborhood: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PhotoDiscNetworkNeighborhood" {...props} />;
};

export default PhotoDiscNetworkNeighborhood;
