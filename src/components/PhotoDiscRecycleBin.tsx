import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PhotoDiscRecycleBin: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PhotoDiscRecycleBin" {...props} />;
};

export default PhotoDiscRecycleBin;
