import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PhotoEditor2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PhotoEditor2" {...props} />;
};

export default PhotoEditor2;
