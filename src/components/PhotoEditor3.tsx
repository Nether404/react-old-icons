import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PhotoEditor3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PhotoEditor3" {...props} />;
};

export default PhotoEditor3;
