import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PhotodiscTheme: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PhotodiscTheme" {...props} />;
};

export default PhotodiscTheme;
