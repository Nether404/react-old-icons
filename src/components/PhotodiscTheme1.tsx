import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PhotodiscTheme1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PhotodiscTheme1" {...props} />;
};

export default PhotodiscTheme1;
