import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Photoshop6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Photoshop6" {...props} />;
};

export default Photoshop6;
