import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Photoshop7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Photoshop7" {...props} />;
};

export default Photoshop7;
