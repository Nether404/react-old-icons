import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PhotoshopCS: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PhotoshopCS" {...props} />;
};

export default PhotoshopCS;
