import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PhotoshopCS3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PhotoshopCS3" {...props} />;
};

export default PhotoshopCS3;
