import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PhotoshopCS3JPEG: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PhotoshopCS3JPEG" {...props} />;
};

export default PhotoshopCS3JPEG;
