import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PhotoshopCS3Plugin: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PhotoshopCS3Plugin" {...props} />;
};

export default PhotoshopCS3Plugin;
