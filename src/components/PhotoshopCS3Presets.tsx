import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PhotoshopCS3Presets: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PhotoshopCS3Presets" {...props} />;
};

export default PhotoshopCS3Presets;
