import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PhotoshopCS3Shapes: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PhotoshopCS3Shapes" {...props} />;
};

export default PhotoshopCS3Shapes;
