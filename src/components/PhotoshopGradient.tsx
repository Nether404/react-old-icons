import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PhotoshopGradient: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PhotoshopGradient" {...props} />;
};

export default PhotoshopGradient;
