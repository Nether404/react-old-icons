import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PhotoshopGradients: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PhotoshopGradients" {...props} />;
};

export default PhotoshopGradients;
