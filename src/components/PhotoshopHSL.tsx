import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PhotoshopHSL: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PhotoshopHSL" {...props} />;
};

export default PhotoshopHSL;
