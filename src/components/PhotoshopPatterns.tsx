import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PhotoshopPatterns: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PhotoshopPatterns" {...props} />;
};

export default PhotoshopPatterns;
