import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Picasa: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Picasa" {...props} />;
};

export default Picasa;
