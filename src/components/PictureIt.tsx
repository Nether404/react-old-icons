import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PictureIt: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PictureIt" {...props} />;
};

export default PictureIt;
