import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PictureworksHotshots: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PictureworksHotshots" {...props} />;
};

export default PictureworksHotshots;
