import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PidginComponent: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PidginComponent" {...props} />;
};

export default PidginComponent;
