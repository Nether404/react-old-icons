import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PifmgrIcon2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PifmgrIcon2" {...props} />;
};

export default PifmgrIcon2;
