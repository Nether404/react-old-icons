import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PifmgrIcon3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PifmgrIcon3" {...props} />;
};

export default PifmgrIcon3;
