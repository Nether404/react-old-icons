import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PifmgrIcon4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PifmgrIcon4" {...props} />;
};

export default PifmgrIcon4;
