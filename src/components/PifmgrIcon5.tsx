import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PifmgrIcon5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PifmgrIcon5" {...props} />;
};

export default PifmgrIcon5;
