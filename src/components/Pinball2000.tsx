import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Pinball2000: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Pinball2000" {...props} />;
};

export default Pinball2000;
