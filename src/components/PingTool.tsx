import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PingTool: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PingTool" {...props} />;
};

export default PingTool;
