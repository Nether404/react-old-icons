import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PipeDream: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PipeDream" {...props} />;
};

export default PipeDream;
