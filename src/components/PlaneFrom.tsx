import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PlaneFrom: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PlaneFrom" {...props} />;
};

export default PlaneFrom;
