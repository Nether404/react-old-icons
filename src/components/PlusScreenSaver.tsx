import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PlusScreenSaver: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PlusScreenSaver" {...props} />;
};

export default PlusScreenSaver;
