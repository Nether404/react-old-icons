import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PokerNight: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PokerNight" {...props} />;
};

export default PokerNight;
