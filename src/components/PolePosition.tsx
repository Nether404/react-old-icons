import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PolePosition: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PolePosition" {...props} />;
};

export default PolePosition;
