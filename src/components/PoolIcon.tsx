import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PoolIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PoolIcon" {...props} />;
};

export default PoolIcon;
