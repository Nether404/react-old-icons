import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PortableMediaPlayer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PortableMediaPlayer" {...props} />;
};

export default PortableMediaPlayer;
