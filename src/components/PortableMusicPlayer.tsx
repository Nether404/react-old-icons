import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PortableMusicPlayer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PortableMusicPlayer" {...props} />;
};

export default PortableMusicPlayer;
