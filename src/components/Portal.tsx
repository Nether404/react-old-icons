import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Portal: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Portal" {...props} />;
};

export default Portal;
