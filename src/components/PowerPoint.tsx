import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PowerPoint: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PowerPoint" {...props} />;
};

export default PowerPoint;
