import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PowerPoint10: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PowerPoint10" {...props} />;
};

export default PowerPoint10;
