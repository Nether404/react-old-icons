import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PowerPoint11: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PowerPoint11" {...props} />;
};

export default PowerPoint11;
