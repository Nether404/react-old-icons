import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PowerPoint2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PowerPoint2" {...props} />;
};

export default PowerPoint2;
