import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PowerPoint3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PowerPoint3" {...props} />;
};

export default PowerPoint3;
