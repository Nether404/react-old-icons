import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PowerPoint4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PowerPoint4" {...props} />;
};

export default PowerPoint4;
