import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PowerPoint7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PowerPoint7" {...props} />;
};

export default PowerPoint7;
