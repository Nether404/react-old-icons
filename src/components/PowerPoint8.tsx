import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PowerPoint8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PowerPoint8" {...props} />;
};

export default PowerPoint8;
