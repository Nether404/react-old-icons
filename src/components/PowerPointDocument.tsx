import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PowerPointDocument: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PowerPointDocument" {...props} />;
};

export default PowerPointDocument;
