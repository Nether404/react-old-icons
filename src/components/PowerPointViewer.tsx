import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PowerPointViewer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PowerPointViewer" {...props} />;
};

export default PowerPointViewer;
