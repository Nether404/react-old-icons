import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PowerPointXP: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PowerPointXP" {...props} />;
};

export default PowerPointXP;
