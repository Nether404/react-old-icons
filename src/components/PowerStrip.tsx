import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PowerStrip: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PowerStrip" {...props} />;
};

export default PowerStrip;
