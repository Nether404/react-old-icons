import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Powerdvd: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Powerdvd" {...props} />;
};

export default Powerdvd;
