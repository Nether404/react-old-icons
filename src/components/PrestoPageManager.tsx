import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PrestoPageManager: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PrestoPageManager" {...props} />;
};

export default PrestoPageManager;
