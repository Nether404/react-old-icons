import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Prime95: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Prime95" {...props} />;
};

export default Prime95;
