import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PrincePersia: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PrincePersia" {...props} />;
};

export default PrincePersia;
