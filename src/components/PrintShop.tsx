import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PrintShop: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PrintShop" {...props} />;
};

export default PrintShop;
