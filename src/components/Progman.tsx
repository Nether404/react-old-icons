import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Progman: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Progman" {...props} />;
};

export default Progman;
