import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Progman2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Progman2" {...props} />;
};

export default Progman2;
