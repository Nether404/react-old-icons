import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Progman3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Progman3" {...props} />;
};

export default Progman3;
