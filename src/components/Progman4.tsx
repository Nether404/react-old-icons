import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Progman4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Progman4" {...props} />;
};

export default Progman4;
