import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ProgmanIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ProgmanIcon" {...props} />;
};

export default ProgmanIcon;
