import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ProgmanIcon10: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ProgmanIcon10" {...props} />;
};

export default ProgmanIcon10;
