import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ProgmanIcon11: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ProgmanIcon11" {...props} />;
};

export default ProgmanIcon11;
