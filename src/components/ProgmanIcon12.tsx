import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ProgmanIcon12: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ProgmanIcon12" {...props} />;
};

export default ProgmanIcon12;
