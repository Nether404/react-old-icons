import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ProgmanIcon13: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ProgmanIcon13" {...props} />;
};

export default ProgmanIcon13;
