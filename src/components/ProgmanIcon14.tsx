import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ProgmanIcon14: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ProgmanIcon14" {...props} />;
};

export default ProgmanIcon14;
