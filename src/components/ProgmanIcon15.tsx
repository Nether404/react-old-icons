import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ProgmanIcon15: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ProgmanIcon15" {...props} />;
};

export default ProgmanIcon15;
