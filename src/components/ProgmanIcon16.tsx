import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ProgmanIcon16: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ProgmanIcon16" {...props} />;
};

export default ProgmanIcon16;
