import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ProgmanIcon2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ProgmanIcon2" {...props} />;
};

export default ProgmanIcon2;
