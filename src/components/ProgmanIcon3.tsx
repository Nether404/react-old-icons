import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ProgmanIcon3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ProgmanIcon3" {...props} />;
};

export default ProgmanIcon3;
