import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ProgmanIcon4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ProgmanIcon4" {...props} />;
};

export default ProgmanIcon4;
