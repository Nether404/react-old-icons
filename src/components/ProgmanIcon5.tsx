import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ProgmanIcon5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ProgmanIcon5" {...props} />;
};

export default ProgmanIcon5;
