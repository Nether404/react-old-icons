import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ProgmanIcon6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ProgmanIcon6" {...props} />;
};

export default ProgmanIcon6;
