import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ProgmanIcon7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ProgmanIcon7" {...props} />;
};

export default ProgmanIcon7;
