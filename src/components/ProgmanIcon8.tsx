import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ProgmanIcon8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ProgmanIcon8" {...props} />;
};

export default ProgmanIcon8;
