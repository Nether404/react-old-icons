import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Project64: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Project64" {...props} />;
};

export default Project64;
