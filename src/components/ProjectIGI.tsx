import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ProjectIGI: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ProjectIGI" {...props} />;
};

export default ProjectIGI;
