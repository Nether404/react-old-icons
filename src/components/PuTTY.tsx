import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PuTTY: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PuTTY" {...props} />;
};

export default PuTTY;
