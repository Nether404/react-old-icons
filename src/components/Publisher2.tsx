import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Publisher2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Publisher2" {...props} />;
};

export default Publisher2;
