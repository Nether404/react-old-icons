import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Publisher3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Publisher3" {...props} />;
};

export default Publisher3;
