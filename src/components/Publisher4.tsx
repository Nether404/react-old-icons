import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Publisher4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Publisher4" {...props} />;
};

export default Publisher4;
