import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PublisherHLinkTool: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PublisherHLinkTool" {...props} />;
};

export default PublisherHLinkTool;
