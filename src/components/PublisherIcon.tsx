import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PublisherIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PublisherIcon" {...props} />;
};

export default PublisherIcon;
