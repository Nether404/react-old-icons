import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PublisherOffice2003: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PublisherOffice2003" {...props} />;
};

export default PublisherOffice2003;
