import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PurblePlace: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PurblePlace" {...props} />;
};

export default PurblePlace;
