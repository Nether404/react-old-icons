import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const PuzzleCollection: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="PuzzleCollection" {...props} />;
};

export default PuzzleCollection;
