import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const QbasicComponent: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="QbasicComponent" {...props} />;
};

export default QbasicComponent;
