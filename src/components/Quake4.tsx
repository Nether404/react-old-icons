import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Quake4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Quake4" {...props} />;
};

export default Quake4;
