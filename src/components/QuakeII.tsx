import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const QuakeII: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="QuakeII" {...props} />;
};

export default QuakeII;
