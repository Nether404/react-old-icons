import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const QuakeIICtf: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="QuakeIICtf" {...props} />;
};

export default QuakeIICtf;
