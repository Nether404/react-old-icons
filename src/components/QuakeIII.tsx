import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const QuakeIII: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="QuakeIII" {...props} />;
};

export default QuakeIII;
