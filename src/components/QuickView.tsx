import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const QuickView: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="QuickView" {...props} />;
};

export default QuickView;
