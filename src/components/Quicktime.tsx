import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Quicktime: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Quicktime" {...props} />;
};

export default Quicktime;
