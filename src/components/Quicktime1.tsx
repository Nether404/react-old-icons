import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Quicktime1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Quicktime1" {...props} />;
};

export default Quicktime1;
