import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const QuicktimeControlPanel: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="QuicktimeControlPanel" {...props} />;
};

export default QuicktimeControlPanel;
