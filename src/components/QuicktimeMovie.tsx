import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const QuicktimeMovie: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="QuicktimeMovie" {...props} />;
};

export default QuicktimeMovie;
