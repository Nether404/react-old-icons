import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const QuicktimeMovie1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="QuicktimeMovie1" {...props} />;
};

export default QuicktimeMovie1;
