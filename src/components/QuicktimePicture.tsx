import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const QuicktimePicture: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="QuicktimePicture" {...props} />;
};

export default QuicktimePicture;
