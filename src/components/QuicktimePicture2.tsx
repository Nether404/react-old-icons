import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const QuicktimePicture2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="QuicktimePicture2" {...props} />;
};

export default QuicktimePicture2;
