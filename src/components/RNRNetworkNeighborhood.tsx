import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RNRNetworkNeighborhood: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RNRNetworkNeighborhood" {...props} />;
};

export default RNRNetworkNeighborhood;
