import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RPGMaker2000RT: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RPGMaker2000RT" {...props} />;
};

export default RPGMaker2000RT;
