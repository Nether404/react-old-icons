import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RatPoker: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RatPoker" {...props} />;
};

export default RatPoker;
