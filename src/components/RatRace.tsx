import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RatRace: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RatRace" {...props} />;
};

export default RatRace;
