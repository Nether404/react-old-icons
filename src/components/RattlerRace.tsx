import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RattlerRace: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RattlerRace" {...props} />;
};

export default RattlerRace;
