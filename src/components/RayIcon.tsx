import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RayIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RayIcon" {...props} />;
};

export default RayIcon;
