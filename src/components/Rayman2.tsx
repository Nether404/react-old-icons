import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Rayman2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Rayman2" {...props} />;
};

export default Rayman2;
