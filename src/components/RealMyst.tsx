import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RealMyst: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RealMyst" {...props} />;
};

export default RealMyst;
