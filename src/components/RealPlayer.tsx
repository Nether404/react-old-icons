import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RealPlayer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RealPlayer" {...props} />;
};

export default RealPlayer;
