import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RealPlayer4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RealPlayer4" {...props} />;
};

export default RealPlayer4;
