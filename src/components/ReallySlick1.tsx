import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ReallySlick1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ReallySlick1" {...props} />;
};

export default ReallySlick1;
