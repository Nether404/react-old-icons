import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ReallySlick2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ReallySlick2" {...props} />;
};

export default ReallySlick2;
