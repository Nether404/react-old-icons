import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ReallySlick3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ReallySlick3" {...props} />;
};

export default ReallySlick3;
