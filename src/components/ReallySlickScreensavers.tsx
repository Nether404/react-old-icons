import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ReallySlickScreensavers: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ReallySlickScreensavers" {...props} />;
};

export default ReallySlickScreensavers;
