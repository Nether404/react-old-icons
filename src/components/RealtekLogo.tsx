import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RealtekLogo: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RealtekLogo" {...props} />;
};

export default RealtekLogo;
