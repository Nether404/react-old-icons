import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RealtekSound: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RealtekSound" {...props} />;
};

export default RealtekSound;
