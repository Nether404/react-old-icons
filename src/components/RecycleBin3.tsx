import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RecycleBin3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RecycleBin3" {...props} />;
};

export default RecycleBin3;
