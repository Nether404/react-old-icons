import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RecycleBin4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RecycleBin4" {...props} />;
};

export default RecycleBin4;
