import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RedFaction: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RedFaction" {...props} />;
};

export default RedFaction;
