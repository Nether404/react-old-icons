import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RegionalAndLanguageOptions: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RegionalAndLanguageOptions" {...props} />;
};

export default RegionalAndLanguageOptions;
