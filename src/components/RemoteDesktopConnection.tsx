import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RemoteDesktopConnection: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RemoteDesktopConnection" {...props} />;
};

export default RemoteDesktopConnection;
