import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ResidentEvilII: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ResidentEvilII" {...props} />;
};

export default ResidentEvilII;
