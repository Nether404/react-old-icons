import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ResidentEvilIII: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ResidentEvilIII" {...props} />;
};

export default ResidentEvilIII;
