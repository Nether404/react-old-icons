import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ReturnIncredibleMachine: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ReturnIncredibleMachine" {...props} />;
};

export default ReturnIncredibleMachine;
