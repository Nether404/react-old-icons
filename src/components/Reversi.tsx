import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Reversi: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Reversi" {...props} />;
};

export default Reversi;
