import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RivenSequel: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RivenSequel" {...props} />;
};

export default RivenSequel;
