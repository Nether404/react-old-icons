import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Robottack: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Robottack" {...props} />;
};

export default Robottack;
