import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RockRoll: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RockRoll" {...props} />;
};

export default RockRoll;
