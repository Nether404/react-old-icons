import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RodentRevenge: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RodentRevenge" {...props} />;
};

export default RodentRevenge;
