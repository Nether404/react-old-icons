import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Rollcage: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Rollcage" {...props} />;
};

export default Rollcage;
