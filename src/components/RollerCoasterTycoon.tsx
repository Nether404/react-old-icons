import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RollerCoasterTycoon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RollerCoasterTycoon" {...props} />;
};

export default RollerCoasterTycoon;
