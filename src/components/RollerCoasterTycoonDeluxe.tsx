import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RollerCoasterTycoonDeluxe: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RollerCoasterTycoonDeluxe" {...props} />;
};

export default RollerCoasterTycoonDeluxe;
