import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RollerCoasterTycoonG: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RollerCoasterTycoonG" {...props} />;
};

export default RollerCoasterTycoonG;
