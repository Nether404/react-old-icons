import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RollerCoasterTycoonSaved: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RollerCoasterTycoonSaved" {...props} />;
};

export default RollerCoasterTycoonSaved;
