import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RollercoasterTycoon11: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RollercoasterTycoon11" {...props} />;
};

export default RollercoasterTycoon11;
