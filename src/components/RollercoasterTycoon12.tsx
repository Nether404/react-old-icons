import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RollercoasterTycoon12: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RollercoasterTycoon12" {...props} />;
};

export default RollercoasterTycoon12;
