import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RollercoasterTycoon13: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RollercoasterTycoon13" {...props} />;
};

export default RollercoasterTycoon13;
