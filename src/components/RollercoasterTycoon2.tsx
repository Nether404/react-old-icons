import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RollercoasterTycoon2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RollercoasterTycoon2" {...props} />;
};

export default RollercoasterTycoon2;
