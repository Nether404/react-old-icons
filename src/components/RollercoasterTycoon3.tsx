import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RollercoasterTycoon3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RollercoasterTycoon3" {...props} />;
};

export default RollercoasterTycoon3;
