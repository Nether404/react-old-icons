import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RollercoasterTycoon4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RollercoasterTycoon4" {...props} />;
};

export default RollercoasterTycoon4;
