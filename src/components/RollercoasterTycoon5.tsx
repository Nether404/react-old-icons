import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RollercoasterTycoon5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RollercoasterTycoon5" {...props} />;
};

export default RollercoasterTycoon5;
