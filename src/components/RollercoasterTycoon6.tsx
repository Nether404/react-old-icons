import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RollercoasterTycoon6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RollercoasterTycoon6" {...props} />;
};

export default RollercoasterTycoon6;
