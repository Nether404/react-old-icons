import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RollercoasterTycoon7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RollercoasterTycoon7" {...props} />;
};

export default RollercoasterTycoon7;
