import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RollercoasterTycoon8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RollercoasterTycoon8" {...props} />;
};

export default RollercoasterTycoon8;
