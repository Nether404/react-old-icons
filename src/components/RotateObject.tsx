import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RotateObject: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RotateObject" {...props} />;
};

export default RotateObject;
