import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RpgMaker2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RpgMaker2" {...props} />;
};

export default RpgMaker2;
