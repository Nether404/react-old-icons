import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const RpgMakerXP: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="RpgMakerXP" {...props} />;
};

export default RpgMakerXP;
