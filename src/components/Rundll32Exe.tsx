import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Rundll32Exe: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Rundll32Exe" {...props} />;
};

export default Rundll32Exe;
