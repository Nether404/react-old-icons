import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Safari2010: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Safari2010" {...props} />;
};

export default Safari2010;
