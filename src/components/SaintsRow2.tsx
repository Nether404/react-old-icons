import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SaintsRow2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SaintsRow2" {...props} />;
};

export default SaintsRow2;
