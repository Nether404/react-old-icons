import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Sampler: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Sampler" {...props} />;
};

export default Sampler;
