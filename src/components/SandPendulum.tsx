import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SandPendulum: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SandPendulum" {...props} />;
};

export default SandPendulum;
