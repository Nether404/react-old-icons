import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ScannersAndCameras: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ScannersAndCameras" {...props} />;
};

export default ScannersAndCameras;
