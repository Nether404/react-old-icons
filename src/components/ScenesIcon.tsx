import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ScenesIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ScenesIcon" {...props} />;
};

export default ScenesIcon;
