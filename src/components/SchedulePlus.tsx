import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SchedulePlus: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SchedulePlus" {...props} />;
};

export default SchedulePlus;
