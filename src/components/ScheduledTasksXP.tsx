import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ScheduledTasksXP: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ScheduledTasksXP" {...props} />;
};

export default ScheduledTasksXP;
