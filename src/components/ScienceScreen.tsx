import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ScienceScreen: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ScienceScreen" {...props} />;
};

export default ScienceScreen;
