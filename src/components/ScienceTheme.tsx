import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ScienceTheme: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ScienceTheme" {...props} />;
};

export default ScienceTheme;
