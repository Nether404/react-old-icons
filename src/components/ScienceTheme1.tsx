import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ScienceTheme1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ScienceTheme1" {...props} />;
};

export default ScienceTheme1;
