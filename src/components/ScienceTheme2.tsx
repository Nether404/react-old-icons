import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ScienceTheme2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ScienceTheme2" {...props} />;
};

export default ScienceTheme2;
