import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ScienceTheme3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ScienceTheme3" {...props} />;
};

export default ScienceTheme3;
