import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ScitechDisplay: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ScitechDisplay" {...props} />;
};

export default ScitechDisplay;
