import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ScreenMate: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ScreenMate" {...props} />;
};

export default ScreenMate;
