import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SecurityEssentials: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SecurityEssentials" {...props} />;
};

export default SecurityEssentials;
