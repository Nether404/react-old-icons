import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SegaBass: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SegaBass" {...props} />;
};

export default SegaBass;
