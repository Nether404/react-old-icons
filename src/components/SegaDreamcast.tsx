import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SegaDreamcast: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SegaDreamcast" {...props} />;
};

export default SegaDreamcast;
