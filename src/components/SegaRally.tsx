import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SegaRally: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SegaRally" {...props} />;
};

export default SegaRally;
