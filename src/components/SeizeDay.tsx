import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SeizeDay: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SeizeDay" {...props} />;
};

export default SeizeDay;
