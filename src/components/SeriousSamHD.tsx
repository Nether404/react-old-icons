import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SeriousSamHD: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SeriousSamHD" {...props} />;
};

export default SeriousSamHD;
