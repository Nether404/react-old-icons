import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SeriousSamHD2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SeriousSamHD2" {...props} />;
};

export default SeriousSamHD2;
