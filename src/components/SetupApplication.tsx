import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SetupApplication: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SetupApplication" {...props} />;
};

export default SetupApplication;
