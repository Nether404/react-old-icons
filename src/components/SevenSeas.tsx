import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SevenSeas: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SevenSeas" {...props} />;
};

export default SevenSeas;
