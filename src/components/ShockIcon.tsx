import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ShockIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ShockIcon" {...props} />;
};

export default ShockIcon;
