import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ShogoMobileArmor: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ShogoMobileArmor" {...props} />;
};

export default ShogoMobileArmor;
