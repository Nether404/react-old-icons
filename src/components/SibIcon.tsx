import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SibIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SibIcon" {...props} />;
};

export default SibIcon;
