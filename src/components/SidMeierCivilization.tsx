import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SidMeierCivilization: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SidMeierCivilization" {...props} />;
};

export default SidMeierCivilization;
