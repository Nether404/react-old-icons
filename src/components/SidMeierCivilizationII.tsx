import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SidMeierCivilizationII: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SidMeierCivilizationII" {...props} />;
};

export default SidMeierCivilizationII;
