import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SidMeiersCivilizationV: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SidMeiersCivilizationV" {...props} />;
};

export default SidMeiersCivilizationV;
