import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Sidekick: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Sidekick" {...props} />;
};

export default Sidekick;
