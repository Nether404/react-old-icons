import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SierraLogo: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SierraLogo" {...props} />;
};

export default SierraLogo;
