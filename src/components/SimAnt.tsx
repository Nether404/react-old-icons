import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SimAnt: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SimAnt" {...props} />;
};

export default SimAnt;
