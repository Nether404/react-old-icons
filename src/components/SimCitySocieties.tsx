import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SimCitySocieties: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SimCitySocieties" {...props} />;
};

export default SimCitySocieties;
