import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SimCopter: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SimCopter" {...props} />;
};

export default SimCopter;
