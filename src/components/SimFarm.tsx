import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SimFarm: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SimFarm" {...props} />;
};

export default SimFarm;
