import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SimIsle: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SimIsle" {...props} />;
};

export default SimIsle;
