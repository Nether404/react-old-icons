import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SimTower: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SimTower" {...props} />;
};

export default SimTower;
