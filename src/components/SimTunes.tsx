import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SimTunes: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SimTunes" {...props} />;
};

export default SimTunes;
