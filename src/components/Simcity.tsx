import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Simcity: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Simcity" {...props} />;
};

export default Simcity;
