import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Simcity2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Simcity2" {...props} />;
};

export default Simcity2;
