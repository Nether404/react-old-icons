import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Simcity4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Simcity4" {...props} />;
};

export default Simcity4;
