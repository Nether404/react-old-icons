import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SimcityClassic: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SimcityClassic" {...props} />;
};

export default SimcityClassic;
