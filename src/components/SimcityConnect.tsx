import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SimcityConnect: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SimcityConnect" {...props} />;
};

export default SimcityConnect;
