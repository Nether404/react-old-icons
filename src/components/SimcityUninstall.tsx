import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SimcityUninstall: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SimcityUninstall" {...props} />;
};

export default SimcityUninstall;
