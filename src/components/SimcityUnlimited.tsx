import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SimcityUnlimited: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SimcityUnlimited" {...props} />;
};

export default SimcityUnlimited;
