import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SimcityUnlimited1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SimcityUnlimited1" {...props} />;
};

export default SimcityUnlimited1;
