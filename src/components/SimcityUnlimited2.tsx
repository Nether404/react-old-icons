import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SimcityUnlimited2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SimcityUnlimited2" {...props} />;
};

export default SimcityUnlimited2;
