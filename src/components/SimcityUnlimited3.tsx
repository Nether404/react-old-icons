import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SimcityUnlimited3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SimcityUnlimited3" {...props} />;
};

export default SimcityUnlimited3;
