import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SimcityUnlimited4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SimcityUnlimited4" {...props} />;
};

export default SimcityUnlimited4;
