import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SimcityUrban: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SimcityUrban" {...props} />;
};

export default SimcityUrban;
