import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SimcityUrban1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SimcityUrban1" {...props} />;
};

export default SimcityUrban1;
