import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SimcityUrban2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SimcityUrban2" {...props} />;
};

export default SimcityUrban2;
