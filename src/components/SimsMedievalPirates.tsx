import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SimsMedievalPirates: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SimsMedievalPirates" {...props} />;
};

export default SimsMedievalPirates;
