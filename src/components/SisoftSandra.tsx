import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SisoftSandra: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SisoftSandra" {...props} />;
};

export default SisoftSandra;
