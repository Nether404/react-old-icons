import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SisoftSandra1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SisoftSandra1" {...props} />;
};

export default SisoftSandra1;
