import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SisoftSandra2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SisoftSandra2" {...props} />;
};

export default SisoftSandra2;
