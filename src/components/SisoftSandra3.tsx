import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SisoftSandra3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SisoftSandra3" {...props} />;
};

export default SisoftSandra3;
