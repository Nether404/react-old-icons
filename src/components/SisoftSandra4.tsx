import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SisoftSandra4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SisoftSandra4" {...props} />;
};

export default SisoftSandra4;
