import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SisoftSandra5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SisoftSandra5" {...props} />;
};

export default SisoftSandra5;
