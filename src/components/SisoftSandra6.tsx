import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SisoftSandra6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SisoftSandra6" {...props} />;
};

export default SisoftSandra6;
