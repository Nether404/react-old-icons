import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SisoftSandra8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SisoftSandra8" {...props} />;
};

export default SisoftSandra8;
