import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SisoftSandra9: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SisoftSandra9" {...props} />;
};

export default SisoftSandra9;
