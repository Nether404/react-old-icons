import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SkiFree: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SkiFree" {...props} />;
};

export default SkiFree;
