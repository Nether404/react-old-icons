import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SlayComponent: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SlayComponent" {...props} />;
};

export default SlayComponent;
