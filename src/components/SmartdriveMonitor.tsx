import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SmartdriveMonitor: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SmartdriveMonitor" {...props} />;
};

export default SmartdriveMonitor;
