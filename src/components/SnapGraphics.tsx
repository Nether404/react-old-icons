import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SnapGraphics: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SnapGraphics" {...props} />;
};

export default SnapGraphics;
