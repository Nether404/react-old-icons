import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Snes9x: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Snes9x" {...props} />;
};

export default Snes9x;
