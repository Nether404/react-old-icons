import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SoftwareLabsBoard: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SoftwareLabsBoard" {...props} />;
};

export default SoftwareLabsBoard;
