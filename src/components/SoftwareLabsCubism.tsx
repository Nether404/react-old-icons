import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SoftwareLabsCubism: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SoftwareLabsCubism" {...props} />;
};

export default SoftwareLabsCubism;
