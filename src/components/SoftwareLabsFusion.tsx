import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SoftwareLabsFusion: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SoftwareLabsFusion" {...props} />;
};

export default SoftwareLabsFusion;
