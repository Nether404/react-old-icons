import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SoftwareLabsSnoopy: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SoftwareLabsSnoopy" {...props} />;
};

export default SoftwareLabsSnoopy;
