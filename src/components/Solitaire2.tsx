import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Solitaire2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Solitaire2" {...props} />;
};

export default Solitaire2;
