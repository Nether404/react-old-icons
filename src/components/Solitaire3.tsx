import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Solitaire3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Solitaire3" {...props} />;
};

export default Solitaire3;
