import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Solitaire4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Solitaire4" {...props} />;
};

export default Solitaire4;
