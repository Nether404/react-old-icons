import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SolitaireCardBack: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SolitaireCardBack" {...props} />;
};

export default SolitaireCardBack;
