import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SolitaireCardBack10: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SolitaireCardBack10" {...props} />;
};

export default SolitaireCardBack10;
