import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SolitaireCardBack11: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SolitaireCardBack11" {...props} />;
};

export default SolitaireCardBack11;
