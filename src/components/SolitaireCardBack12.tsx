import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SolitaireCardBack12: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SolitaireCardBack12" {...props} />;
};

export default SolitaireCardBack12;
