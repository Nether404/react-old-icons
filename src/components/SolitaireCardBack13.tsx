import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SolitaireCardBack13: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SolitaireCardBack13" {...props} />;
};

export default SolitaireCardBack13;
