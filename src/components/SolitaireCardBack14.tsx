import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SolitaireCardBack14: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SolitaireCardBack14" {...props} />;
};

export default SolitaireCardBack14;
