import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SolitaireCardBack15: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SolitaireCardBack15" {...props} />;
};

export default SolitaireCardBack15;
