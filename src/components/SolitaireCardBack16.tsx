import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SolitaireCardBack16: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SolitaireCardBack16" {...props} />;
};

export default SolitaireCardBack16;
