import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SolitaireCardBack17: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SolitaireCardBack17" {...props} />;
};

export default SolitaireCardBack17;
