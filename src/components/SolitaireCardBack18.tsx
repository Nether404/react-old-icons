import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SolitaireCardBack18: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SolitaireCardBack18" {...props} />;
};

export default SolitaireCardBack18;
