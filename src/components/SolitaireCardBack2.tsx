import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SolitaireCardBack2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SolitaireCardBack2" {...props} />;
};

export default SolitaireCardBack2;
