import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SolitaireCardBack3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SolitaireCardBack3" {...props} />;
};

export default SolitaireCardBack3;
