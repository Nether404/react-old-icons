import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SolitaireCardBack5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SolitaireCardBack5" {...props} />;
};

export default SolitaireCardBack5;
