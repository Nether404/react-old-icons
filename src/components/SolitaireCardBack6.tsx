import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SolitaireCardBack6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SolitaireCardBack6" {...props} />;
};

export default SolitaireCardBack6;
