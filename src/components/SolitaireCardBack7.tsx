import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SolitaireCardBack7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SolitaireCardBack7" {...props} />;
};

export default SolitaireCardBack7;
