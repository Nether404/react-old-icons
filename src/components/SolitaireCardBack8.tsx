import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SolitaireCardBack8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SolitaireCardBack8" {...props} />;
};

export default SolitaireCardBack8;
