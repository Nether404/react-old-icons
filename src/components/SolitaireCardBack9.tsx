import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SolitaireCardBack9: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SolitaireCardBack9" {...props} />;
};

export default SolitaireCardBack9;
