import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SolitaireKing: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SolitaireKing" {...props} />;
};

export default SolitaireKing;
