import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SonicAdventureDX: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SonicAdventureDX" {...props} />;
};

export default SonicAdventureDX;
