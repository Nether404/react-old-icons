import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SonicCD: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SonicCD" {...props} />;
};

export default SonicCD;
