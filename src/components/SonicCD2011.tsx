import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SonicCD2011: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SonicCD2011" {...props} />;
};

export default SonicCD2011;
