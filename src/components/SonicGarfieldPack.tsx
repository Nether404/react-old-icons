import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SonicGarfieldPack: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SonicGarfieldPack" {...props} />;
};

export default SonicGarfieldPack;
