import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SonicKnuckles: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SonicKnuckles" {...props} />;
};

export default SonicKnuckles;
