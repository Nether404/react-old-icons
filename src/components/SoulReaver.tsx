import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SoulReaver: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SoulReaver" {...props} />;
};

export default SoulReaver;
