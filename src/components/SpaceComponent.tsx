import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SpaceComponent: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SpaceComponent" {...props} />;
};

export default SpaceComponent;
