import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SpaceMonger: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SpaceMonger" {...props} />;
};

export default SpaceMonger;
