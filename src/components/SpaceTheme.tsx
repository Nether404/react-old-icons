import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SpaceTheme: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SpaceTheme" {...props} />;
};

export default SpaceTheme;
