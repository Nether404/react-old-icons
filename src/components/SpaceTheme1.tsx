import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SpaceTheme1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SpaceTheme1" {...props} />;
};

export default SpaceTheme1;
