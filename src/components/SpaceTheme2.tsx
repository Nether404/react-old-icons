import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SpaceTheme2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SpaceTheme2" {...props} />;
};

export default SpaceTheme2;
