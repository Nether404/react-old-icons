import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SpiderSolitaire: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SpiderSolitaire" {...props} />;
};

export default SpiderSolitaire;
