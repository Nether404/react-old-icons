import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SpiderSolitaire2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SpiderSolitaire2" {...props} />;
};

export default SpiderSolitaire2;
