import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SpiroComponent: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SpiroComponent" {...props} />;
};

export default SpiroComponent;
