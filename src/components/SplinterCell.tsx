import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SplinterCell: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SplinterCell" {...props} />;
};

export default SplinterCell;
