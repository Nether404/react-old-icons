import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Spore: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Spore" {...props} />;
};

export default Spore;
