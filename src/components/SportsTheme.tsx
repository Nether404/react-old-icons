import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SportsTheme: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SportsTheme" {...props} />;
};

export default SportsTheme;
