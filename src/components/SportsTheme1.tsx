import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SportsTheme1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SportsTheme1" {...props} />;
};

export default SportsTheme1;
