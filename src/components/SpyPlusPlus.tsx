import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SpyPlusPlus: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SpyPlusPlus" {...props} />;
};

export default SpyPlusPlus;
