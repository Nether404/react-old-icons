import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SpybotSearchDestroy: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SpybotSearchDestroy" {...props} />;
};

export default SpybotSearchDestroy;
