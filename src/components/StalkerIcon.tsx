import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const StalkerIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="StalkerIcon" {...props} />;
};

export default StalkerIcon;
