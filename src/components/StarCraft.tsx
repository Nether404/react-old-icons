import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const StarCraft: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="StarCraft" {...props} />;
};

export default StarCraft;
