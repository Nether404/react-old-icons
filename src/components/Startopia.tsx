import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Startopia: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Startopia" {...props} />;
};

export default Startopia;
