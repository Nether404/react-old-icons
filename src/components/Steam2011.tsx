import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Steam2011: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Steam2011" {...props} />;
};

export default Steam2011;
