import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Steam20112: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Steam20112" {...props} />;
};

export default Steam20112;
