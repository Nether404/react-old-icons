import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const StreetsOfSimCity: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="StreetsOfSimCity" {...props} />;
};

export default StreetsOfSimCity;
