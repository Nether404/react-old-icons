import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const StressApp: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="StressApp" {...props} />;
};

export default StressApp;
