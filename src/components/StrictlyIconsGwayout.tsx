import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const StrictlyIconsGwayout: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="StrictlyIconsGwayout" {...props} />;
};

export default StrictlyIconsGwayout;
