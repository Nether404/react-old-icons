import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const StrictlyPaperoll: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="StrictlyPaperoll" {...props} />;
};

export default StrictlyPaperoll;
