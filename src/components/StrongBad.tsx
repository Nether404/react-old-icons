import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const StrongBad: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="StrongBad" {...props} />;
};

export default StrongBad;
