import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const StudioMax3D: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="StudioMax3D" {...props} />;
};

export default StudioMax3D;
