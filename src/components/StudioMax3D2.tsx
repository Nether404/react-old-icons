import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const StudioMax3D2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="StudioMax3D2" {...props} />;
};

export default StudioMax3D2;
