import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SuperMeatBoy: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SuperMeatBoy" {...props} />;
};

export default SuperMeatBoy;
