import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SuperType: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SuperType" {...props} />;
};

export default SuperType;
