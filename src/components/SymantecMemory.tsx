import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SymantecMemory: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SymantecMemory" {...props} />;
};

export default SymantecMemory;
