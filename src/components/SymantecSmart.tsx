import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SymantecSmart: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SymantecSmart" {...props} />;
};

export default SymantecSmart;
