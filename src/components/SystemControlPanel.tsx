import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const SystemControlPanel: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="SystemControlPanel" {...props} />;
};

export default SystemControlPanel;
