import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TalkingSolitaire2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TalkingSolitaire2" {...props} />;
};

export default TalkingSolitaire2;
