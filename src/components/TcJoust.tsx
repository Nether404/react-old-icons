import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TcJoust: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TcJoust" {...props} />;
};

export default TcJoust;
