import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TcRobot: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TcRobot" {...props} />;
};

export default TcRobot;
