import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TeamFortress2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TeamFortress2" {...props} />;
};

export default TeamFortress2;
