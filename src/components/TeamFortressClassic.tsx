import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TeamFortressClassic: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TeamFortressClassic" {...props} />;
};

export default TeamFortressClassic;
