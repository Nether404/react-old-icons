import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TetraVex: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TetraVex" {...props} />;
};

export default TetraVex;
