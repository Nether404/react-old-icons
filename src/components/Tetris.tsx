import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Tetris: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Tetris" {...props} />;
};

export default Tetris;
