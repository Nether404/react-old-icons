import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TetrisWorlds: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TetrisWorlds" {...props} />;
};

export default TetrisWorlds;
