import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TetrisWorlds2001: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TetrisWorlds2001" {...props} />;
};

export default TetrisWorlds2001;
