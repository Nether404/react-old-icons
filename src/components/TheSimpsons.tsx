import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TheSimpsons: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TheSimpsons" {...props} />;
};

export default TheSimpsons;
