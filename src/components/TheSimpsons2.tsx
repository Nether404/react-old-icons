import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TheSimpsons2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TheSimpsons2" {...props} />;
};

export default TheSimpsons2;
