import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TheSimpsons3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TheSimpsons3" {...props} />;
};

export default TheSimpsons3;
