import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TheSimpsons4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TheSimpsons4" {...props} />;
};

export default TheSimpsons4;
