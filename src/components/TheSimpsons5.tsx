import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TheSimpsons5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TheSimpsons5" {...props} />;
};

export default TheSimpsons5;
