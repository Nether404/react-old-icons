import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TheSimpsons6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TheSimpsons6" {...props} />;
};

export default TheSimpsons6;
