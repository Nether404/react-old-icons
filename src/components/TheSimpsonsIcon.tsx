import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TheSimpsonsIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TheSimpsonsIcon" {...props} />;
};

export default TheSimpsonsIcon;
