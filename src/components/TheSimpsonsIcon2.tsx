import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TheSimpsonsIcon2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TheSimpsonsIcon2" {...props} />;
};

export default TheSimpsonsIcon2;
