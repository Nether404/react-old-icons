import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TheSimpsonsIcon3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TheSimpsonsIcon3" {...props} />;
};

export default TheSimpsonsIcon3;
