import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TheSims: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TheSims" {...props} />;
};

export default TheSims;
