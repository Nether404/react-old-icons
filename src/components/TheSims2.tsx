import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TheSims2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TheSims2" {...props} />;
};

export default TheSims2;
