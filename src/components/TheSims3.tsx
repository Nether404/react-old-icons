import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TheSims3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TheSims3" {...props} />;
};

export default TheSims3;
