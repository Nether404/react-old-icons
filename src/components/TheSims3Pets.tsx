import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TheSims3Pets: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TheSims3Pets" {...props} />;
};

export default TheSims3Pets;
