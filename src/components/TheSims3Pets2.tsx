import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TheSims3Pets2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TheSims3Pets2" {...props} />;
};

export default TheSims3Pets2;
