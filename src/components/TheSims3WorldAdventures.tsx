import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TheSims3WorldAdventures: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TheSims3WorldAdventures" {...props} />;
};

export default TheSims3WorldAdventures;
