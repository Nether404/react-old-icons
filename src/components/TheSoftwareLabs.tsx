import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TheSoftwareLabs: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TheSoftwareLabs" {...props} />;
};

export default TheSoftwareLabs;
