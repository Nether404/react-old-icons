import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TheSoftwareLabs2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TheSoftwareLabs2" {...props} />;
};

export default TheSoftwareLabs2;
