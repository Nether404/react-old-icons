import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TheSoftwareLabs3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TheSoftwareLabs3" {...props} />;
};

export default TheSoftwareLabs3;
