import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ThemeHospital: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ThemeHospital" {...props} />;
};

export default ThemeHospital;
