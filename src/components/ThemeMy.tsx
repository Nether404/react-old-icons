import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ThemeMy: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ThemeMy" {...props} />;
};

export default ThemeMy;
