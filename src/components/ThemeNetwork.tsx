import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ThemeNetwork: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ThemeNetwork" {...props} />;
};

export default ThemeNetwork;
