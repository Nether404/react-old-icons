import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ThemeRecycle: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ThemeRecycle" {...props} />;
};

export default ThemeRecycle;
