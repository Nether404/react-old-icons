import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ThiefDeadly: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ThiefDeadly" {...props} />;
};

export default ThiefDeadly;
