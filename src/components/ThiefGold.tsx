import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ThiefGold: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ThiefGold" {...props} />;
};

export default ThiefGold;
