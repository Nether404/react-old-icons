import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ThiefII: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ThiefII" {...props} />;
};

export default ThiefII;
