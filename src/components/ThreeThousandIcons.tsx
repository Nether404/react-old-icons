import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ThreeThousandIcons: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ThreeThousandIcons" {...props} />;
};

export default ThreeThousandIcons;
