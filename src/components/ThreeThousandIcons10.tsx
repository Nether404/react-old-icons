import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ThreeThousandIcons10: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ThreeThousandIcons10" {...props} />;
};

export default ThreeThousandIcons10;
