import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ThreeThousandIcons2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ThreeThousandIcons2" {...props} />;
};

export default ThreeThousandIcons2;
