import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ThreeThousandIcons3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ThreeThousandIcons3" {...props} />;
};

export default ThreeThousandIcons3;
