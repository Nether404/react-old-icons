import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ThreeThousandIcons5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ThreeThousandIcons5" {...props} />;
};

export default ThreeThousandIcons5;
