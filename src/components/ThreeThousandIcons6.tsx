import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ThreeThousandIcons6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ThreeThousandIcons6" {...props} />;
};

export default ThreeThousandIcons6;
