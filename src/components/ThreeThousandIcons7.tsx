import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ThreeThousandIcons7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ThreeThousandIcons7" {...props} />;
};

export default ThreeThousandIcons7;
