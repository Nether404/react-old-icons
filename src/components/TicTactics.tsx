import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TicTactics: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TicTactics" {...props} />;
};

export default TicTactics;
