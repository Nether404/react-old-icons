import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TiptopDeluxe: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TiptopDeluxe" {...props} />;
};

export default TiptopDeluxe;
