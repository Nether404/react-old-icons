import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TombRaiderII: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TombRaiderII" {...props} />;
};

export default TombRaiderII;
