import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Tonica: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Tonica" {...props} />;
};

export default Tonica;
