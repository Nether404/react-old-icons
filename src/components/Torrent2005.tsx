import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Torrent2005: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Torrent2005" {...props} />;
};

export default Torrent2005;
