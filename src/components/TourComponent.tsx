import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TourComponent: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TourComponent" {...props} />;
};

export default TourComponent;
