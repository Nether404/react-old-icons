import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Transmath: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Transmath" {...props} />;
};

export default Transmath;
