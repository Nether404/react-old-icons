import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TravelTheme: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TravelTheme" {...props} />;
};

export default TravelTheme;
