import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TravelTheme1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TravelTheme1" {...props} />;
};

export default TravelTheme1;
