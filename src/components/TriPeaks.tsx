import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TriPeaks: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TriPeaks" {...props} />;
};

export default TriPeaks;
