import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TriTris: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TriTris" {...props} />;
};

export default TriTris;
