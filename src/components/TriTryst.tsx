import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TriTryst: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TriTryst" {...props} />;
};

export default TriTryst;
