import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TrickStyle: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TrickStyle" {...props} />;
};

export default TrickStyle;
