import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TrillianBasic: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TrillianBasic" {...props} />;
};

export default TrillianBasic;
