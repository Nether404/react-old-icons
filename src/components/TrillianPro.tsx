import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TrillianPro: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TrillianPro" {...props} />;
};

export default TrillianPro;
