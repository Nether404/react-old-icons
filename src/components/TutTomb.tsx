import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TutTomb: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TutTomb" {...props} />;
};

export default TutTomb;
