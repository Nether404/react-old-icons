import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TweakUI: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TweakUI" {...props} />;
};

export default TweakUI;
