import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const TyperShark: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="TyperShark" {...props} />;
};

export default TyperShark;
