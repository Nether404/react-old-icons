import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Tyrian: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Tyrian" {...props} />;
};

export default Tyrian;
