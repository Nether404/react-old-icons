import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UleadCool: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UleadCool" {...props} />;
};

export default UleadCool;
