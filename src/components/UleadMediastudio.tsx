import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UleadMediastudio: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UleadMediastudio" {...props} />;
};

export default UleadMediastudio;
