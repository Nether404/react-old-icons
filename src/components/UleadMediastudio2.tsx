import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UleadMediastudio2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UleadMediastudio2" {...props} />;
};

export default UleadMediastudio2;
