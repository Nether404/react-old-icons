import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UltraRacing: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UltraRacing" {...props} />;
};

export default UltraRacing;
