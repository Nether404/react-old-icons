import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UltraVNCViewer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UltraVNCViewer" {...props} />;
};

export default UltraVNCViewer;
