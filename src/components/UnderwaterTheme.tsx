import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UnderwaterTheme: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UnderwaterTheme" {...props} />;
};

export default UnderwaterTheme;
