import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UnderwaterTheme1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UnderwaterTheme1" {...props} />;
};

export default UnderwaterTheme1;
