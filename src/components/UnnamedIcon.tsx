import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UnnamedIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UnnamedIcon" {...props} />;
};

export default UnnamedIcon;
