import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UnrealComponent: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UnrealComponent" {...props} />;
};

export default UnrealComponent;
