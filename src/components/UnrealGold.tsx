import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UnrealGold: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UnrealGold" {...props} />;
};

export default UnrealGold;
