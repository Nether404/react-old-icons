import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UnrealGold2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UnrealGold2" {...props} />;
};

export default UnrealGold2;
