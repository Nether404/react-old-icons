import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UnrealTournament: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UnrealTournament" {...props} />;
};

export default UnrealTournament;
