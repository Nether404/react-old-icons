import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UnrealTournament2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UnrealTournament2" {...props} />;
};

export default UnrealTournament2;
