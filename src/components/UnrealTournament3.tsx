import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UnrealTournament3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UnrealTournament3" {...props} />;
};

export default UnrealTournament3;
