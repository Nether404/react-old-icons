import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UnrealTournament4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UnrealTournament4" {...props} />;
};

export default UnrealTournament4;
