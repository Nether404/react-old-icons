import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UsaTheme: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UsaTheme" {...props} />;
};

export default UsaTheme;
