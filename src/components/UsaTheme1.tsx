import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UsaTheme1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UsaTheme1" {...props} />;
};

export default UsaTheme1;
