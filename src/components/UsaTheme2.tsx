import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UsaTheme2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UsaTheme2" {...props} />;
};

export default UsaTheme2;
