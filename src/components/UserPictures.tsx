import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UserPictures: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UserPictures" {...props} />;
};

export default UserPictures;
