import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UserPictures10: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UserPictures10" {...props} />;
};

export default UserPictures10;
