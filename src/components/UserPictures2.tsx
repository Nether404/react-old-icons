import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UserPictures2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UserPictures2" {...props} />;
};

export default UserPictures2;
