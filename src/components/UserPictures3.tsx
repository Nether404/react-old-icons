import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UserPictures3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UserPictures3" {...props} />;
};

export default UserPictures3;
