import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UserPictures4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UserPictures4" {...props} />;
};

export default UserPictures4;
