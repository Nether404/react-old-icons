import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UserPictures5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UserPictures5" {...props} />;
};

export default UserPictures5;
