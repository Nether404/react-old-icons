import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UserPictures6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UserPictures6" {...props} />;
};

export default UserPictures6;
