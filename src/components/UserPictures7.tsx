import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UserPictures7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UserPictures7" {...props} />;
};

export default UserPictures7;
