import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UserPictures8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UserPictures8" {...props} />;
};

export default UserPictures8;
