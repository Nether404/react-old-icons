import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const UserPictures9: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="UserPictures9" {...props} />;
};

export default UserPictures9;
