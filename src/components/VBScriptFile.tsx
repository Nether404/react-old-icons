import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VBScriptFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VBScriptFile" {...props} />;
};

export default VBScriptFile;
