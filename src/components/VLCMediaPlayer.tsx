import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VLCMediaPlayer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VLCMediaPlayer" {...props} />;
};

export default VLCMediaPlayer;
