import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VMwareWorkstation4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VMwareWorkstation4" {...props} />;
};

export default VMwareWorkstation4;
