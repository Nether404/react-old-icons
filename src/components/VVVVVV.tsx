import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VVVVVV: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VVVVVV" {...props} />;
};

export default VVVVVV;
