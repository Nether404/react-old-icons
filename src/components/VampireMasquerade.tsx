import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VampireMasquerade: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VampireMasquerade" {...props} />;
};

export default VampireMasquerade;
