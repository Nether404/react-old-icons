import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VirtualDub: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VirtualDub" {...props} />;
};

export default VirtualDub;
