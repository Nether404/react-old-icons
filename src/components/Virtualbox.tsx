import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Virtualbox: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Virtualbox" {...props} />;
};

export default Virtualbox;
