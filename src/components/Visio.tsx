import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Visio: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Visio" {...props} />;
};

export default Visio;
