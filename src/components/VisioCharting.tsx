import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisioCharting: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisioCharting" {...props} />;
};

export default VisioCharting;
