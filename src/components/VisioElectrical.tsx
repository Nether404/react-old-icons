import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisioElectrical: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisioElectrical" {...props} />;
};

export default VisioElectrical;
