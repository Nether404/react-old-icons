import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisioGraphmaker: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisioGraphmaker" {...props} />;
};

export default VisioGraphmaker;
