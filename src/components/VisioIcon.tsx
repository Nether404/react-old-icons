import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisioIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisioIcon" {...props} />;
};

export default VisioIcon;
