import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisioMap: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisioMap" {...props} />;
};

export default VisioMap;
