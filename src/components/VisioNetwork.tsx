import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisioNetwork: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisioNetwork" {...props} />;
};

export default VisioNetwork;
