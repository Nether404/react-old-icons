import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisioSpace: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisioSpace" {...props} />;
};

export default VisioSpace;
