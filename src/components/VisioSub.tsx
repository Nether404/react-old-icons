import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisioSub: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisioSub" {...props} />;
};

export default VisioSub;
