import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualBasic: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualBasic" {...props} />;
};

export default VisualBasic;
