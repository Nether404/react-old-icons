import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualBasic1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualBasic1" {...props} />;
};

export default VisualBasic1;
