import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualBasic2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualBasic2" {...props} />;
};

export default VisualBasic2;
