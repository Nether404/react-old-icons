import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualBasic3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualBasic3" {...props} />;
};

export default VisualBasic3;
