import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualBasic4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualBasic4" {...props} />;
};

export default VisualBasic4;
