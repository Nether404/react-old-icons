import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualBoyAdvance: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualBoyAdvance" {...props} />;
};

export default VisualBoyAdvance;
