import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualCPP22: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualCPP22" {...props} />;
};

export default VisualCPP22;
