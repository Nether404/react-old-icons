import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualCppCodeView: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualCppCodeView" {...props} />;
};

export default VisualCppCodeView;
