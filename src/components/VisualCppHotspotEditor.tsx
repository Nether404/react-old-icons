import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualCppHotspotEditor: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualCppHotspotEditor" {...props} />;
};

export default VisualCppHotspotEditor;
