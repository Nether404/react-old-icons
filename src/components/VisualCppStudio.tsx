import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualCppStudio: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualCppStudio" {...props} />;
};

export default VisualCppStudio;
