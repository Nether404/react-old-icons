import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualProject: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualProject" {...props} />;
};

export default VisualProject;
