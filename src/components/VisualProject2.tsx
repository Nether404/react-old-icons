import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualProject2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualProject2" {...props} />;
};

export default VisualProject2;
