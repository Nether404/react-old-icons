import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudio: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudio" {...props} />;
};

export default VisualStudio;
