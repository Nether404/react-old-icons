import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudio12: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudio12" {...props} />;
};

export default VisualStudio12;
