import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudio2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudio2" {...props} />;
};

export default VisualStudio2;
