import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudio3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudio3" {...props} />;
};

export default VisualStudio3;
