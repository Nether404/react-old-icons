import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudio5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudio5" {...props} />;
};

export default VisualStudio5;
