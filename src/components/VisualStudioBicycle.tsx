import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioBicycle: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioBicycle" {...props} />;
};

export default VisualStudioBicycle;
