import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioBinoculr: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioBinoculr" {...props} />;
};

export default VisualStudioBinoculr;
