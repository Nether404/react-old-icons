import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioBook: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioBook" {...props} />;
};

export default VisualStudioBook;
