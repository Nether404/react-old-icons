import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioBook2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioBook2" {...props} />;
};

export default VisualStudioBook2;
