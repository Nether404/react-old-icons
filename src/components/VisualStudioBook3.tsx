import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioBook3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioBook3" {...props} />;
};

export default VisualStudioBook3;
