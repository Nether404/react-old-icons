import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioBooks: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioBooks" {...props} />;
};

export default VisualStudioBooks;
