import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioBridge: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioBridge" {...props} />;
};

export default VisualStudioBridge;
