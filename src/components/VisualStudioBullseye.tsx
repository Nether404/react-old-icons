import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioBullseye: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioBullseye" {...props} />;
};

export default VisualStudioBullseye;
