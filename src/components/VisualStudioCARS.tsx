import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioCARS: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioCARS" {...props} />;
};

export default VisualStudioCARS;
