import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioCDROM: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioCDROM" {...props} />;
};

export default VisualStudioCDROM;
