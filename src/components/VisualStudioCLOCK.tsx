import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioCLOCK: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioCLOCK" {...props} />;
};

export default VisualStudioCLOCK;
