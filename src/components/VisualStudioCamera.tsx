import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioCamera: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioCamera" {...props} />;
};

export default VisualStudioCamera;
