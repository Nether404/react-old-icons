import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioCheckmark: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioCheckmark" {...props} />;
};

export default VisualStudioCheckmark;
