import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioClip: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioClip" {...props} />;
};

export default VisualStudioClip;
