import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioClip06: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioClip06" {...props} />;
};

export default VisualStudioClip06;
