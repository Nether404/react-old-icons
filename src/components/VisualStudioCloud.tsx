import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioCloud: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioCloud" {...props} />;
};

export default VisualStudioCloud;
