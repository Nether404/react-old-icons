import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioDisk: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioDisk" {...props} />;
};

export default VisualStudioDisk;
