import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioDisk01: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioDisk01" {...props} />;
};

export default VisualStudioDisk01;
