import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioDisks: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioDisks" {...props} />;
};

export default VisualStudioDisks;
