import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioDrag: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioDrag" {...props} />;
};

export default VisualStudioDrag;
