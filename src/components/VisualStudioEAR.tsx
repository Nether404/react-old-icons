import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioEAR: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioEAR" {...props} />;
};

export default VisualStudioEAR;
