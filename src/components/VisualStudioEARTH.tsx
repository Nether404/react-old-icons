import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioEARTH: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioEARTH" {...props} />;
};

export default VisualStudioEARTH;
