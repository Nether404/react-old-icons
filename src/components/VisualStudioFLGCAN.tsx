import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioFLGCAN: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioFLGCAN" {...props} />;
};

export default VisualStudioFLGCAN;
