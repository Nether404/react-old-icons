import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioFLGFIN: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioFLGFIN" {...props} />;
};

export default VisualStudioFLGFIN;
