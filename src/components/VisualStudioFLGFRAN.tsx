import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioFLGFRAN: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioFLGFRAN" {...props} />;
};

export default VisualStudioFLGFRAN;
