import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioFLGIREL: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioFLGIREL" {...props} />;
};

export default VisualStudioFLGIREL;
