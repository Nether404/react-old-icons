import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioFLGJAPAN: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioFLGJAPAN" {...props} />;
};

export default VisualStudioFLGJAPAN;
