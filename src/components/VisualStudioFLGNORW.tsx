import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioFLGNORW: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioFLGNORW" {...props} />;
};

export default VisualStudioFLGNORW;
