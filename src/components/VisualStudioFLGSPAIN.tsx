import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioFLGSPAIN: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioFLGSPAIN" {...props} />;
};

export default VisualStudioFLGSPAIN;
