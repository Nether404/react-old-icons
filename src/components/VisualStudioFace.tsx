import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioFace: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioFace" {...props} />;
};

export default VisualStudioFace;
