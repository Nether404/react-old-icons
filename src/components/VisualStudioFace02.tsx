import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioFace02: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioFace02" {...props} />;
};

export default VisualStudioFace02;
