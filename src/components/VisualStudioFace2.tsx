import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioFace2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioFace2" {...props} />;
};

export default VisualStudioFace2;
