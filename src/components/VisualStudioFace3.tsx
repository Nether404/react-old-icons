import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioFace3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioFace3" {...props} />;
};

export default VisualStudioFace3;
