import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioFace4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioFace4" {...props} />;
};

export default VisualStudioFace4;
