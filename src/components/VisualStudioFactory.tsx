import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioFactory: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioFactory" {...props} />;
};

export default VisualStudioFactory;
