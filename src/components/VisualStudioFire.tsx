import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioFire: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioFire" {...props} />;
};

export default VisualStudioFire;
