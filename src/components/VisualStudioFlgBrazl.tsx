import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioFlgBrazl: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioFlgBrazl" {...props} />;
};

export default VisualStudioFlgBrazl;
