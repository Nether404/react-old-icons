import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioFlggerm: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioFlggerm" {...props} />;
};

export default VisualStudioFlggerm;
