import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioFolder: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioFolder" {...props} />;
};

export default VisualStudioFolder;
