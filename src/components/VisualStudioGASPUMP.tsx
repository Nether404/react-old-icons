import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioGASPUMP: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioGASPUMP" {...props} />;
};

export default VisualStudioGASPUMP;
