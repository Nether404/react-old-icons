import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioGraph: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioGraph" {...props} />;
};

export default VisualStudioGraph;
