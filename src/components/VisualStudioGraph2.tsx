import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioGraph2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioGraph2" {...props} />;
};

export default VisualStudioGraph2;
