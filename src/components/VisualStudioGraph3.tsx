import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioGraph3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioGraph3" {...props} />;
};

export default VisualStudioGraph3;
