import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioGraph4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioGraph4" {...props} />;
};

export default VisualStudioGraph4;
