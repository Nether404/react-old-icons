import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioHammer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioHammer" {...props} />;
};

export default VisualStudioHammer;
