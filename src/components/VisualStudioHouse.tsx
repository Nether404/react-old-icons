import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioHouse: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioHouse" {...props} />;
};

export default VisualStudioHouse;
