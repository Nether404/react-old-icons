import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioLITENING: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioLITENING" {...props} />;
};

export default VisualStudioLITENING;
