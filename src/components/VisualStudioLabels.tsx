import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioLabels: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioLabels" {...props} />;
};

export default VisualStudioLabels;
