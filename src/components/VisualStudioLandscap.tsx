import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioLandscap: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioLandscap" {...props} />;
};

export default VisualStudioLandscap;
