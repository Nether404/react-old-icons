import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioLightoff: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioLightoff" {...props} />;
};

export default VisualStudioLightoff;
