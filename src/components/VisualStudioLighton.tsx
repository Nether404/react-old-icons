import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioLighton: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioLighton" {...props} />;
};

export default VisualStudioLighton;
