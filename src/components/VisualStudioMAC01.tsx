import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMAC01: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMAC01" {...props} />;
};

export default VisualStudioMAC01;
