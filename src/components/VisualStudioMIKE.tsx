import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMIKE: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMIKE" {...props} />;
};

export default VisualStudioMIKE;
