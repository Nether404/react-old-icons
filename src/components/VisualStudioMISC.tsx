import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMISC: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMISC" {...props} />;
};

export default VisualStudioMISC;
