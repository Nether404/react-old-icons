import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMail: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMail" {...props} />;
};

export default VisualStudioMail;
