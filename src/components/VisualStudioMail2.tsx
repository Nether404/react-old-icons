import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMail2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMail2" {...props} />;
};

export default VisualStudioMail2;
