import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMail3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMail3" {...props} />;
};

export default VisualStudioMail3;
