import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMail4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMail4" {...props} />;
};

export default VisualStudioMail4;
