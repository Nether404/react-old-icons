import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMail5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMail5" {...props} />;
};

export default VisualStudioMail5;
