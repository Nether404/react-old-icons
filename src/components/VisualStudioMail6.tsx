import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMail6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMail6" {...props} />;
};

export default VisualStudioMail6;
