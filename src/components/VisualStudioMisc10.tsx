import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMisc10: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMisc10" {...props} />;
};

export default VisualStudioMisc10;
