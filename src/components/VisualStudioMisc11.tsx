import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMisc11: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMisc11" {...props} />;
};

export default VisualStudioMisc11;
