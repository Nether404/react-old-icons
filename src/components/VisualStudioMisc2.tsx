import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMisc2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMisc2" {...props} />;
};

export default VisualStudioMisc2;
