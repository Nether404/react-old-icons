import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMisc3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMisc3" {...props} />;
};

export default VisualStudioMisc3;
