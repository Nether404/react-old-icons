import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMisc4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMisc4" {...props} />;
};

export default VisualStudioMisc4;
