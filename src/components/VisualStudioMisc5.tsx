import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMisc5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMisc5" {...props} />;
};

export default VisualStudioMisc5;
