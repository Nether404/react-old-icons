import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMisc6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMisc6" {...props} />;
};

export default VisualStudioMisc6;
