import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMisc7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMisc7" {...props} />;
};

export default VisualStudioMisc7;
