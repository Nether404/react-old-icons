import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMisc8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMisc8" {...props} />;
};

export default VisualStudioMisc8;
