import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMisc9: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMisc9" {...props} />;
};

export default VisualStudioMisc9;
