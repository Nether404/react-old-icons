import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMouse: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMouse" {...props} />;
};

export default VisualStudioMouse;
