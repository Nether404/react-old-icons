import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMouse2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMouse2" {...props} />;
};

export default VisualStudioMouse2;
