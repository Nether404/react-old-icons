import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMsgbox2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMsgbox2" {...props} />;
};

export default VisualStudioMsgbox2;
