import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMsgbox3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMsgbox3" {...props} />;
};

export default VisualStudioMsgbox3;
