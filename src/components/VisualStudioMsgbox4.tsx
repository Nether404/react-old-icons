import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioMsgbox4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioMsgbox4" {...props} />;
};

export default VisualStudioMsgbox4;
