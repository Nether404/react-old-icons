import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioNET: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioNET" {...props} />;
};

export default VisualStudioNET;
