import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioNET06: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioNET06" {...props} />;
};

export default VisualStudioNET06;
