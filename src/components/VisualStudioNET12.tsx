import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioNET12: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioNET12" {...props} />;
};

export default VisualStudioNET12;
