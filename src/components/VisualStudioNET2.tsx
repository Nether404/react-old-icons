import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioNET2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioNET2" {...props} />;
};

export default VisualStudioNET2;
