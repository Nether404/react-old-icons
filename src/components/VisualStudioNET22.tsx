import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioNET22: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioNET22" {...props} />;
};

export default VisualStudioNET22;
