import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioNET3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioNET3" {...props} />;
};

export default VisualStudioNET3;
