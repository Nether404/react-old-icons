import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioNET4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioNET4" {...props} />;
};

export default VisualStudioNET4;
