import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioNOTE16: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioNOTE16" {...props} />;
};

export default VisualStudioNOTE16;
