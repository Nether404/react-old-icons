import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioNote: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioNote" {...props} />;
};

export default VisualStudioNote;
