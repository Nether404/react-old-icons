import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioNote02: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioNote02" {...props} />;
};

export default VisualStudioNote02;
