import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioNote05: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioNote05" {...props} />;
};

export default VisualStudioNote05;
