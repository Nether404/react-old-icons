import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioNote08: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioNote08" {...props} />;
};

export default VisualStudioNote08;
