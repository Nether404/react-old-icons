import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioNote13: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioNote13" {...props} />;
};

export default VisualStudioNote13;
