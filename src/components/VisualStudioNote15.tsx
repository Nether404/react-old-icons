import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioNote15: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioNote15" {...props} />;
};

export default VisualStudioNote15;
