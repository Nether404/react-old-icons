import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioPC: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioPC" {...props} />;
};

export default VisualStudioPC;
