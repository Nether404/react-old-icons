import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioPEN01: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioPEN01" {...props} />;
};

export default VisualStudioPEN01;
