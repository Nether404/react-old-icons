import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioPens: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioPens" {...props} />;
};

export default VisualStudioPens;
