import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioPhone: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioPhone" {...props} />;
};

export default VisualStudioPhone;
