import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioPhone11: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioPhone11" {...props} />;
};

export default VisualStudioPhone11;
