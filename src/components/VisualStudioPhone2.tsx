import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioPhone2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioPhone2" {...props} />;
};

export default VisualStudioPhone2;
