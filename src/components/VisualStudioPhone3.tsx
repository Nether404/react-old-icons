import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioPhone3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioPhone3" {...props} />;
};

export default VisualStudioPhone3;
