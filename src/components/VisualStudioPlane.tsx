import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioPlane: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioPlane" {...props} />;
};

export default VisualStudioPlane;
