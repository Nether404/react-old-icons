import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioPoint: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioPoint" {...props} />;
};

export default VisualStudioPoint;
