import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioPoint02: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioPoint02" {...props} />;
};

export default VisualStudioPoint02;
