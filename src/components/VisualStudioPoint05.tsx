import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioPoint05: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioPoint05" {...props} />;
};

export default VisualStudioPoint05;
