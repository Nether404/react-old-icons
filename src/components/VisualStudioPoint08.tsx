import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioPoint08: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioPoint08" {...props} />;
};

export default VisualStudioPoint08;
