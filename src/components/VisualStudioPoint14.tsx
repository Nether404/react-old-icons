import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioPoint14: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioPoint14" {...props} />;
};

export default VisualStudioPoint14;
