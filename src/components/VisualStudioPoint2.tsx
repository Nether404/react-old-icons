import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioPoint2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioPoint2" {...props} />;
};

export default VisualStudioPoint2;
