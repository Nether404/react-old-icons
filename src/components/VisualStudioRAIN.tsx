import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioRAIN: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioRAIN" {...props} />;
};

export default VisualStudioRAIN;
