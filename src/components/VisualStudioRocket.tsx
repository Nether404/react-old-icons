import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioRocket: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioRocket" {...props} />;
};

export default VisualStudioRocket;
