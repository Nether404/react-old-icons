import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioRulers: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioRulers" {...props} />;
};

export default VisualStudioRulers;
