import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioSNOW: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioSNOW" {...props} />;
};

export default VisualStudioSNOW;
