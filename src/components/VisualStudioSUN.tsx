import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioSUN: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioSUN" {...props} />;
};

export default VisualStudioSUN;
