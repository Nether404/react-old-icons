import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioSecur02B: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioSecur02B" {...props} />;
};

export default VisualStudioSecur02B;
