import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioSecur05: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioSecur05" {...props} />;
};

export default VisualStudioSecur05;
