import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioSpyPlusPlus: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioSpyPlusPlus" {...props} />;
};

export default VisualStudioSpyPlusPlus;
