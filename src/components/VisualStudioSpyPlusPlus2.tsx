import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioSpyPlusPlus2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioSpyPlusPlus2" {...props} />;
};

export default VisualStudioSpyPlusPlus2;
