import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioTRFFC05: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioTRFFC05" {...props} />;
};

export default VisualStudioTRFFC05;
