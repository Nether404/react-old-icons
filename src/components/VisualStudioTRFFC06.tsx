import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioTRFFC06: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioTRFFC06" {...props} />;
};

export default VisualStudioTRFFC06;
