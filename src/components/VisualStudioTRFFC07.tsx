import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioTRFFC07: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioTRFFC07" {...props} />;
};

export default VisualStudioTRFFC07;
