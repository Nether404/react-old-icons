import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioTRFFC14: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioTRFFC14" {...props} />;
};

export default VisualStudioTRFFC14;
