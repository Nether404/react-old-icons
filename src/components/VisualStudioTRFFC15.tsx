import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioTRFFC15: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioTRFFC15" {...props} />;
};

export default VisualStudioTRFFC15;
