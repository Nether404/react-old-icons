import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioTRFFC16: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioTRFFC16" {...props} />;
};

export default VisualStudioTRFFC16;
