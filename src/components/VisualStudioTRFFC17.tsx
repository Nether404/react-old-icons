import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioTRFFC17: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioTRFFC17" {...props} />;
};

export default VisualStudioTRFFC17;
