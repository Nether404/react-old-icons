import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioTRFFC18A: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioTRFFC18A" {...props} />;
};

export default VisualStudioTRFFC18A;
