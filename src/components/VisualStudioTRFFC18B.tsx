import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioTRFFC18B: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioTRFFC18B" {...props} />;
};

export default VisualStudioTRFFC18B;
