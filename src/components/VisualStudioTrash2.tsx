import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioTrash2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioTrash2" {...props} />;
};

export default VisualStudioTrash2;
