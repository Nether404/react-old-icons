import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioWRENCH: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioWRENCH" {...props} />;
};

export default VisualStudioWRENCH;
