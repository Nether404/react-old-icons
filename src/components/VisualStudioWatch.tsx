import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioWatch: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioWatch" {...props} />;
};

export default VisualStudioWatch;
