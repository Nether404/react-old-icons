import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VisualStudioWater: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VisualStudioWater" {...props} />;
};

export default VisualStudioWater;
