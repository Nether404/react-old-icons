import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const VmwareWorkstation: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="VmwareWorkstation" {...props} />;
};

export default VmwareWorkstation;
