import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WVMemoryDiagnosticTool: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WVMemoryDiagnosticTool" {...props} />;
};

export default WVMemoryDiagnosticTool;
