import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WVNetworkSharingCenter: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WVNetworkSharingCenter" {...props} />;
};

export default WVNetworkSharingCenter;
