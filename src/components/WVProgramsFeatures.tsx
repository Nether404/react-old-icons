import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WVProgramsFeatures: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WVProgramsFeatures" {...props} />;
};

export default WVProgramsFeatures;
