import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WVReliabilityPerformance: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WVReliabilityPerformance" {...props} />;
};

export default WVReliabilityPerformance;
