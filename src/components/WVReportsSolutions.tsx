import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WVReportsSolutions: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WVReportsSolutions" {...props} />;
};

export default WVReportsSolutions;
