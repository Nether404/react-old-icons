import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WXPAccessibilityOptions: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WXPAccessibilityOptions" {...props} />;
};

export default WXPAccessibilityOptions;
