import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WXPInternetConnectionWizard: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WXPInternetConnectionWizard" {...props} />;
};

export default WXPInternetConnectionWizard;
