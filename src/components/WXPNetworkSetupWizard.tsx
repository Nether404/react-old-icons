import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WXPNetworkSetupWizard: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WXPNetworkSetupWizard" {...props} />;
};

export default WXPNetworkSetupWizard;
