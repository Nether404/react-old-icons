import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WarcraftIII: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WarcraftIII" {...props} />;
};

export default WarcraftIII;
