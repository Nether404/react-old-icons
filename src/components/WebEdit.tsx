import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WebEdit: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WebEdit" {...props} />;
};

export default WebEdit;
