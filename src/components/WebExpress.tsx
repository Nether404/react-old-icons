import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WebExpress: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WebExpress" {...props} />;
};

export default WebExpress;
