import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WebshotsDesktop: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WebshotsDesktop" {...props} />;
};

export default WebshotsDesktop;
