import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WillowPondMediaRack: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WillowPondMediaRack" {...props} />;
};

export default WillowPondMediaRack;
