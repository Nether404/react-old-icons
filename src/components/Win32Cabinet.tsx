import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Win32Cabinet: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Win32Cabinet" {...props} />;
};

export default Win32Cabinet;
