import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WinBrick96: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WinBrick96" {...props} />;
};

export default WinBrick96;
