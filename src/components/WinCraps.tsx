import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WinCraps: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WinCraps" {...props} />;
};

export default WinCraps;
