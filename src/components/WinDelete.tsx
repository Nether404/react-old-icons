import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WinDelete: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WinDelete" {...props} />;
};

export default WinDelete;
