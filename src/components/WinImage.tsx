import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WinImage: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WinImage" {...props} />;
};

export default WinImage;
