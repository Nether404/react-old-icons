import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WinImageSelfExtractor: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WinImageSelfExtractor" {...props} />;
};

export default WinImageSelfExtractor;
