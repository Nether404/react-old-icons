import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WinMX: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WinMX" {...props} />;
};

export default WinMX;
