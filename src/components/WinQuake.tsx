import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WinQuake: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WinQuake" {...props} />;
};

export default WinQuake;
