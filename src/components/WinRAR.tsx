import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WinRAR: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WinRAR" {...props} />;
};

export default WinRAR;
