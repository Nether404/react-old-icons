import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WinRAR2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WinRAR2" {...props} />;
};

export default WinRAR2;
