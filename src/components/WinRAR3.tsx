import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WinRAR3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WinRAR3" {...props} />;
};

export default WinRAR3;
