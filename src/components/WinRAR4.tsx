import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WinRAR4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WinRAR4" {...props} />;
};

export default WinRAR4;
