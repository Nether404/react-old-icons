import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WinRAR5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WinRAR5" {...props} />;
};

export default WinRAR5;
