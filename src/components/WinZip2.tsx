import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WinZip2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WinZip2" {...props} />;
};

export default WinZip2;
