import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WinZip2003: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WinZip2003" {...props} />;
};

export default WinZip2003;
