import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WinZip3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WinZip3" {...props} />;
};

export default WinZip3;
