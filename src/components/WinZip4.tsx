import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WinZip4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WinZip4" {...props} />;
};

export default WinZip4;
