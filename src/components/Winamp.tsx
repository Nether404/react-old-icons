import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Winamp: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Winamp" {...props} />;
};

export default Winamp;
