import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Winamp1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Winamp1" {...props} />;
};

export default Winamp1;
