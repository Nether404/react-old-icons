import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Winamp2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Winamp2" {...props} />;
};

export default Winamp2;
