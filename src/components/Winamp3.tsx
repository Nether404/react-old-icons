import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Winamp3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Winamp3" {...props} />;
};

export default Winamp3;
