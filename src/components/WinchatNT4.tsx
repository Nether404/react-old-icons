import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WinchatNT4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WinchatNT4" {...props} />;
};

export default WinchatNT4;
