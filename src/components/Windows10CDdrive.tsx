import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows10CDdrive: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows10CDdrive" {...props} />;
};

export default Windows10CDdrive;
