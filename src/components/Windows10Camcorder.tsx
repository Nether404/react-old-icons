import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows10Camcorder: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows10Camcorder" {...props} />;
};

export default Windows10Camcorder;
