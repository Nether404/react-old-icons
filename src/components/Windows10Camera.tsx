import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows10Camera: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows10Camera" {...props} />;
};

export default Windows10Camera;
