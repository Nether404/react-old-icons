import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows10Chip: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows10Chip" {...props} />;
};

export default Windows10Chip;
