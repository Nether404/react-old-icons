import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows10HDDVDmovie: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows10HDDVDmovie" {...props} />;
};

export default Windows10HDDVDmovie;
