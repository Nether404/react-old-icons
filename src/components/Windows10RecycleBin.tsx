import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows10RecycleBin: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows10RecycleBin" {...props} />;
};

export default Windows10RecycleBin;
