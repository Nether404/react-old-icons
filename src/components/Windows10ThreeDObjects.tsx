import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows10ThreeDObjects: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows10ThreeDObjects" {...props} />;
};

export default Windows10ThreeDObjects;
