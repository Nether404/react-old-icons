import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows10ZipDrive: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows10ZipDrive" {...props} />;
};

export default Windows10ZipDrive;
