import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows2" {...props} />;
};

export default Windows2;
