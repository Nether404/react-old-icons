import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows2000EventViewer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows2000EventViewer" {...props} />;
};

export default Windows2000EventViewer;
