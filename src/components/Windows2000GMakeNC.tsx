import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows2000GMakeNC: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows2000GMakeNC" {...props} />;
};

export default Windows2000GMakeNC;
