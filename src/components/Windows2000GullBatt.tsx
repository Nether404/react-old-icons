import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows2000GullBatt: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows2000GullBatt" {...props} />;
};

export default Windows2000GullBatt;
