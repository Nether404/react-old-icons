import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows2000GuruRecycleBin: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows2000GuruRecycleBin" {...props} />;
};

export default Windows2000GuruRecycleBin;
