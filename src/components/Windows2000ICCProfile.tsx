import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows2000ICCProfile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows2000ICCProfile" {...props} />;
};

export default Windows2000ICCProfile;
