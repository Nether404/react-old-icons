import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows2000Icm32: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows2000Icm32" {...props} />;
};

export default Windows2000Icm32;
