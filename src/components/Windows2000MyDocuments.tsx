import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows2000MyDocuments: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows2000MyDocuments" {...props} />;
};

export default Windows2000MyDocuments;
