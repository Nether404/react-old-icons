import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows2000MyNetworkPlaces: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows2000MyNetworkPlaces" {...props} />;
};

export default Windows2000MyNetworkPlaces;
