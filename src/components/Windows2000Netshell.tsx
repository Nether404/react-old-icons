import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows2000Netshell: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows2000Netshell" {...props} />;
};

export default Windows2000Netshell;
