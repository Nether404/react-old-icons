import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows2000Sysdm: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows2000Sysdm" {...props} />;
};

export default Windows2000Sysdm;
