import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows2000SysdmIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows2000SysdmIcon" {...props} />;
};

export default Windows2000SysdmIcon;
