import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows2000SysdmIcon2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows2000SysdmIcon2" {...props} />;
};

export default Windows2000SysdmIcon2;
