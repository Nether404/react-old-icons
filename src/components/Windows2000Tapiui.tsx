import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows2000Tapiui: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows2000Tapiui" {...props} />;
};

export default Windows2000Tapiui;
