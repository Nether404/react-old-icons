import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows31Colors: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows31Colors" {...props} />;
};

export default Windows31Colors;
