import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows31ControlPanel: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows31ControlPanel" {...props} />;
};

export default Windows31ControlPanel;
