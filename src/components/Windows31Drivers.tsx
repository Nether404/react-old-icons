import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows31Drivers: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows31Drivers" {...props} />;
};

export default Windows31Drivers;
