import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows31Fonts: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows31Fonts" {...props} />;
};

export default Windows31Fonts;
