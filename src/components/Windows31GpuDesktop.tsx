import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows31GpuDesktop: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows31GpuDesktop" {...props} />;
};

export default Windows31GpuDesktop;
