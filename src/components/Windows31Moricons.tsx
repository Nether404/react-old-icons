import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows31Moricons: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows31Moricons" {...props} />;
};

export default Windows31Moricons;
