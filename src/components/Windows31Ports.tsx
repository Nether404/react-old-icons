import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows31Ports: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows31Ports" {...props} />;
};

export default Windows31Ports;
