import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows31Progman: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows31Progman" {...props} />;
};

export default Windows31Progman;
