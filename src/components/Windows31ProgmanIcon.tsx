import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows31ProgmanIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows31ProgmanIcon" {...props} />;
};

export default Windows31ProgmanIcon;
