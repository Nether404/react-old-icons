import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows31ProgramManager: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows31ProgramManager" {...props} />;
};

export default Windows31ProgramManager;
