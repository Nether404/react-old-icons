import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows31Sound: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows31Sound" {...props} />;
};

export default Windows31Sound;
