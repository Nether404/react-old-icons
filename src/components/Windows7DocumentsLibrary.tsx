import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows7DocumentsLibrary: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows7DocumentsLibrary" {...props} />;
};

export default Windows7DocumentsLibrary;
