import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows7GamesExplorer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows7GamesExplorer" {...props} />;
};

export default Windows7GamesExplorer;
