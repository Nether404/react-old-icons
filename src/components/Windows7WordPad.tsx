import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows7WordPad: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows7WordPad" {...props} />;
};

export default Windows7WordPad;
