import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Access2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Access2" {...props} />;
};

export default Windows95Access2;
