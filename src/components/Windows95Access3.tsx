import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Access3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Access3" {...props} />;
};

export default Windows95Access3;
