import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Access4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Access4" {...props} />;
};

export default Windows95Access4;
