import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Access5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Access5" {...props} />;
};

export default Windows95Access5;
