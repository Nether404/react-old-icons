import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Access6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Access6" {...props} />;
};

export default Windows95Access6;
