import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Access7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Access7" {...props} />;
};

export default Windows95Access7;
