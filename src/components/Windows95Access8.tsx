import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Access8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Access8" {...props} />;
};

export default Windows95Access8;
