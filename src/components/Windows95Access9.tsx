import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Access9: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Access9" {...props} />;
};

export default Windows95Access9;
