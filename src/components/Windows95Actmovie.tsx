import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Actmovie: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Actmovie" {...props} />;
};

export default Windows95Actmovie;
