import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95AddPrinter: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95AddPrinter" {...props} />;
};

export default Windows95AddPrinter;
