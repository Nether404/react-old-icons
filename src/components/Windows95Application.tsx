import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Application: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Application" {...props} />;
};

export default Windows95Application;
