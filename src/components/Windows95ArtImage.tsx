import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95ArtImage: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95ArtImage" {...props} />;
};

export default Windows95ArtImage;
