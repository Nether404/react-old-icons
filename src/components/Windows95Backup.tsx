import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Backup: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Backup" {...props} />;
};

export default Windows95Backup;
