import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95BatchFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95BatchFile" {...props} />;
};

export default Windows95BatchFile;
