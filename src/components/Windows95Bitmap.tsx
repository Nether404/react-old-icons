import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Bitmap: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Bitmap" {...props} />;
};

export default Windows95Bitmap;
