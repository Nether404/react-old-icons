import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95BlankScreen: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95BlankScreen" {...props} />;
};

export default Windows95BlankScreen;
