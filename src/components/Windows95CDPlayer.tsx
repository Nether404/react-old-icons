import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95CDPlayer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95CDPlayer" {...props} />;
};

export default Windows95CDPlayer;
