import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Calculator: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Calculator" {...props} />;
};

export default Windows95Calculator;
