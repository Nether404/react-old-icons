import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Comctl322: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Comctl322" {...props} />;
};

export default Windows95Comctl322;
