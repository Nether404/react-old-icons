import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95CompressionAgent: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95CompressionAgent" {...props} />;
};

export default Windows95CompressionAgent;
