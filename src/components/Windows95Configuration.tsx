import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Configuration: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Configuration" {...props} />;
};

export default Windows95Configuration;
