import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Cool: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Cool" {...props} />;
};

export default Windows95Cool;
