import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95DiskDefragmenter: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95DiskDefragmenter" {...props} />;
};

export default Windows95DiskDefragmenter;
