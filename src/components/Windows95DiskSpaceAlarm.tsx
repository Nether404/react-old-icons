import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95DiskSpaceAlarm: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95DiskSpaceAlarm" {...props} />;
};

export default Windows95DiskSpaceAlarm;
