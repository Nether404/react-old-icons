import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Executable: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Executable" {...props} />;
};

export default Windows95Executable;
