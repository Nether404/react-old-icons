import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Explorer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Explorer" {...props} />;
};

export default Windows95Explorer;
