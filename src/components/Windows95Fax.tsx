import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Fax: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Fax" {...props} />;
};

export default Windows95Fax;
