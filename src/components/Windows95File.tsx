import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95File: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95File" {...props} />;
};

export default Windows95File;
