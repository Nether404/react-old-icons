import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95FlyingWindows: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95FlyingWindows" {...props} />;
};

export default Windows95FlyingWindows;
