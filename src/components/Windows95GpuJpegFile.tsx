import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95GpuJpegFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95GpuJpegFile" {...props} />;
};

export default Windows95GpuJpegFile;
