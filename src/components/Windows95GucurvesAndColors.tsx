import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95GucurvesAndColors: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95GucurvesAndColors" {...props} />;
};

export default Windows95GucurvesAndColors;
