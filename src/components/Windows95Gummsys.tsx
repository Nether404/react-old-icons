import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Gummsys: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Gummsys" {...props} />;
};

export default Windows95Gummsys;
