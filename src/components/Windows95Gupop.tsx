import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Gupop: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Gupop" {...props} />;
};

export default Windows95Gupop;
