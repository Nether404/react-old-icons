import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Guprinter: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Guprinter" {...props} />;
};

export default Windows95Guprinter;
