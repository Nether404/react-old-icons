import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95GurichTextFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95GurichTextFile" {...props} />;
};

export default Windows95GurichTextFile;
