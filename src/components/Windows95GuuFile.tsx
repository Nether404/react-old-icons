import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95GuuFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95GuuFile" {...props} />;
};

export default Windows95GuuFile;
