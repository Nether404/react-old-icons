import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Hticons: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Hticons" {...props} />;
};

export default Windows95Hticons;
