import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Hticons10: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Hticons10" {...props} />;
};

export default Windows95Hticons10;
