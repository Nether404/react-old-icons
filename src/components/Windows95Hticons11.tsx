import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Hticons11: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Hticons11" {...props} />;
};

export default Windows95Hticons11;
