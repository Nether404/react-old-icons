import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Hticons2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Hticons2" {...props} />;
};

export default Windows95Hticons2;
