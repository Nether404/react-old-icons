import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Hticons3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Hticons3" {...props} />;
};

export default Windows95Hticons3;
