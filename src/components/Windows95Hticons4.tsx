import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Hticons4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Hticons4" {...props} />;
};

export default Windows95Hticons4;
