import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Hticons5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Hticons5" {...props} />;
};

export default Windows95Hticons5;
