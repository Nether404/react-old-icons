import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Hticons6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Hticons6" {...props} />;
};

export default Windows95Hticons6;
