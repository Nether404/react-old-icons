import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Hticons8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Hticons8" {...props} />;
};

export default Windows95Hticons8;
