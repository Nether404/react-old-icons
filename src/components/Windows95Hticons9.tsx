import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Hticons9: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Hticons9" {...props} />;
};

export default Windows95Hticons9;
