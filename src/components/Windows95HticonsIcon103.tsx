import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95HticonsIcon103: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95HticonsIcon103" {...props} />;
};

export default Windows95HticonsIcon103;
