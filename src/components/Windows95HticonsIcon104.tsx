import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95HticonsIcon104: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95HticonsIcon104" {...props} />;
};

export default Windows95HticonsIcon104;
