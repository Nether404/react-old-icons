import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Icons: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Icons" {...props} />;
};

export default Windows95Icons;
