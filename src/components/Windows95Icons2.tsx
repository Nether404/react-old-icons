import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Icons2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Icons2" {...props} />;
};

export default Windows95Icons2;
