import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Imaging: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Imaging" {...props} />;
};

export default Windows95Imaging;
