import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Inbox: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Inbox" {...props} />;
};

export default Windows95Inbox;
