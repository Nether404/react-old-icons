import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95InetcplIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95InetcplIcon" {...props} />;
};

export default Windows95InetcplIcon;
