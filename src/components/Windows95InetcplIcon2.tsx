import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95InetcplIcon2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95InetcplIcon2" {...props} />;
};

export default Windows95InetcplIcon2;
