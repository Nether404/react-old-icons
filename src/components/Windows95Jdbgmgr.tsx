import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Jdbgmgr: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Jdbgmgr" {...props} />;
};

export default Windows95Jdbgmgr;
