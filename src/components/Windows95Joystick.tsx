import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Joystick: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Joystick" {...props} />;
};

export default Windows95Joystick;
