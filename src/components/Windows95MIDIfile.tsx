import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95MIDIfile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95MIDIfile" {...props} />;
};

export default Windows95MIDIfile;
