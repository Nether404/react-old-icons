import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Main: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Main" {...props} />;
};

export default Windows95Main;
