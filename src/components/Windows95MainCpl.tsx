import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95MainCpl: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95MainCpl" {...props} />;
};

export default Windows95MainCpl;
