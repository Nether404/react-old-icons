import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95MainCpl2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95MainCpl2" {...props} />;
};

export default Windows95MainCpl2;
