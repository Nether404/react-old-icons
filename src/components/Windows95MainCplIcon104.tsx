import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95MainCplIcon104: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95MainCplIcon104" {...props} />;
};

export default Windows95MainCplIcon104;
