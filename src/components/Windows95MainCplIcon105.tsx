import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95MainCplIcon105: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95MainCplIcon105" {...props} />;
};

export default Windows95MainCplIcon105;
