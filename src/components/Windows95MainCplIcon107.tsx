import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95MainCplIcon107: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95MainCplIcon107" {...props} />;
};

export default Windows95MainCplIcon107;
