import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Minesweeper: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Minesweeper" {...props} />;
};

export default Windows95Minesweeper;
