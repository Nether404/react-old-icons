import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95MmsysIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95MmsysIcon" {...props} />;
};

export default Windows95MmsysIcon;
