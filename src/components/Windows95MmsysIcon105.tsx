import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95MmsysIcon105: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95MmsysIcon105" {...props} />;
};

export default Windows95MmsysIcon105;
