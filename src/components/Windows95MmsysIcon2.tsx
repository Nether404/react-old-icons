import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95MmsysIcon2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95MmsysIcon2" {...props} />;
};

export default Windows95MmsysIcon2;
