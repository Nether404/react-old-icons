import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95MmsysIcon4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95MmsysIcon4" {...props} />;
};

export default Windows95MmsysIcon4;
