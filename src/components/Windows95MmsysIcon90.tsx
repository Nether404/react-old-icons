import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95MmsysIcon90: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95MmsysIcon90" {...props} />;
};

export default Windows95MmsysIcon90;
