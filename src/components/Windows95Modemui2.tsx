import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Modemui2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Modemui2" {...props} />;
};

export default Windows95Modemui2;
