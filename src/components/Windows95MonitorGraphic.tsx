import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95MonitorGraphic: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95MonitorGraphic" {...props} />;
};

export default Windows95MonitorGraphic;
