import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Mspaint: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Mspaint" {...props} />;
};

export default Windows95Mspaint;
