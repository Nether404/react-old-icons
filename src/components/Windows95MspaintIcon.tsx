import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95MspaintIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95MspaintIcon" {...props} />;
};

export default Windows95MspaintIcon;
