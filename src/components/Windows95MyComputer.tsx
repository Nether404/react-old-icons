import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95MyComputer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95MyComputer" {...props} />;
};

export default Windows95MyComputer;
