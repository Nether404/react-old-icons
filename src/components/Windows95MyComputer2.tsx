import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95MyComputer2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95MyComputer2" {...props} />;
};

export default Windows95MyComputer2;
