import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95MystifyYourMind: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95MystifyYourMind" {...props} />;
};

export default Windows95MystifyYourMind;
