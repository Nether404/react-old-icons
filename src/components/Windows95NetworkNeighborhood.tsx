import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95NetworkNeighborhood: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95NetworkNeighborhood" {...props} />;
};

export default Windows95NetworkNeighborhood;
