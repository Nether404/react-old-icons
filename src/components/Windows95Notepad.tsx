import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Notepad: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Notepad" {...props} />;
};

export default Windows95Notepad;
