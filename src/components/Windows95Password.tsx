import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Password: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Password" {...props} />;
};

export default Windows95Password;
