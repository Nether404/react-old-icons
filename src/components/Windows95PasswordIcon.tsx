import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95PasswordIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95PasswordIcon" {...props} />;
};

export default Windows95PasswordIcon;
