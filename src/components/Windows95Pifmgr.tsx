import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Pifmgr: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Pifmgr" {...props} />;
};

export default Windows95Pifmgr;
