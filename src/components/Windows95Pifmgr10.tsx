import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Pifmgr10: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Pifmgr10" {...props} />;
};

export default Windows95Pifmgr10;
