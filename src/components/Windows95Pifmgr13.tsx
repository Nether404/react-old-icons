import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Pifmgr13: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Pifmgr13" {...props} />;
};

export default Windows95Pifmgr13;
