import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Pifmgr14: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Pifmgr14" {...props} />;
};

export default Windows95Pifmgr14;
