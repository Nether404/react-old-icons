import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Pifmgr15: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Pifmgr15" {...props} />;
};

export default Windows95Pifmgr15;
