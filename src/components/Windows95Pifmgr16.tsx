import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Pifmgr16: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Pifmgr16" {...props} />;
};

export default Windows95Pifmgr16;
