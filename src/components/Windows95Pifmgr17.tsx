import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Pifmgr17: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Pifmgr17" {...props} />;
};

export default Windows95Pifmgr17;
