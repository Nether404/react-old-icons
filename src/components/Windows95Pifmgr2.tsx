import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Pifmgr2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Pifmgr2" {...props} />;
};

export default Windows95Pifmgr2;
