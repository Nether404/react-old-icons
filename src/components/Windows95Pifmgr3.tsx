import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Pifmgr3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Pifmgr3" {...props} />;
};

export default Windows95Pifmgr3;
