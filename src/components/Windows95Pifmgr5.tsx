import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Pifmgr5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Pifmgr5" {...props} />;
};

export default Windows95Pifmgr5;
