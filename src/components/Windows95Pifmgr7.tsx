import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Pifmgr7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Pifmgr7" {...props} />;
};

export default Windows95Pifmgr7;
