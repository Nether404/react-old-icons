import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Pifmgr8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Pifmgr8" {...props} />;
};

export default Windows95Pifmgr8;
