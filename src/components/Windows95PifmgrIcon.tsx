import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95PifmgrIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95PifmgrIcon" {...props} />;
};

export default Windows95PifmgrIcon;
