import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95PlusMyComputer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95PlusMyComputer" {...props} />;
};

export default Windows95PlusMyComputer;
