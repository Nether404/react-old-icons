import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Printer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Printer" {...props} />;
};

export default Windows95Printer;
