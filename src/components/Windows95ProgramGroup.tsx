import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95ProgramGroup: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95ProgramGroup" {...props} />;
};

export default Windows95ProgramGroup;
