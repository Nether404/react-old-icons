import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95RealAudioFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95RealAudioFile" {...props} />;
};

export default Windows95RealAudioFile;
