import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95RecycleBin: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95RecycleBin" {...props} />;
};

export default Windows95RecycleBin;
