import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95RecycleBin2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95RecycleBin2" {...props} />;
};

export default Windows95RecycleBin2;
