import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95RecycleBin3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95RecycleBin3" {...props} />;
};

export default Windows95RecycleBin3;
