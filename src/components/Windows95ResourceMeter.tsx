import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95ResourceMeter: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95ResourceMeter" {...props} />;
};

export default Windows95ResourceMeter;
