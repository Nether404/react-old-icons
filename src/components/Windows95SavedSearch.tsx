import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95SavedSearch: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95SavedSearch" {...props} />;
};

export default Windows95SavedSearch;
