import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95ScanDisk: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95ScanDisk" {...props} />;
};

export default Windows95ScanDisk;
