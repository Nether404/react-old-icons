import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95ScanDisk2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95ScanDisk2" {...props} />;
};

export default Windows95ScanDisk2;
