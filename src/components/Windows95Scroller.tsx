import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Scroller: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Scroller" {...props} />;
};

export default Windows95Scroller;
