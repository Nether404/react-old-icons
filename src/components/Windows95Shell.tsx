import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Shell: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Shell" {...props} />;
};

export default Windows95Shell;
