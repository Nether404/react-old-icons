import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Shell14: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Shell14" {...props} />;
};

export default Windows95Shell14;
