import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Shell32: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Shell32" {...props} />;
};

export default Windows95Shell32;
