import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Shell322: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Shell322" {...props} />;
};

export default Windows95Shell322;
