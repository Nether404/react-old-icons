import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Shell41: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Shell41" {...props} />;
};

export default Windows95Shell41;
