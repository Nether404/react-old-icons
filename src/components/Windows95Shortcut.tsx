import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Shortcut: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Shortcut" {...props} />;
};

export default Windows95Shortcut;
