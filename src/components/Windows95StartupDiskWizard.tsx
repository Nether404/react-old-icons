import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95StartupDiskWizard: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95StartupDiskWizard" {...props} />;
};

export default Windows95StartupDiskWizard;
