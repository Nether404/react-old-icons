import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95StickyKeys: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95StickyKeys" {...props} />;
};

export default Windows95StickyKeys;
