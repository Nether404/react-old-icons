import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Syncui: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Syncui" {...props} />;
};

export default Windows95Syncui;
