import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95SysdmIcon009: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95SysdmIcon009" {...props} />;
};

export default Windows95SysdmIcon009;
