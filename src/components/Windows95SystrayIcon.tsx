import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95SystrayIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95SystrayIcon" {...props} />;
};

export default Windows95SystrayIcon;
