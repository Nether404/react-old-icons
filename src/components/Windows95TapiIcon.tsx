import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95TapiIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95TapiIcon" {...props} />;
};

export default Windows95TapiIcon;
