import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95TextFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95TextFile" {...props} />;
};

export default Windows95TextFile;
