import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95TheInternet: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95TheInternet" {...props} />;
};

export default Windows95TheInternet;
