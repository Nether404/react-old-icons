import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Tree: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Tree" {...props} />;
};

export default Windows95Tree;
