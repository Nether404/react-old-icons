import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95Tree2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95Tree2" {...props} />;
};

export default Windows95Tree2;
