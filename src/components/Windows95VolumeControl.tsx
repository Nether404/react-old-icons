import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95VolumeControl: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95VolumeControl" {...props} />;
};

export default Windows95VolumeControl;
