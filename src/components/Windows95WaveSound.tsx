import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95WaveSound: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95WaveSound" {...props} />;
};

export default Windows95WaveSound;
