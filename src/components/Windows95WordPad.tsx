import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95WordPad: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95WordPad" {...props} />;
};

export default Windows95WordPad;
