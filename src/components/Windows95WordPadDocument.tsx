import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows95WordPadDocument: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows95WordPadDocument" {...props} />;
};

export default Windows95WordPadDocument;
