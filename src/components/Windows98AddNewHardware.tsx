import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows98AddNewHardware: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows98AddNewHardware" {...props} />;
};

export default Windows98AddNewHardware;
