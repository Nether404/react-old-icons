import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows98AiffFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows98AiffFile" {...props} />;
};

export default Windows98AiffFile;
