import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows98Appwiz: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows98Appwiz" {...props} />;
};

export default Windows98Appwiz;
