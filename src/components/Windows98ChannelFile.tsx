import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows98ChannelFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows98ChannelFile" {...props} />;
};

export default Windows98ChannelFile;
