import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows98ComicChat: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows98ComicChat" {...props} />;
};

export default Windows98ComicChat;
