import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows98DateTime: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows98DateTime" {...props} />;
};

export default Windows98DateTime;
