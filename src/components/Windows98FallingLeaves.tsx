import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows98FallingLeaves: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows98FallingLeaves" {...props} />;
};

export default Windows98FallingLeaves;
