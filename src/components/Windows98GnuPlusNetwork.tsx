import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows98GnuPlusNetwork: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows98GnuPlusNetwork" {...props} />;
};

export default Windows98GnuPlusNetwork;
