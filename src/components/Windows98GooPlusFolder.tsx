import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows98GooPlusFolder: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows98GooPlusFolder" {...props} />;
};

export default Windows98GooPlusFolder;
