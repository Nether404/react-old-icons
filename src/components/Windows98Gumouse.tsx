import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows98Gumouse: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows98Gumouse" {...props} />;
};

export default Windows98Gumouse;
