import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows98Imaging: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows98Imaging" {...props} />;
};

export default Windows98Imaging;
