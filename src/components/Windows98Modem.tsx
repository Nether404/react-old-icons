import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows98Modem: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows98Modem" {...props} />;
};

export default Windows98Modem;
