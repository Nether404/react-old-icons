import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows98MyDocuments: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows98MyDocuments" {...props} />;
};

export default Windows98MyDocuments;
