import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows98MyDocuments2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows98MyDocuments2" {...props} />;
};

export default Windows98MyDocuments2;
