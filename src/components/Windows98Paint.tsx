import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows98Paint: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows98Paint" {...props} />;
};

export default Windows98Paint;
