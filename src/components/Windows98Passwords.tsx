import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows98Passwords: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows98Passwords" {...props} />;
};

export default Windows98Passwords;
