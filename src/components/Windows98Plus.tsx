import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows98Plus: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows98Plus" {...props} />;
};

export default Windows98Plus;
