import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows98Plus2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows98Plus2" {...props} />;
};

export default Windows98Plus2;
