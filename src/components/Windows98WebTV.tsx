import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows98WebTV: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows98WebTV" {...props} />;
};

export default Windows98WebTV;
