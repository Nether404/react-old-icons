import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Windows98WriteDocument: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Windows98WriteDocument" {...props} />;
};

export default Windows98WriteDocument;
