import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsAccessibilityOptions: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsAccessibilityOptions" {...props} />;
};

export default WindowsAccessibilityOptions;
