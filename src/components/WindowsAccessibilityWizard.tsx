import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsAccessibilityWizard: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsAccessibilityWizard" {...props} />;
};

export default WindowsAccessibilityWizard;
