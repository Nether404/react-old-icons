import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsActionCenter: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsActionCenter" {...props} />;
};

export default WindowsActionCenter;
