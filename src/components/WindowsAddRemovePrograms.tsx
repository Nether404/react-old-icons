import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsAddRemovePrograms: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsAddRemovePrograms" {...props} />;
};

export default WindowsAddRemovePrograms;
