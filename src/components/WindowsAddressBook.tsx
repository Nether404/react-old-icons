import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsAddressBook: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsAddressBook" {...props} />;
};

export default WindowsAddressBook;
