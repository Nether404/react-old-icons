import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsAdministrative: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsAdministrative" {...props} />;
};

export default WindowsAdministrative;
