import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsBackup: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsBackup" {...props} />;
};

export default WindowsBackup;
