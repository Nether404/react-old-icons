import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsBitmapFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsBitmapFile" {...props} />;
};

export default WindowsBitmapFile;
