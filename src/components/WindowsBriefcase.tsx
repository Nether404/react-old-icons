import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsBriefcase: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsBriefcase" {...props} />;
};

export default WindowsBriefcase;
