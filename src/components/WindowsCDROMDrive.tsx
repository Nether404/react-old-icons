import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsCDROMDrive: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsCDROMDrive" {...props} />;
};

export default WindowsCDROMDrive;
