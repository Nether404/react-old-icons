import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsCDROMDrive2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsCDROMDrive2" {...props} />;
};

export default WindowsCDROMDrive2;
