import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsCalculator: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsCalculator" {...props} />;
};

export default WindowsCalculator;
