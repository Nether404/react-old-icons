import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsCardfile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsCardfile" {...props} />;
};

export default WindowsCardfile;
