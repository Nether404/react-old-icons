import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsClipboard: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsClipboard" {...props} />;
};

export default WindowsClipboard;
