import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsClipbook: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsClipbook" {...props} />;
};

export default WindowsClipbook;
