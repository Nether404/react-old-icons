import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsCmdl32: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsCmdl32" {...props} />;
};

export default WindowsCmdl32;
