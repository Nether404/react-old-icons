import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsCmmgr32: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsCmmgr32" {...props} />;
};

export default WindowsCmmgr32;
