import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsColorManagement: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsColorManagement" {...props} />;
};

export default WindowsColorManagement;
