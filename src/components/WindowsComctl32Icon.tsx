import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsComctl32Icon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsComctl32Icon" {...props} />;
};

export default WindowsComctl32Icon;
