import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsConfigurationFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsConfigurationFile" {...props} />;
};

export default WindowsConfigurationFile;
