import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsConfigure: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsConfigure" {...props} />;
};

export default WindowsConfigure;
