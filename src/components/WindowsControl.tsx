import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsControl: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsControl" {...props} />;
};

export default WindowsControl;
