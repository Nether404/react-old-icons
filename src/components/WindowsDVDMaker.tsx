import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsDVDMaker: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsDVDMaker" {...props} />;
};

export default WindowsDVDMaker;
