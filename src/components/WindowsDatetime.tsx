import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsDatetime: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsDatetime" {...props} />;
};

export default WindowsDatetime;
