import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsDefender: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsDefender" {...props} />;
};

export default WindowsDefender;
