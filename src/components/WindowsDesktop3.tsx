import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsDesktop3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsDesktop3" {...props} />;
};

export default WindowsDesktop3;
