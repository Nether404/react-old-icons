import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsDesktopControlPanel: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsDesktopControlPanel" {...props} />;
};

export default WindowsDesktopControlPanel;
