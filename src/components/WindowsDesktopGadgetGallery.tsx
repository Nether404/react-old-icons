import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsDesktopGadgetGallery: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsDesktopGadgetGallery" {...props} />;
};

export default WindowsDesktopGadgetGallery;
