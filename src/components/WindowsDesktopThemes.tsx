import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsDesktopThemes: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsDesktopThemes" {...props} />;
};

export default WindowsDesktopThemes;
