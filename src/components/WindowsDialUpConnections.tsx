import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsDialUpConnections: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsDialUpConnections" {...props} />;
};

export default WindowsDialUpConnections;
