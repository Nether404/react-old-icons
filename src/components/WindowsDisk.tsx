import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsDisk: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsDisk" {...props} />;
};

export default WindowsDisk;
