import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsDos: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsDos" {...props} />;
};

export default WindowsDos;
