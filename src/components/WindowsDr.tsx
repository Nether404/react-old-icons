import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsDr: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsDr" {...props} />;
};

export default WindowsDr;
