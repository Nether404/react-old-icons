import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsDriveConverterFAT32: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsDriveConverterFAT32" {...props} />;
};

export default WindowsDriveConverterFAT32;
