import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsDriveSpace: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsDriveSpace" {...props} />;
};

export default WindowsDriveSpace;
