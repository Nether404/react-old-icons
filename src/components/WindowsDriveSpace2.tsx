import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsDriveSpace2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsDriveSpace2" {...props} />;
};

export default WindowsDriveSpace2;
