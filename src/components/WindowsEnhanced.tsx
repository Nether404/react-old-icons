import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsEnhanced: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsEnhanced" {...props} />;
};

export default WindowsEnhanced;
