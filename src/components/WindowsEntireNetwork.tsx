import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsEntireNetwork: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsEntireNetwork" {...props} />;
};

export default WindowsEntireNetwork;
