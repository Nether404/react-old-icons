import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsExplorer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsExplorer" {...props} />;
};

export default WindowsExplorer;
