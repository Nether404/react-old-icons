import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsExplorer107: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsExplorer107" {...props} />;
};

export default WindowsExplorer107;
