import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsExplorer2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsExplorer2" {...props} />;
};

export default WindowsExplorer2;
