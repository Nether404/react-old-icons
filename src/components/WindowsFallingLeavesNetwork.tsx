import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsFallingLeavesNetwork: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsFallingLeavesNetwork" {...props} />;
};

export default WindowsFallingLeavesNetwork;
