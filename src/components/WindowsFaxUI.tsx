import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsFaxUI: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsFaxUI" {...props} />;
};

export default WindowsFaxUI;
