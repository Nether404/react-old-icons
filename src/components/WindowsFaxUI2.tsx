import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsFaxUI2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsFaxUI2" {...props} />;
};

export default WindowsFaxUI2;
