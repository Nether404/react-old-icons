import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsFile" {...props} />;
};

export default WindowsFile;
