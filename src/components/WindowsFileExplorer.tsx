import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsFileExplorer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsFileExplorer" {...props} />;
};

export default WindowsFileExplorer;
