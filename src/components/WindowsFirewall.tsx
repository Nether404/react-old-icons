import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsFirewall: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsFirewall" {...props} />;
};

export default WindowsFirewall;
