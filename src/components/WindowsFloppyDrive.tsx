import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsFloppyDrive: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsFloppyDrive" {...props} />;
};

export default WindowsFloppyDrive;
