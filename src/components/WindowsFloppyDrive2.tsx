import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsFloppyDrive2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsFloppyDrive2" {...props} />;
};

export default WindowsFloppyDrive2;
