import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsFloppyDrive3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsFloppyDrive3" {...props} />;
};

export default WindowsFloppyDrive3;
