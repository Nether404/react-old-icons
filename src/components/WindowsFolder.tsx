import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsFolder: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsFolder" {...props} />;
};

export default WindowsFolder;
