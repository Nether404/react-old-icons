import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsFolder16x16: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsFolder16x16" {...props} />;
};

export default WindowsFolder16x16;
