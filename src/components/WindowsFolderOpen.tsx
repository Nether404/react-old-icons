import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsFolderOpen: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsFolderOpen" {...props} />;
};

export default WindowsFolderOpen;
