import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsFonts: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsFonts" {...props} />;
};

export default WindowsFonts;
