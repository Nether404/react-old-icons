import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsFrontPageExpress: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsFrontPageExpress" {...props} />;
};

export default WindowsFrontPageExpress;
