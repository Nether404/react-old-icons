import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsGamingOptions: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsGamingOptions" {...props} />;
};

export default WindowsGamingOptions;
