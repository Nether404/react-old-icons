import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsGroup: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsGroup" {...props} />;
};

export default WindowsGroup;
