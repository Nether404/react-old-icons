import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsGroup1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsGroup1" {...props} />;
};

export default WindowsGroup1;
