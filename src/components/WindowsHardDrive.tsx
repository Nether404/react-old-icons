import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsHardDrive: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsHardDrive" {...props} />;
};

export default WindowsHardDrive;
