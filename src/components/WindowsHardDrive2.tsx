import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsHardDrive2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsHardDrive2" {...props} />;
};

export default WindowsHardDrive2;
