import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsHearts: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsHearts" {...props} />;
};

export default WindowsHearts;
