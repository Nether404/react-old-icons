import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsHibernate: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsHibernate" {...props} />;
};

export default WindowsHibernate;
