import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsHomeGroup: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsHomeGroup" {...props} />;
};

export default WindowsHomeGroup;
