import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsIcon1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsIcon1" {...props} />;
};

export default WindowsIcon1;
