import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsInstaller: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsInstaller" {...props} />;
};

export default WindowsInstaller;
