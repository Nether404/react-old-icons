import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsInternational: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsInternational" {...props} />;
};

export default WindowsInternational;
