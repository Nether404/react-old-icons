import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsInternet: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsInternet" {...props} />;
};

export default WindowsInternet;
