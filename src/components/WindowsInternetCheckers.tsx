import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsInternetCheckers: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsInternetCheckers" {...props} />;
};

export default WindowsInternetCheckers;
