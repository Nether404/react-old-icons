import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsIrprops: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsIrprops" {...props} />;
};

export default WindowsIrprops;
