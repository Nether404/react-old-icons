import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsJazDrive: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsJazDrive" {...props} />;
};

export default WindowsJazDrive;
