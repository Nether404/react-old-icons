import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsKeyboard: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsKeyboard" {...props} />;
};

export default WindowsKeyboard;
