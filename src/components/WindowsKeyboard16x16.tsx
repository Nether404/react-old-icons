import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsKeyboard16x16: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsKeyboard16x16" {...props} />;
};

export default WindowsKeyboard16x16;
