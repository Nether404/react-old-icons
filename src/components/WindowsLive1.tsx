import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsLive1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsLive1" {...props} />;
};

export default WindowsLive1;
