import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsLive2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsLive2" {...props} />;
};

export default WindowsLive2;
