import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsLive3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsLive3" {...props} />;
};

export default WindowsLive3;
