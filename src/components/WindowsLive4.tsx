import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsLive4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsLive4" {...props} />;
};

export default WindowsLive4;
