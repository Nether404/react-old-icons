import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsLiveMail: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsLiveMail" {...props} />;
};

export default WindowsLiveMail;
