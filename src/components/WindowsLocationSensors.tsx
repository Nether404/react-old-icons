import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsLocationSensors: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsLocationSensors" {...props} />;
};

export default WindowsLocationSensors;
