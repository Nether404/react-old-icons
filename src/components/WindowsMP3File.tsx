import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMP3File: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMP3File" {...props} />;
};

export default WindowsMP3File;
