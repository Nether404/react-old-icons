import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMSN: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMSN" {...props} />;
};

export default WindowsMSN;
