import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMSN2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMSN2" {...props} />;
};

export default WindowsMSN2;
