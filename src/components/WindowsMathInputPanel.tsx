import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMathInputPanel: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMathInputPanel" {...props} />;
};

export default WindowsMathInputPanel;
