import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMe2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMe2" {...props} />;
};

export default WindowsMe2;
