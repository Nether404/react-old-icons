import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMe5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMe5" {...props} />;
};

export default WindowsMe5;
