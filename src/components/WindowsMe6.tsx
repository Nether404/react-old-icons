import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMe6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMe6" {...props} />;
};

export default WindowsMe6;
