import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMeGPlus107: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMeGPlus107" {...props} />;
};

export default WindowsMeGPlus107;
