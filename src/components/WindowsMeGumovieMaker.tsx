import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMeGumovieMaker: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMeGumovieMaker" {...props} />;
};

export default WindowsMeGumovieMaker;
