import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMeGusell32: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMeGusell32" {...props} />;
};

export default WindowsMeGusell32;
