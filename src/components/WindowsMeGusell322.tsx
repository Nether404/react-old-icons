import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMeGusell322: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMeGusell322" {...props} />;
};

export default WindowsMeGusell322;
