import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMeGusell323: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMeGusell323" {...props} />;
};

export default WindowsMeGusell323;
