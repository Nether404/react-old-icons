import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMeGusell324: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMeGusell324" {...props} />;
};

export default WindowsMeGusell324;
