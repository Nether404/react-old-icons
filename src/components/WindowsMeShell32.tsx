import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMeShell32: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMeShell32" {...props} />;
};

export default WindowsMeShell32;
