import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMeShell322: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMeShell322" {...props} />;
};

export default WindowsMeShell322;
