import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMeShell323: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMeShell323" {...props} />;
};

export default WindowsMeShell323;
