import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMeShell32Icon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMeShell32Icon" {...props} />;
};

export default WindowsMeShell32Icon;
