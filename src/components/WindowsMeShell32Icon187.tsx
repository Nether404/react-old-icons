import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMeShell32Icon187: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMeShell32Icon187" {...props} />;
};

export default WindowsMeShell32Icon187;
