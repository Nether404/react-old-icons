import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMeSpiderSolitaire: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMeSpiderSolitaire" {...props} />;
};

export default WindowsMeSpiderSolitaire;
