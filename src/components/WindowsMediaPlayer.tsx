import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMediaPlayer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMediaPlayer" {...props} />;
};

export default WindowsMediaPlayer;
