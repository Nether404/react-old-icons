import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMediaPlayer2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMediaPlayer2" {...props} />;
};

export default WindowsMediaPlayer2;
