import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMediaPlayer22: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMediaPlayer22" {...props} />;
};

export default WindowsMediaPlayer22;
