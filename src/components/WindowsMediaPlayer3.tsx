import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMediaPlayer3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMediaPlayer3" {...props} />;
};

export default WindowsMediaPlayer3;
