import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMediaPlayer32: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMediaPlayer32" {...props} />;
};

export default WindowsMediaPlayer32;
