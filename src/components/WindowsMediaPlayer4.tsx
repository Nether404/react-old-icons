import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMediaPlayer4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMediaPlayer4" {...props} />;
};

export default WindowsMediaPlayer4;
