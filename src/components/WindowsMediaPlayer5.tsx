import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMediaPlayer5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMediaPlayer5" {...props} />;
};

export default WindowsMediaPlayer5;
