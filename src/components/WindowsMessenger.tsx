import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMessenger: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMessenger" {...props} />;
};

export default WindowsMessenger;
