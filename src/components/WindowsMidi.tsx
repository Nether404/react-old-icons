import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMidi: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMidi" {...props} />;
};

export default WindowsMidi;
