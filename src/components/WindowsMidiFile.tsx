import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMidiFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMidiFile" {...props} />;
};

export default WindowsMidiFile;
