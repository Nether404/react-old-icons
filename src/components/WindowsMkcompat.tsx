import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMkcompat: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMkcompat" {...props} />;
};

export default WindowsMkcompat;
