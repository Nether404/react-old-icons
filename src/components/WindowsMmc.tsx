import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMmc: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMmc" {...props} />;
};

export default WindowsMmc;
