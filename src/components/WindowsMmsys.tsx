import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMmsys: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMmsys" {...props} />;
};

export default WindowsMmsys;
