import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMouse: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMouse" {...props} />;
};

export default WindowsMouse;
