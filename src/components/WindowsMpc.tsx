import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMpc: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMpc" {...props} />;
};

export default WindowsMpc;
