import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMpc1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMpc1" {...props} />;
};

export default WindowsMpc1;
