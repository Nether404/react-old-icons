import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMpc2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMpc2" {...props} />;
};

export default WindowsMpc2;
