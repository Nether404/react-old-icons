import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMpc3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMpc3" {...props} />;
};

export default WindowsMpc3;
