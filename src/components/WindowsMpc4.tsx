import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMpc4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMpc4" {...props} />;
};

export default WindowsMpc4;
