import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMpc5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMpc5" {...props} />;
};

export default WindowsMpc5;
