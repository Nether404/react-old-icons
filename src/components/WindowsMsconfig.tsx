import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMsconfig: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMsconfig" {...props} />;
};

export default WindowsMsconfig;
