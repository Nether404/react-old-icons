import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMultimedia: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMultimedia" {...props} />;
};

export default WindowsMultimedia;
