import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMusic: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMusic" {...props} />;
};

export default WindowsMusic;
