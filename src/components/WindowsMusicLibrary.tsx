import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMusicLibrary: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMusicLibrary" {...props} />;
};

export default WindowsMusicLibrary;
