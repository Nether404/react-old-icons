import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMy: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMy" {...props} />;
};

export default WindowsMy;
