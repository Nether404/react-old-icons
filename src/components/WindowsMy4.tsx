import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMy4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMy4" {...props} />;
};

export default WindowsMy4;
