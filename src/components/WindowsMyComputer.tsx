import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMyComputer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMyComputer" {...props} />;
};

export default WindowsMyComputer;
