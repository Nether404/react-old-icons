import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsMyComputer2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsMyComputer2" {...props} />;
};

export default WindowsMyComputer2;
