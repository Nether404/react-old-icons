import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNT31Terminal: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNT31Terminal" {...props} />;
};

export default WindowsNT31Terminal;
