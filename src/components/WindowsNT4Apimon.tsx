import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNT4Apimon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNT4Apimon" {...props} />;
};

export default WindowsNT4Apimon;
