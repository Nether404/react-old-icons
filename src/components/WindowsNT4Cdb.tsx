import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNT4Cdb: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNT4Cdb" {...props} />;
};

export default WindowsNT4Cdb;
