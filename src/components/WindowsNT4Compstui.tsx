import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNT4Compstui: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNT4Compstui" {...props} />;
};

export default WindowsNT4Compstui;
