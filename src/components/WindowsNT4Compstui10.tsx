import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNT4Compstui10: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNT4Compstui10" {...props} />;
};

export default WindowsNT4Compstui10;
