import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNT4Cryptui: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNT4Cryptui" {...props} />;
};

export default WindowsNT4Cryptui;
