import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNT4Perfmon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNT4Perfmon" {...props} />;
};

export default WindowsNT4Perfmon;
