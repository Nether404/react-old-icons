import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNT4Pifmgr: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNT4Pifmgr" {...props} />;
};

export default WindowsNT4Pifmgr;
