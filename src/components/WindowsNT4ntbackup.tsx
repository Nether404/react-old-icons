import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNT4ntbackup: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNT4ntbackup" {...props} />;
};

export default WindowsNT4ntbackup;
