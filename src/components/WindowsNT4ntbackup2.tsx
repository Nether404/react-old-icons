import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNT4ntbackup2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNT4ntbackup2" {...props} />;
};

export default WindowsNT4ntbackup2;
