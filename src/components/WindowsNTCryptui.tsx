import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNTCryptui: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNTCryptui" {...props} />;
};

export default WindowsNTCryptui;
