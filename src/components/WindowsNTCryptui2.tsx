import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNTCryptui2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNTCryptui2" {...props} />;
};

export default WindowsNTCryptui2;
