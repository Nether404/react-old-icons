import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNarrator: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNarrator" {...props} />;
};

export default WindowsNarrator;
