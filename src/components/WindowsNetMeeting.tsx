import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNetMeeting: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNetMeeting" {...props} />;
};

export default WindowsNetMeeting;
