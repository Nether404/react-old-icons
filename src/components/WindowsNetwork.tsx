import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNetwork: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNetwork" {...props} />;
};

export default WindowsNetwork;
