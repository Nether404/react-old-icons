import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNetwork1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNetwork1" {...props} />;
};

export default WindowsNetwork1;
