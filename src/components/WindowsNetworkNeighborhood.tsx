import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNetworkNeighborhood: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNetworkNeighborhood" {...props} />;
};

export default WindowsNetworkNeighborhood;
