import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNetworkNeighborhood2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNetworkNeighborhood2" {...props} />;
};

export default WindowsNetworkNeighborhood2;
