import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNetworkPC: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNetworkPC" {...props} />;
};

export default WindowsNetworkPC;
