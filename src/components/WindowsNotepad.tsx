import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNotepad: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNotepad" {...props} />;
};

export default WindowsNotepad;
