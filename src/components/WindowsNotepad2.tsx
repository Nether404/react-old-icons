import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNotepad2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNotepad2" {...props} />;
};

export default WindowsNotepad2;
