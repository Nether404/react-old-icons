import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNotepad3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNotepad3" {...props} />;
};

export default WindowsNotepad3;
