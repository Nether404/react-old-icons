import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNotificationAreaIcons: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNotificationAreaIcons" {...props} />;
};

export default WindowsNotificationAreaIcons;
