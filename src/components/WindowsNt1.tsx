import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNt1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNt1" {...props} />;
};

export default WindowsNt1;
