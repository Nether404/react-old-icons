import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNt2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNt2" {...props} />;
};

export default WindowsNt2;
