import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNt24: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNt24" {...props} />;
};

export default WindowsNt24;
