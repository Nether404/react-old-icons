import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNt25: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNt25" {...props} />;
};

export default WindowsNt25;
