import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNt28: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNt28" {...props} />;
};

export default WindowsNt28;
