import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNt3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNt3" {...props} />;
};

export default WindowsNt3;
