import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNt37: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNt37" {...props} />;
};

export default WindowsNt37;
