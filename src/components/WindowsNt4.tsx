import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsNt4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsNt4" {...props} />;
};

export default WindowsNt4;
