import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsObject: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsObject" {...props} />;
};

export default WindowsObject;
