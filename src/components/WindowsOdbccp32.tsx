import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsOdbccp32: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsOdbccp32" {...props} />;
};

export default WindowsOdbccp32;
