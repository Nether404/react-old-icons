import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsOpentype: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsOpentype" {...props} />;
};

export default WindowsOpentype;
