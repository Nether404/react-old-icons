import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsOutlookExpress: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsOutlookExpress" {...props} />;
};

export default WindowsOutlookExpress;
