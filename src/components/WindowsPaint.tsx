import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsPaint: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsPaint" {...props} />;
};

export default WindowsPaint;
