import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsPaint2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsPaint2" {...props} />;
};

export default WindowsPaint2;
