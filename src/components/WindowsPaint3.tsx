import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsPaint3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsPaint3" {...props} />;
};

export default WindowsPaint3;
