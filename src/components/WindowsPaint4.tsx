import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsPaint4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsPaint4" {...props} />;
};

export default WindowsPaint4;
