import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsPaintbrush: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsPaintbrush" {...props} />;
};

export default WindowsPaintbrush;
