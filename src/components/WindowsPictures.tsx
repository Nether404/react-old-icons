import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsPictures: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsPictures" {...props} />;
};

export default WindowsPictures;
