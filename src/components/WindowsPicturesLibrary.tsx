import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsPicturesLibrary: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsPicturesLibrary" {...props} />;
};

export default WindowsPicturesLibrary;
