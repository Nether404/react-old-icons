import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsPif: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsPif" {...props} />;
};

export default WindowsPif;
