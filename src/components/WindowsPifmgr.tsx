import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsPifmgr: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsPifmgr" {...props} />;
};

export default WindowsPifmgr;
