import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsPifmgr10: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsPifmgr10" {...props} />;
};

export default WindowsPifmgr10;
