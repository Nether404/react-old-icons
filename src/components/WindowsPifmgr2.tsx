import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsPifmgr2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsPifmgr2" {...props} />;
};

export default WindowsPifmgr2;
