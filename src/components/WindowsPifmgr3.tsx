import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsPifmgr3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsPifmgr3" {...props} />;
};

export default WindowsPifmgr3;
