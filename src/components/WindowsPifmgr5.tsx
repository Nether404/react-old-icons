import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsPifmgr5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsPifmgr5" {...props} />;
};

export default WindowsPifmgr5;
