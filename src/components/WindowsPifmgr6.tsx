import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsPifmgr6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsPifmgr6" {...props} />;
};

export default WindowsPifmgr6;
