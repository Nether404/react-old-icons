import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsPifmgr7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsPifmgr7" {...props} />;
};

export default WindowsPifmgr7;
