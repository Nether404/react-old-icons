import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsPifmgr8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsPifmgr8" {...props} />;
};

export default WindowsPifmgr8;
