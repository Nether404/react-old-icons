import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsPifmgr9: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsPifmgr9" {...props} />;
};

export default WindowsPifmgr9;
