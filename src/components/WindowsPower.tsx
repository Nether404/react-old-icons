import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsPower: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsPower" {...props} />;
};

export default WindowsPower;
