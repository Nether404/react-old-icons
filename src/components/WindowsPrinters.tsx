import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsPrinters: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsPrinters" {...props} />;
};

export default WindowsPrinters;
