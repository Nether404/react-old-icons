import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsPrinters2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsPrinters2" {...props} />;
};

export default WindowsPrinters2;
