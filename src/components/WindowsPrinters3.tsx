import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsPrinters3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsPrinters3" {...props} />;
};

export default WindowsPrinters3;
