import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsProgman: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsProgman" {...props} />;
};

export default WindowsProgman;
