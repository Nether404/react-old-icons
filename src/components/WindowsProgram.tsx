import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsProgram: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsProgram" {...props} />;
};

export default WindowsProgram;
