import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsProgramGroup: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsProgramGroup" {...props} />;
};

export default WindowsProgramGroup;
