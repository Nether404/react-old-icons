import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsProgramGroup2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsProgramGroup2" {...props} />;
};

export default WindowsProgramGroup2;
