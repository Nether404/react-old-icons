import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsProgramGroup3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsProgramGroup3" {...props} />;
};

export default WindowsProgramGroup3;
