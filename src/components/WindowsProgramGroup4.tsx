import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsProgramGroup4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsProgramGroup4" {...props} />;
};

export default WindowsProgramGroup4;
