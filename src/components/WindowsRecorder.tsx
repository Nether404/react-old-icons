import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsRecorder: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsRecorder" {...props} />;
};

export default WindowsRecorder;
