import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsRecycle: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsRecycle" {...props} />;
};

export default WindowsRecycle;
