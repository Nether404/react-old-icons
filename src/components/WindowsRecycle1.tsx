import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsRecycle1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsRecycle1" {...props} />;
};

export default WindowsRecycle1;
