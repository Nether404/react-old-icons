import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsRecycleBin: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsRecycleBin" {...props} />;
};

export default WindowsRecycleBin;
