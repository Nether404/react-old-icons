import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsRecycleBin2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsRecycleBin2" {...props} />;
};

export default WindowsRecycleBin2;
