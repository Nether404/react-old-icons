import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsRegionalSettings: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsRegionalSettings" {...props} />;
};

export default WindowsRegionalSettings;
