import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsRegistry: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsRegistry" {...props} />;
};

export default WindowsRegistry;
