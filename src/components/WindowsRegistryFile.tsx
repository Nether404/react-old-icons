import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsRegistryFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsRegistryFile" {...props} />;
};

export default WindowsRegistryFile;
