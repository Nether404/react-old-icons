import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsReversi: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsReversi" {...props} />;
};

export default WindowsReversi;
