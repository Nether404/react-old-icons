import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsScrapObject: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsScrapObject" {...props} />;
};

export default WindowsScrapObject;
