import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsServer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsServer" {...props} />;
};

export default WindowsServer;
