import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsServer1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsServer1" {...props} />;
};

export default WindowsServer1;
