import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsServer2003Tsadmin: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsServer2003Tsadmin" {...props} />;
};

export default WindowsServer2003Tsadmin;
