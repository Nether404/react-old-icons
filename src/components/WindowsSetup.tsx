import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsSetup: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsSetup" {...props} />;
};

export default WindowsSetup;
