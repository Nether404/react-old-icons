import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsSetupapi: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsSetupapi" {...props} />;
};

export default WindowsSetupapi;
