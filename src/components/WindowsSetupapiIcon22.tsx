import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsSetupapiIcon22: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsSetupapiIcon22" {...props} />;
};

export default WindowsSetupapiIcon22;
