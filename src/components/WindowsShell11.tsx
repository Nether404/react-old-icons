import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell11: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell11" {...props} />;
};

export default WindowsShell11;
