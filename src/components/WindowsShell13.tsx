import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell13: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell13" {...props} />;
};

export default WindowsShell13;
