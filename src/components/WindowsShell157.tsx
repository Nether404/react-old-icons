import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell157: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell157" {...props} />;
};

export default WindowsShell157;
