import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell16: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell16" {...props} />;
};

export default WindowsShell16;
