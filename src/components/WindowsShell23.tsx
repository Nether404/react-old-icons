import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell23: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell23" {...props} />;
};

export default WindowsShell23;
