import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell26: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell26" {...props} />;
};

export default WindowsShell26;
