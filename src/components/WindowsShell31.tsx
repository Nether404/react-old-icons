import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell31: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell31" {...props} />;
};

export default WindowsShell31;
