import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell32Icon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell32Icon" {...props} />;
};

export default WindowsShell32Icon;
