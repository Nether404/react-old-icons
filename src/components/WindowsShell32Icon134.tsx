import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell32Icon134: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell32Icon134" {...props} />;
};

export default WindowsShell32Icon134;
