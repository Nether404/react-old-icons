import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell32Icon142: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell32Icon142" {...props} />;
};

export default WindowsShell32Icon142;
