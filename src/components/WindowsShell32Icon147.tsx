import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell32Icon147: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell32Icon147" {...props} />;
};

export default WindowsShell32Icon147;
