import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell32Icon148: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell32Icon148" {...props} />;
};

export default WindowsShell32Icon148;
