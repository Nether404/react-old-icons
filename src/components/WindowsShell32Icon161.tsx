import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell32Icon161: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell32Icon161" {...props} />;
};

export default WindowsShell32Icon161;
