import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell32Icon171: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell32Icon171" {...props} />;
};

export default WindowsShell32Icon171;
