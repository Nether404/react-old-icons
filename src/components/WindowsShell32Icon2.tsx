import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell32Icon2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell32Icon2" {...props} />;
};

export default WindowsShell32Icon2;
