import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell32Icon22: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell32Icon22" {...props} />;
};

export default WindowsShell32Icon22;
