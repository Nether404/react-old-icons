import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell32Icon24: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell32Icon24" {...props} />;
};

export default WindowsShell32Icon24;
