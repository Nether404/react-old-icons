import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell32Icon3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell32Icon3" {...props} />;
};

export default WindowsShell32Icon3;
