import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell40: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell40" {...props} />;
};

export default WindowsShell40;
