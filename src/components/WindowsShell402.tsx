import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell402: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell402" {...props} />;
};

export default WindowsShell402;
