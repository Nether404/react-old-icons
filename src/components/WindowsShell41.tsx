import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell41: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell41" {...props} />;
};

export default WindowsShell41;
