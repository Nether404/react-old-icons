import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell43: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell43" {...props} />;
};

export default WindowsShell43;
