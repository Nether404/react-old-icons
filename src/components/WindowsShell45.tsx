import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell45: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell45" {...props} />;
};

export default WindowsShell45;
