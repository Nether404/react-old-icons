import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell47: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell47" {...props} />;
};

export default WindowsShell47;
