import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShell8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShell8" {...props} />;
};

export default WindowsShell8;
