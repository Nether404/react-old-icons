import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShellIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShellIcon" {...props} />;
};

export default WindowsShellIcon;
