import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShellIcon142: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShellIcon142" {...props} />;
};

export default WindowsShellIcon142;
