import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShellIcon15: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShellIcon15" {...props} />;
};

export default WindowsShellIcon15;
