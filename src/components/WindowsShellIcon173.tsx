import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShellIcon173: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShellIcon173" {...props} />;
};

export default WindowsShellIcon173;
