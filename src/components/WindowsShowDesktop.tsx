import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShowDesktop: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShowDesktop" {...props} />;
};

export default WindowsShowDesktop;
