import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsShutDown: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsShutDown" {...props} />;
};

export default WindowsShutDown;
