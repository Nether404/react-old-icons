import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsSndrec32: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsSndrec32" {...props} />;
};

export default WindowsSndrec32;
