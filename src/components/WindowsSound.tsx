import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsSound: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsSound" {...props} />;
};

export default WindowsSound;
