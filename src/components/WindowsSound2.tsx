import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsSound2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsSound2" {...props} />;
};

export default WindowsSound2;
