import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsSpeechControlPanel: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsSpeechControlPanel" {...props} />;
};

export default WindowsSpeechControlPanel;
