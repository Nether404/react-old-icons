import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsSpeechRecognition: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsSpeechRecognition" {...props} />;
};

export default WindowsSpeechRecognition;
