import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsSrvMgr: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsSrvMgr" {...props} />;
};

export default WindowsSrvMgr;
