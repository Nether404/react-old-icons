import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsSticpl: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsSticpl" {...props} />;
};

export default WindowsSticpl;
