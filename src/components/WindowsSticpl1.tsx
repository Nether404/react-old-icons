import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsSticpl1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsSticpl1" {...props} />;
};

export default WindowsSticpl1;
