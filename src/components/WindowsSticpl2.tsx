import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsSticpl2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsSticpl2" {...props} />;
};

export default WindowsSticpl2;
