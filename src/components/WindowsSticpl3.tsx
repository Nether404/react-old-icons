import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsSticpl3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsSticpl3" {...props} />;
};

export default WindowsSticpl3;
