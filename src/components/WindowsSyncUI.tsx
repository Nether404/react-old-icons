import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsSyncUI: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsSyncUI" {...props} />;
};

export default WindowsSyncUI;
