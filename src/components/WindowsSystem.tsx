import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsSystem: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsSystem" {...props} />;
};

export default WindowsSystem;
