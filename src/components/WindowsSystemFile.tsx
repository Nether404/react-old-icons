import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsSystemFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsSystemFile" {...props} />;
};

export default WindowsSystemFile;
