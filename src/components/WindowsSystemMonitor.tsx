import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsSystemMonitor: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsSystemMonitor" {...props} />;
};

export default WindowsSystemMonitor;
