import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsTask: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsTask" {...props} />;
};

export default WindowsTask;
