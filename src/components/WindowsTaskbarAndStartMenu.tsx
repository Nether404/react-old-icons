import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsTaskbarAndStartMenu: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsTaskbarAndStartMenu" {...props} />;
};

export default WindowsTaskbarAndStartMenu;
