import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsTaskman: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsTaskman" {...props} />;
};

export default WindowsTaskman;
