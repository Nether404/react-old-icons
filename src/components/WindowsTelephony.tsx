import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsTelephony: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsTelephony" {...props} />;
};

export default WindowsTelephony;
