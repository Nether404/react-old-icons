import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsTelnet: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsTelnet" {...props} />;
};

export default WindowsTelnet;
