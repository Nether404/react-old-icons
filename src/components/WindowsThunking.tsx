import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsThunking: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsThunking" {...props} />;
};

export default WindowsThunking;
