import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsTour: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsTour" {...props} />;
};

export default WindowsTour;
