import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsTrueTypeFont: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsTrueTypeFont" {...props} />;
};

export default WindowsTrueTypeFont;
