import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsTrueTypeFont2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsTrueTypeFont2" {...props} />;
};

export default WindowsTrueTypeFont2;
