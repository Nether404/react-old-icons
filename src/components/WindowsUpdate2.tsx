import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsUpdate2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsUpdate2" {...props} />;
};

export default WindowsUpdate2;
