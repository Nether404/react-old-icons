import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsUserPictures2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsUserPictures2" {...props} />;
};

export default WindowsUserPictures2;
