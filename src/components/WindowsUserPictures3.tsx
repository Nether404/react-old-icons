import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsUserPictures3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsUserPictures3" {...props} />;
};

export default WindowsUserPictures3;
