import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsUserPictures4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsUserPictures4" {...props} />;
};

export default WindowsUserPictures4;
