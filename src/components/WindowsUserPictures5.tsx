import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsUserPictures5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsUserPictures5" {...props} />;
};

export default WindowsUserPictures5;
