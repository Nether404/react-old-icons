import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsUserPictures6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsUserPictures6" {...props} />;
};

export default WindowsUserPictures6;
