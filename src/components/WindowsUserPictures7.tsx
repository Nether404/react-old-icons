import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsUserPictures7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsUserPictures7" {...props} />;
};

export default WindowsUserPictures7;
