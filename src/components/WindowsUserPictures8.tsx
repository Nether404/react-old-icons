import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsUserPictures8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsUserPictures8" {...props} />;
};

export default WindowsUserPictures8;
