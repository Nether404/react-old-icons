import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsUserPictures9: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsUserPictures9" {...props} />;
};

export default WindowsUserPictures9;
