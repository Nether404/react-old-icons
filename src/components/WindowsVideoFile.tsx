import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVideoFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVideoFile" {...props} />;
};

export default WindowsVideoFile;
