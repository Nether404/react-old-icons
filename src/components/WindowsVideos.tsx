import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVideos: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVideos" {...props} />;
};

export default WindowsVideos;
