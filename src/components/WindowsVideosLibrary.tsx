import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVideosLibrary: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVideosLibrary" {...props} />;
};

export default WindowsVideosLibrary;
