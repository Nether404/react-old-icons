import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaAnytimeUpgrade: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaAnytimeUpgrade" {...props} />;
};

export default WindowsVistaAnytimeUpgrade;
