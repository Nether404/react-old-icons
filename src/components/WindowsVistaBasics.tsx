import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaBasics: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaBasics" {...props} />;
};

export default WindowsVistaBasics;
