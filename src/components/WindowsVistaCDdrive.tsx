import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaCDdrive: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaCDdrive" {...props} />;
};

export default WindowsVistaCDdrive;
