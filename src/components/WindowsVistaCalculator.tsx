import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaCalculator: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaCalculator" {...props} />;
};

export default WindowsVistaCalculator;
