import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaCalendar: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaCalendar" {...props} />;
};

export default WindowsVistaCalendar;
