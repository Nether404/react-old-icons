import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaCompactFlash: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaCompactFlash" {...props} />;
};

export default WindowsVistaCompactFlash;
