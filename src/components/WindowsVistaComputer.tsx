import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaComputer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaComputer" {...props} />;
};

export default WindowsVistaComputer;
