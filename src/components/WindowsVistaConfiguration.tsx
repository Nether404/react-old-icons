import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaConfiguration: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaConfiguration" {...props} />;
};

export default WindowsVistaConfiguration;
