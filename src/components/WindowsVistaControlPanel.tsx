import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaControlPanel: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaControlPanel" {...props} />;
};

export default WindowsVistaControlPanel;
