import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaDVD: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaDVD" {...props} />;
};

export default WindowsVistaDVD;
