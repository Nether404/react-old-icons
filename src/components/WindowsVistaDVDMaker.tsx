import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaDVDMaker: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaDVDMaker" {...props} />;
};

export default WindowsVistaDVDMaker;
