import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaDemos: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaDemos" {...props} />;
};

export default WindowsVistaDemos;
