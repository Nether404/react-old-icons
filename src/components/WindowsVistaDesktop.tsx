import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaDesktop: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaDesktop" {...props} />;
};

export default WindowsVistaDesktop;
