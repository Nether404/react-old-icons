import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaDocuments: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaDocuments" {...props} />;
};

export default WindowsVistaDocuments;
