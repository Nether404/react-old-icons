import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaDownloads: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaDownloads" {...props} />;
};

export default WindowsVistaDownloads;
