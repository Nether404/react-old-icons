import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaEventViewer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaEventViewer" {...props} />;
};

export default WindowsVistaEventViewer;
