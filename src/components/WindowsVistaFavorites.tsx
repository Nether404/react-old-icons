import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaFavorites: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaFavorites" {...props} />;
};

export default WindowsVistaFavorites;
