import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaFax: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaFax" {...props} />;
};

export default WindowsVistaFax;
