import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaFloppyDrive: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaFloppyDrive" {...props} />;
};

export default WindowsVistaFloppyDrive;
