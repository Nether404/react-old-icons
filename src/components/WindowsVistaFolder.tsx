import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaFolder: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaFolder" {...props} />;
};

export default WindowsVistaFolder;
