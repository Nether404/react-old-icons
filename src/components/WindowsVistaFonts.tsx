import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaFonts: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaFonts" {...props} />;
};

export default WindowsVistaFonts;
