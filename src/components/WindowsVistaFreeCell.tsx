import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaFreeCell: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaFreeCell" {...props} />;
};

export default WindowsVistaFreeCell;
