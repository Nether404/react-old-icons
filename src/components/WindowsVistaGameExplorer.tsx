import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaGameExplorer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaGameExplorer" {...props} />;
};

export default WindowsVistaGameExplorer;
