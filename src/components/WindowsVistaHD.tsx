import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaHD: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaHD" {...props} />;
};

export default WindowsVistaHD;
