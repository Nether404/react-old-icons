import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaHearts: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaHearts" {...props} />;
};

export default WindowsVistaHearts;
