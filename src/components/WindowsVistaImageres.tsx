import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaImageres: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaImageres" {...props} />;
};

export default WindowsVistaImageres;
