import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaImageres10: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaImageres10" {...props} />;
};

export default WindowsVistaImageres10;
