import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaImageres2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaImageres2" {...props} />;
};

export default WindowsVistaImageres2;
