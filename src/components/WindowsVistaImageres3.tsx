import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaImageres3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaImageres3" {...props} />;
};

export default WindowsVistaImageres3;
