import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaImageres4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaImageres4" {...props} />;
};

export default WindowsVistaImageres4;
