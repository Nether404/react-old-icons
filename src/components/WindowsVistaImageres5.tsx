import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaImageres5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaImageres5" {...props} />;
};

export default WindowsVistaImageres5;
