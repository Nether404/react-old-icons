import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaImageres6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaImageres6" {...props} />;
};

export default WindowsVistaImageres6;
