import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaImageres7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaImageres7" {...props} />;
};

export default WindowsVistaImageres7;
