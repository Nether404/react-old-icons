import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaImageres8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaImageres8" {...props} />;
};

export default WindowsVistaImageres8;
