import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaImageres9: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaImageres9" {...props} />;
};

export default WindowsVistaImageres9;
