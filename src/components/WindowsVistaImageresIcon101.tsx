import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaImageresIcon101: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaImageresIcon101" {...props} />;
};

export default WindowsVistaImageresIcon101;
