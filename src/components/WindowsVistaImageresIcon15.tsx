import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaImageresIcon15: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaImageresIcon15" {...props} />;
};

export default WindowsVistaImageresIcon15;
