import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaImageresIcon155: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaImageresIcon155" {...props} />;
};

export default WindowsVistaImageresIcon155;
