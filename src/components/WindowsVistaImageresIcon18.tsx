import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaImageresIcon18: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaImageresIcon18" {...props} />;
};

export default WindowsVistaImageresIcon18;
