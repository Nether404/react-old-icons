import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaImageresIcon47: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaImageresIcon47" {...props} />;
};

export default WindowsVistaImageresIcon47;
