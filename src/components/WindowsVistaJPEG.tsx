import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaJPEG: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaJPEG" {...props} />;
};

export default WindowsVistaJPEG;
