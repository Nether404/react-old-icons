import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaJournal: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaJournal" {...props} />;
};

export default WindowsVistaJournal;
