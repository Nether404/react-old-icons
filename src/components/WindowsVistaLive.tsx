import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaLive: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaLive" {...props} />;
};

export default WindowsVistaLive;
