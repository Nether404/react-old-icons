import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaLiveMessenger: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaLiveMessenger" {...props} />;
};

export default WindowsVistaLiveMessenger;
