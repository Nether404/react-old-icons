import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaMail: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaMail" {...props} />;
};

export default WindowsVistaMail;
