import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaMainIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaMainIcon" {...props} />;
};

export default WindowsVistaMainIcon;
