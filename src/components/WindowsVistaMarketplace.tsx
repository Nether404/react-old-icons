import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaMarketplace: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaMarketplace" {...props} />;
};

export default WindowsVistaMarketplace;
