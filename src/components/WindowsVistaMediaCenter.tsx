import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaMediaCenter: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaMediaCenter" {...props} />;
};

export default WindowsVistaMediaCenter;
