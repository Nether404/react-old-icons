import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaMediaPlayer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaMediaPlayer" {...props} />;
};

export default WindowsVistaMediaPlayer;
