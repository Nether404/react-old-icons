import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaModem: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaModem" {...props} />;
};

export default WindowsVistaModem;
