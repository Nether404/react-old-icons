import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaMouse: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaMouse" {...props} />;
};

export default WindowsVistaMouse;
