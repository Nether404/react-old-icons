import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaMovieMaker: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaMovieMaker" {...props} />;
};

export default WindowsVistaMovieMaker;
