import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaNetworkDrive: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaNetworkDrive" {...props} />;
};

export default WindowsVistaNetworkDrive;
