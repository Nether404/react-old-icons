import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaNetworkmap: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaNetworkmap" {...props} />;
};

export default WindowsVistaNetworkmap;
