import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaNetworkmap2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaNetworkmap2" {...props} />;
};

export default WindowsVistaNetworkmap2;
