import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaNetworkmap3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaNetworkmap3" {...props} />;
};

export default WindowsVistaNetworkmap3;
