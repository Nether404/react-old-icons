import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaNetworkmap4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaNetworkmap4" {...props} />;
};

export default WindowsVistaNetworkmap4;
