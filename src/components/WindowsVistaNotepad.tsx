import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaNotepad: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaNotepad" {...props} />;
};

export default WindowsVistaNotepad;
