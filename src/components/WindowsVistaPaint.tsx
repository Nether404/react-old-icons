import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaPaint: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaPaint" {...props} />;
};

export default WindowsVistaPaint;
