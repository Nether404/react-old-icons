import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaPersonalization: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaPersonalization" {...props} />;
};

export default WindowsVistaPersonalization;
