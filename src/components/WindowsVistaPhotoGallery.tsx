import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaPhotoGallery: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaPhotoGallery" {...props} />;
};

export default WindowsVistaPhotoGallery;
