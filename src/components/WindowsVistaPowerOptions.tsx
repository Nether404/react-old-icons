import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaPowerOptions: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaPowerOptions" {...props} />;
};

export default WindowsVistaPowerOptions;
