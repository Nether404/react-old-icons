import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaRecycleBin: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaRecycleBin" {...props} />;
};

export default WindowsVistaRecycleBin;
