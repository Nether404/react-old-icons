import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaRecycleBinEmpty: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaRecycleBinEmpty" {...props} />;
};

export default WindowsVistaRecycleBinEmpty;
