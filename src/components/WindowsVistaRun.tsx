import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaRun: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaRun" {...props} />;
};

export default WindowsVistaRun;
