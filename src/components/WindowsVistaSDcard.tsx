import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaSDcard: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaSDcard" {...props} />;
};

export default WindowsVistaSDcard;
