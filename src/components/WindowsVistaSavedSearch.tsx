import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaSavedSearch: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaSavedSearch" {...props} />;
};

export default WindowsVistaSavedSearch;
