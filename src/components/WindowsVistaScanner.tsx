import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaScanner: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaScanner" {...props} />;
};

export default WindowsVistaScanner;
