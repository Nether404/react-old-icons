import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaSetup: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaSetup" {...props} />;
};

export default WindowsVistaSetup;
