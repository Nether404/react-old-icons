import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaShell32: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaShell32" {...props} />;
};

export default WindowsVistaShell32;
