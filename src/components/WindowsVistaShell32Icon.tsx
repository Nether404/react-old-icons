import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaShell32Icon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaShell32Icon" {...props} />;
};

export default WindowsVistaShell32Icon;
