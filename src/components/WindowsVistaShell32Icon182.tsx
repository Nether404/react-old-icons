import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaShell32Icon182: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaShell32Icon182" {...props} />;
};

export default WindowsVistaShell32Icon182;
