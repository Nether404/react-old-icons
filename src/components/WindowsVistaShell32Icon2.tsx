import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaShell32Icon2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaShell32Icon2" {...props} />;
};

export default WindowsVistaShell32Icon2;
