import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaShell32Icon262: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaShell32Icon262" {...props} />;
};

export default WindowsVistaShell32Icon262;
