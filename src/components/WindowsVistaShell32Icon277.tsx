import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaShell32Icon277: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaShell32Icon277" {...props} />;
};

export default WindowsVistaShell32Icon277;
