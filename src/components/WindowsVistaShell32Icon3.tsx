import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaShell32Icon3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaShell32Icon3" {...props} />;
};

export default WindowsVistaShell32Icon3;
