import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaShellIcon184: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaShellIcon184" {...props} />;
};

export default WindowsVistaShellIcon184;
