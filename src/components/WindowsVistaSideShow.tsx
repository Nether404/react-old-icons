import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaSideShow: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaSideShow" {...props} />;
};

export default WindowsVistaSideShow;
