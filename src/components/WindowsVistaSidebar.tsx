import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaSidebar: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaSidebar" {...props} />;
};

export default WindowsVistaSidebar;
