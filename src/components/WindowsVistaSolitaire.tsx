import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaSolitaire: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaSolitaire" {...props} />;
};

export default WindowsVistaSolitaire;
