import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaSpiderSolitaire: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaSpiderSolitaire" {...props} />;
};

export default WindowsVistaSpiderSolitaire;
