import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaStickyNotes: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaStickyNotes" {...props} />;
};

export default WindowsVistaStickyNotes;
