import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaSyncCenter: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaSyncCenter" {...props} />;
};

export default WindowsVistaSyncCenter;
