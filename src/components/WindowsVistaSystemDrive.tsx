import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaSystemDrive: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaSystemDrive" {...props} />;
};

export default WindowsVistaSystemDrive;
