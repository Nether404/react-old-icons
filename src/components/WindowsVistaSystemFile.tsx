import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaSystemFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaSystemFile" {...props} />;
};

export default WindowsVistaSystemFile;
