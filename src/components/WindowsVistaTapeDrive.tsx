import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaTapeDrive: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaTapeDrive" {...props} />;
};

export default WindowsVistaTapeDrive;
