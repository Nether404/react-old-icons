import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaTextFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaTextFile" {...props} />;
};

export default WindowsVistaTextFile;
