import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaUserPictures: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaUserPictures" {...props} />;
};

export default WindowsVistaUserPictures;
