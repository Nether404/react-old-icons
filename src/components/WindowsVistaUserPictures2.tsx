import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaUserPictures2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaUserPictures2" {...props} />;
};

export default WindowsVistaUserPictures2;
