import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaUserPictures4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaUserPictures4" {...props} />;
};

export default WindowsVistaUserPictures4;
