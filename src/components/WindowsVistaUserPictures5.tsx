import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaUserPictures5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaUserPictures5" {...props} />;
};

export default WindowsVistaUserPictures5;
