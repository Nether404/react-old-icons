import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaVideo: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaVideo" {...props} />;
};

export default WindowsVistaVideo;
