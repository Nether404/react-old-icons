import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaWelcomeCenter: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaWelcomeCenter" {...props} />;
};

export default WindowsVistaWelcomeCenter;
