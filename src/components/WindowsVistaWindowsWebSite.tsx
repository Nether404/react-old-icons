import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaWindowsWebSite: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaWindowsWebSite" {...props} />;
};

export default WindowsVistaWindowsWebSite;
