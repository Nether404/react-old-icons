import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaWordPad: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaWordPad" {...props} />;
};

export default WindowsVistaWordPad;
