import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaWordPadDocument: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaWordPadDocument" {...props} />;
};

export default WindowsVistaWordPadDocument;
