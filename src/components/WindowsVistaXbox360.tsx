import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVistaXbox360: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVistaXbox360" {...props} />;
};

export default WindowsVistaXbox360;
