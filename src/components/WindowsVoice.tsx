import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsVoice: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsVoice" {...props} />;
};

export default WindowsVoice;
