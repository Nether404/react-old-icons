import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsWebFolders: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsWebFolders" {...props} />;
};

export default WindowsWebFolders;
