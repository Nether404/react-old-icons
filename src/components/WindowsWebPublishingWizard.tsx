import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsWebPublishingWizard: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsWebPublishingWizard" {...props} />;
};

export default WindowsWebPublishingWizard;
