import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsWebTV: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsWebTV" {...props} />;
};

export default WindowsWebTV;
