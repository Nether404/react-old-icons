import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsWindows: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsWindows" {...props} />;
};

export default WindowsWindows;
