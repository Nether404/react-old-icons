import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsWrite: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsWrite" {...props} />;
};

export default WindowsWrite;
