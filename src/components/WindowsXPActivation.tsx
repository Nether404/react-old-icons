import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPActivation: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPActivation" {...props} />;
};

export default WindowsXPActivation;
