import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPAddHardware: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPAddHardware" {...props} />;
};

export default WindowsXPAddHardware;
