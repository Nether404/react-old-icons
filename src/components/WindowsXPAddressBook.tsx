import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPAddressBook: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPAddressBook" {...props} />;
};

export default WindowsXPAddressBook;
