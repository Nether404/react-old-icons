import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPAudioCD: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPAudioCD" {...props} />;
};

export default WindowsXPAudioCD;
