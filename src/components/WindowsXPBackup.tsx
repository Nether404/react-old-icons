import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPBackup: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPBackup" {...props} />;
};

export default WindowsXPBackup;
