import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPBitmapImage: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPBitmapImage" {...props} />;
};

export default WindowsXPBitmapImage;
