import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPBluetooth: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPBluetooth" {...props} />;
};

export default WindowsXPBluetooth;
