import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPCDR: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPCDR" {...props} />;
};

export default WindowsXPCDR;
