import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPCalculator: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPCalculator" {...props} />;
};

export default WindowsXPCalculator;
