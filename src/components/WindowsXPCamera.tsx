import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPCamera: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPCamera" {...props} />;
};

export default WindowsXPCamera;
