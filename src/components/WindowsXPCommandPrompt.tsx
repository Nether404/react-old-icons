import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPCommandPrompt: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPCommandPrompt" {...props} />;
};

export default WindowsXPCommandPrompt;
