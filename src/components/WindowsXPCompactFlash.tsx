import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPCompactFlash: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPCompactFlash" {...props} />;
};

export default WindowsXPCompactFlash;
