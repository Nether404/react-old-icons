import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPComres: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPComres" {...props} />;
};

export default WindowsXPComres;
