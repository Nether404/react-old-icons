import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPComres2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPComres2" {...props} />;
};

export default WindowsXPComres2;
