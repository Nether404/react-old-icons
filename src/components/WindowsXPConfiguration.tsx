import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPConfiguration: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPConfiguration" {...props} />;
};

export default WindowsXPConfiguration;
