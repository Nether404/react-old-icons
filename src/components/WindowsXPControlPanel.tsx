import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPControlPanel: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPControlPanel" {...props} />;
};

export default WindowsXPControlPanel;
