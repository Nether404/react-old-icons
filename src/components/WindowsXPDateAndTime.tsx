import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPDateAndTime: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPDateAndTime" {...props} />;
};

export default WindowsXPDateAndTime;
