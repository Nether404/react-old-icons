import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPDialer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPDialer" {...props} />;
};

export default WindowsXPDialer;
