import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPDisplay: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPDisplay" {...props} />;
};

export default WindowsXPDisplay;
