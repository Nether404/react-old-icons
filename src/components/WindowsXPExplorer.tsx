import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPExplorer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPExplorer" {...props} />;
};

export default WindowsXPExplorer;
