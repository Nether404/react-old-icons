import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPExplorer2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPExplorer2" {...props} />;
};

export default WindowsXPExplorer2;
