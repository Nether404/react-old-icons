import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPFavorite: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPFavorite" {...props} />;
};

export default WindowsXPFavorite;
