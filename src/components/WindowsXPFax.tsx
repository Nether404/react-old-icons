import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPFax: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPFax" {...props} />;
};

export default WindowsXPFax;
