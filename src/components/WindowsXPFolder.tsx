import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPFolder: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPFolder" {...props} />;
};

export default WindowsXPFolder;
