import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPFolderOpen: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPFolderOpen" {...props} />;
};

export default WindowsXPFolderOpen;
