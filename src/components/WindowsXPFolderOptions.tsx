import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPFolderOptions: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPFolderOptions" {...props} />;
};

export default WindowsXPFolderOptions;
