import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPFont: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPFont" {...props} />;
};

export default WindowsXPFont;
