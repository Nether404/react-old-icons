import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPFonts: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPFonts" {...props} />;
};

export default WindowsXPFonts;
