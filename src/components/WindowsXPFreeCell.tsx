import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPFreeCell: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPFreeCell" {...props} />;
};

export default WindowsXPFreeCell;
