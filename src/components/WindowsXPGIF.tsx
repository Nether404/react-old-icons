import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPGIF: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPGIF" {...props} />;
};

export default WindowsXPGIF;
