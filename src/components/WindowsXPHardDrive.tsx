import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPHardDrive: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPHardDrive" {...props} />;
};

export default WindowsXPHardDrive;
