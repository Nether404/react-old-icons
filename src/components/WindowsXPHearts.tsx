import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPHearts: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPHearts" {...props} />;
};

export default WindowsXPHearts;
