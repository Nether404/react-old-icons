import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPHelp: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPHelp" {...props} />;
};

export default WindowsXPHelp;
