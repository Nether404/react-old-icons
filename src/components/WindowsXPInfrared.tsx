import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPInfrared: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPInfrared" {...props} />;
};

export default WindowsXPInfrared;
