import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPInternetHearts: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPInternetHearts" {...props} />;
};

export default WindowsXPInternetHearts;
