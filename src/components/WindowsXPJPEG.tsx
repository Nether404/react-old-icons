import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPJPEG: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPJPEG" {...props} />;
};

export default WindowsXPJPEG;
