import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPLogOff: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPLogOff" {...props} />;
};

export default WindowsXPLogOff;
