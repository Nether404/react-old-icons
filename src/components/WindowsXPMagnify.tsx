import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPMagnify: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPMagnify" {...props} />;
};

export default WindowsXPMagnify;
