import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPMail: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPMail" {...props} />;
};

export default WindowsXPMail;
