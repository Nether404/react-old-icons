import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPMediaCenter: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPMediaCenter" {...props} />;
};

export default WindowsXPMediaCenter;
