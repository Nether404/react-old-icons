import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPModem: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPModem" {...props} />;
};

export default WindowsXPModem;
