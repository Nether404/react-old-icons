import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPMouse: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPMouse" {...props} />;
};

export default WindowsXPMouse;
