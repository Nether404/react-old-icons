import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPMovieMaker: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPMovieMaker" {...props} />;
};

export default WindowsXPMovieMaker;
