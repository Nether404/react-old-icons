import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPMute: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPMute" {...props} />;
};

export default WindowsXPMute;
