import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPMyComputer2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPMyComputer2" {...props} />;
};

export default WindowsXPMyComputer2;
