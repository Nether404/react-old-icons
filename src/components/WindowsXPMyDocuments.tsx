import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPMyDocuments: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPMyDocuments" {...props} />;
};

export default WindowsXPMyDocuments;
