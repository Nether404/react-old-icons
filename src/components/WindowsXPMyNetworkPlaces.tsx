import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPMyNetworkPlaces: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPMyNetworkPlaces" {...props} />;
};

export default WindowsXPMyNetworkPlaces;
