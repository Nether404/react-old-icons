import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPMyPictures: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPMyPictures" {...props} />;
};

export default WindowsXPMyPictures;
