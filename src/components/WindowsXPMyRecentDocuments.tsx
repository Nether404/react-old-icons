import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPMyRecentDocuments: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPMyRecentDocuments" {...props} />;
};

export default WindowsXPMyRecentDocuments;
