import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPMycomput: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPMycomput" {...props} />;
};

export default WindowsXPMycomput;
