import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPNarrator: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPNarrator" {...props} />;
};

export default WindowsXPNarrator;
