import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPNetshell: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPNetshell" {...props} />;
};

export default WindowsXPNetshell;
