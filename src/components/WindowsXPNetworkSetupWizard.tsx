import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPNetworkSetupWizard: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPNetworkSetupWizard" {...props} />;
};

export default WindowsXPNetworkSetupWizard;
