import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPPaint: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPPaint" {...props} />;
};

export default WindowsXPPaint;
