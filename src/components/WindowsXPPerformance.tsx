import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPPerformance: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPPerformance" {...props} />;
};

export default WindowsXPPerformance;
