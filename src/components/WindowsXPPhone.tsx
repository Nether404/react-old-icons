import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPPhone: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPPhone" {...props} />;
};

export default WindowsXPPhone;
