import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPPhotoViewer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPPhotoViewer" {...props} />;
};

export default WindowsXPPhotoViewer;
