import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPPinball: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPPinball" {...props} />;
};

export default WindowsXPPinball;
