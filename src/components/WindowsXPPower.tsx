import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPPower: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPPower" {...props} />;
};

export default WindowsXPPower;
