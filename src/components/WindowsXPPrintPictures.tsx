import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPPrintPictures: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPPrintPictures" {...props} />;
};

export default WindowsXPPrintPictures;
