import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPPrinter: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPPrinter" {...props} />;
};

export default WindowsXPPrinter;
