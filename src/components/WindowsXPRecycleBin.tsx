import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPRecycleBin: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPRecycleBin" {...props} />;
};

export default WindowsXPRecycleBin;
