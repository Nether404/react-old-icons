import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPRemoteAssistance: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPRemoteAssistance" {...props} />;
};

export default WindowsXPRemoteAssistance;
