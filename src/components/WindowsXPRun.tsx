import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPRun: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPRun" {...props} />;
};

export default WindowsXPRun;
