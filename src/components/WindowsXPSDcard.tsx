import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPSDcard: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPSDcard" {...props} />;
};

export default WindowsXPSDcard;
