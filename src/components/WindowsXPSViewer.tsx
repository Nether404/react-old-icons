import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPSViewer: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPSViewer" {...props} />;
};

export default WindowsXPSViewer;
