import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPScanner: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPScanner" {...props} />;
};

export default WindowsXPScanner;
