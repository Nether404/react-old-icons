import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPSecurityCenter: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPSecurityCenter" {...props} />;
};

export default WindowsXPSecurityCenter;
