import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPShell32: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPShell32" {...props} />;
};

export default WindowsXPShell32;
