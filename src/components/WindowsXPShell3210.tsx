import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPShell3210: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPShell3210" {...props} />;
};

export default WindowsXPShell3210;
