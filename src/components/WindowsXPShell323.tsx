import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPShell323: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPShell323" {...props} />;
};

export default WindowsXPShell323;
