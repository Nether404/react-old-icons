import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPShell324: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPShell324" {...props} />;
};

export default WindowsXPShell324;
