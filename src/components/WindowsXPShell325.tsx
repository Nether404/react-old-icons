import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPShell325: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPShell325" {...props} />;
};

export default WindowsXPShell325;
