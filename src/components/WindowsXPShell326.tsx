import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPShell326: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPShell326" {...props} />;
};

export default WindowsXPShell326;
