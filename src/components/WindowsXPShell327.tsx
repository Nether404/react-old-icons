import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPShell327: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPShell327" {...props} />;
};

export default WindowsXPShell327;
