import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPShell328: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPShell328" {...props} />;
};

export default WindowsXPShell328;
