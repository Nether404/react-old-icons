import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPShell329: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPShell329" {...props} />;
};

export default WindowsXPShell329;
