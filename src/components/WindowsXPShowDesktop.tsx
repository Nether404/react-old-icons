import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPShowDesktop: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPShowDesktop" {...props} />;
};

export default WindowsXPShowDesktop;
