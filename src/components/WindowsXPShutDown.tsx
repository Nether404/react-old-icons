import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPShutDown: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPShutDown" {...props} />;
};

export default WindowsXPShutDown;
