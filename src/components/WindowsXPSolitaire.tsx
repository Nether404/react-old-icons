import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPSolitaire: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPSolitaire" {...props} />;
};

export default WindowsXPSolitaire;
