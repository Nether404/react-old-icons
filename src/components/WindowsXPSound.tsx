import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPSound: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPSound" {...props} />;
};

export default WindowsXPSound;
