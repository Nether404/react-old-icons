import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPSpiderSolitaire: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPSpiderSolitaire" {...props} />;
};

export default WindowsXPSpiderSolitaire;
