import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPSystemFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPSystemFile" {...props} />;
};

export default WindowsXPSystemFile;
