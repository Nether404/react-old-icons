import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPSystemInformation: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPSystemInformation" {...props} />;
};

export default WindowsXPSystemInformation;
