import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPSystemRestore: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPSystemRestore" {...props} />;
};

export default WindowsXPSystemRestore;
