import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPTextFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPTextFile" {...props} />;
};

export default WindowsXPTextFile;
