import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPTheme: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPTheme" {...props} />;
};

export default WindowsXPTheme;
