import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPTrueTypeFont: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPTrueTypeFont" {...props} />;
};

export default WindowsXPTrueTypeFont;
