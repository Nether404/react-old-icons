import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPUPS: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPUPS" {...props} />;
};

export default WindowsXPUPS;
