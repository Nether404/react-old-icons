import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPUsers: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPUsers" {...props} />;
};

export default WindowsXPUsers;
