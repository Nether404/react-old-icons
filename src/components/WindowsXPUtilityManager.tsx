import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPUtilityManager: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPUtilityManager" {...props} />;
};

export default WindowsXPUtilityManager;
