import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPWindowsCatalog: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPWindowsCatalog" {...props} />;
};

export default WindowsXPWindowsCatalog;
