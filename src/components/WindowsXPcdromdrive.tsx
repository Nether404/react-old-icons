import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPcdromdrive: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPcdromdrive" {...props} />;
};

export default WindowsXPcdromdrive;
