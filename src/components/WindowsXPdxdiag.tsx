import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPdxdiag: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPdxdiag" {...props} />;
};

export default WindowsXPdxdiag;
