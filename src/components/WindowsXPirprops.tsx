import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPirprops: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPirprops" {...props} />;
};

export default WindowsXPirprops;
