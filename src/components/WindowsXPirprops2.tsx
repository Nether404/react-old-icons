import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPirprops2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPirprops2" {...props} />;
};

export default WindowsXPirprops2;
