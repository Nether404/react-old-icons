import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPprintui: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPprintui" {...props} />;
};

export default WindowsXPprintui;
