import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPtiff: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPtiff" {...props} />;
};

export default WindowsXPtiff;
