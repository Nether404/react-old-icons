import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPwmploc: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPwmploc" {...props} />;
};

export default WindowsXPwmploc;
