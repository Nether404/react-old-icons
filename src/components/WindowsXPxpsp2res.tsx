import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsXPxpsp2res: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsXPxpsp2res" {...props} />;
};

export default WindowsXPxpsp2res;
