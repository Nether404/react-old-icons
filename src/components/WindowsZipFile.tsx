import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WindowsZipFile: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WindowsZipFile" {...props} />;
};

export default WindowsZipFile;
