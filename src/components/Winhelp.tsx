import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Winhelp: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Winhelp" {...props} />;
};

export default Winhelp;
