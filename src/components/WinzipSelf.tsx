import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WinzipSelf: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WinzipSelf" {...props} />;
};

export default WinzipSelf;
