import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WiredSound1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WiredSound1" {...props} />;
};

export default WiredSound1;
