import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WiseUninstall: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WiseUninstall" {...props} />;
};

export default WiseUninstall;
