import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Wolfenstein: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Wolfenstein" {...props} />;
};

export default Wolfenstein;
