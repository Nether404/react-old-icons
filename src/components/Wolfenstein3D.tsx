import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Wolfenstein3D: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Wolfenstein3D" {...props} />;
};

export default Wolfenstein3D;
