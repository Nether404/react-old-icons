import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WonSwapInstall: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WonSwapInstall" {...props} />;
};

export default WonSwapInstall;
