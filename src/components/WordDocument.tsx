import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WordDocument: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WordDocument" {...props} />;
};

export default WordDocument;
