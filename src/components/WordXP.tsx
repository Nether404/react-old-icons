import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WordXP: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WordXP" {...props} />;
};

export default WordXP;
