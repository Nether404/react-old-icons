import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WordZap: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WordZap" {...props} />;
};

export default WordZap;
