import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WordpadXP: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WordpadXP" {...props} />;
};

export default WordpadXP;
