import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const WorldTraveler: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="WorldTraveler" {...props} />;
};

export default WorldTraveler;
