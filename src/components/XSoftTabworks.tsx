import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const XSoftTabworks: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="XSoftTabworks" {...props} />;
};

export default XSoftTabworks;
