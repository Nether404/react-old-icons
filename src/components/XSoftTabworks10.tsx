import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const XSoftTabworks10: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="XSoftTabworks10" {...props} />;
};

export default XSoftTabworks10;
