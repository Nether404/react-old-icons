import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const XSoftTabworks11: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="XSoftTabworks11" {...props} />;
};

export default XSoftTabworks11;
