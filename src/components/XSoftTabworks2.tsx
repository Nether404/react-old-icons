import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const XSoftTabworks2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="XSoftTabworks2" {...props} />;
};

export default XSoftTabworks2;
