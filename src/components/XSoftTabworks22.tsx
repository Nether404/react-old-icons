import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const XSoftTabworks22: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="XSoftTabworks22" {...props} />;
};

export default XSoftTabworks22;
