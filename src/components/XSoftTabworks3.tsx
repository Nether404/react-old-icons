import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const XSoftTabworks3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="XSoftTabworks3" {...props} />;
};

export default XSoftTabworks3;
