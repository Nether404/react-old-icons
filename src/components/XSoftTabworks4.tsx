import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const XSoftTabworks4: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="XSoftTabworks4" {...props} />;
};

export default XSoftTabworks4;
