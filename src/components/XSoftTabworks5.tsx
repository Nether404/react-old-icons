import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const XSoftTabworks5: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="XSoftTabworks5" {...props} />;
};

export default XSoftTabworks5;
