import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const XSoftTabworks6: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="XSoftTabworks6" {...props} />;
};

export default XSoftTabworks6;
