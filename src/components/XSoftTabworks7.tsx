import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const XSoftTabworks7: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="XSoftTabworks7" {...props} />;
};

export default XSoftTabworks7;
