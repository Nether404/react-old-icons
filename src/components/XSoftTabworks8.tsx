import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const XSoftTabworks8: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="XSoftTabworks8" {...props} />;
};

export default XSoftTabworks8;
