import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const XSoftTabworks9: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="XSoftTabworks9" {...props} />;
};

export default XSoftTabworks9;
