import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const XVI32: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="XVI32" {...props} />;
};

export default XVI32;
