import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const XaraWebstyle: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="XaraWebstyle" {...props} />;
};

export default XaraWebstyle;
