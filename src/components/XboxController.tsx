import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const XboxController: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="XboxController" {...props} />;
};

export default XboxController;
