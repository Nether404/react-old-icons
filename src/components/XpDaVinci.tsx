import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const XpDaVinci: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="XpDaVinci" {...props} />;
};

export default XpDaVinci;
