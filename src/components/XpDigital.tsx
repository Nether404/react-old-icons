import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const XpDigital: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="XpDigital" {...props} />;
};

export default XpDigital;
