import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const XpLabyrinth: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="XpLabyrinth" {...props} />;
};

export default XpLabyrinth;
