import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const XpNature: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="XpNature" {...props} />;
};

export default XpNature;
