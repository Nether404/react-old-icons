import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const XpSpace: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="XpSpace" {...props} />;
};

export default XpSpace;
