import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const XpSpace1: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="XpSpace1" {...props} />;
};

export default XpSpace1;
