import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const YahooMessenger2001: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="YahooMessenger2001" {...props} />;
};

export default YahooMessenger2001;
