import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const YetAnotherTetris: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="YetAnotherTetris" {...props} />;
};

export default YetAnotherTetris;
