import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const YouBetYourHead: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="YouBetYourHead" {...props} />;
};

export default YouBetYourHead;
