import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const YouDontKnowJack: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="YouDontKnowJack" {...props} />;
};

export default YouDontKnowJack;
