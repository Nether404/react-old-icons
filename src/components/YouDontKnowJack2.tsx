import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const YouDontKnowJack2: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="YouDontKnowJack2" {...props} />;
};

export default YouDontKnowJack2;
