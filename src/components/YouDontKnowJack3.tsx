import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const YouDontKnowJack3: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="YouDontKnowJack3" {...props} />;
};

export default YouDontKnowJack3;
