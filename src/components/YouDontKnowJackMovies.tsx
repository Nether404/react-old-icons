import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const YouDontKnowJackMovies: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="YouDontKnowJackMovies" {...props} />;
};

export default YouDontKnowJackMovies;
