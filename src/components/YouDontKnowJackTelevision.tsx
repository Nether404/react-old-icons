import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const YouDontKnowJackTelevision: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="YouDontKnowJackTelevision" {...props} />;
};

export default YouDontKnowJackTelevision;
