import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ZSNES: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ZSNES" {...props} />;
};

export default ZSNES;
