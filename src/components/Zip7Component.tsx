import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const Zip7Component: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="Zip7Component" {...props} />;
};

export default Zip7Component;
