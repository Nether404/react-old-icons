import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ZipArchive: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ZipArchive" {...props} />;
};

export default ZipArchive;
