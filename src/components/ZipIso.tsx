import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ZipIso: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ZipIso" {...props} />;
};

export default ZipIso;
