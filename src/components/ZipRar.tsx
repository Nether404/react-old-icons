import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ZipRar: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ZipRar" {...props} />;
};

export default ZipRar;
