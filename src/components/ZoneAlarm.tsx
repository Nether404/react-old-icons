import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ZoneAlarm: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ZoneAlarm" {...props} />;
};

export default ZoneAlarm;
