import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ZooTycoon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ZooTycoon" {...props} />;
};

export default ZooTycoon;
