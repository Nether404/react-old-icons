import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ZooTycoonI: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ZooTycoonI" {...props} />;
};

export default ZooTycoonI;
