import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ZooommmIcon: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ZooommmIcon" {...props} />;
};

export default ZooommmIcon;
