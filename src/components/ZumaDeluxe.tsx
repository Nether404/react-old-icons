import React from 'react';
import OldIcon, { OldIconProps } from '../OldIcon';

const ZumaDeluxe: React.FC<OldIconProps> = (props) => {
  return <OldIcon name="ZumaDeluxe" {...props} />;
};

export default ZumaDeluxe;
